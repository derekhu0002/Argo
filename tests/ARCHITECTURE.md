# Intent Query Test Contract

This local contract refines `OVERALL_ARCHITECTURE.md`.

## Responsibilities

- `tests/harness/intentArchitectureQueryHarness.js` hides MCP response parsing, canonical-file access, and invocation plumbing behind business-readable methods.
- `tests/explicit/entries/` owns the four frozen DT-01/02/03/12 entrypoints.
- `tests/architecture/intent-query/` owns frozen critical non-explicit guardrails for boundaries, dependency direction, entry correctness, and traceability.
- Explicit entrypoints preserve GIVEN / WHEN / THEN, semantic data names, control points, observation points, and readable business failure categories.
- `tests/harness/productionGraphRagHarness.js` hides runtime composition, external configuration, injected Neo4j projection behavior, and canonical fixture plumbing for the W2 production boundary.
- C1-C4 use independently callable configuration, qualification, canonical-authority, and native-retrieval public boundaries so missing runtime composition cannot mask their completion signals.
- TS-06 requires `approvedByHuman === true`, rejects non-boolean truthy values, trims provider/model/version, requires positive-integer dimensions without coercion, and separately prohibits implicit defaults.
- TS-07 isolates every missing external credential field, blocks credential-free startup and semantic query, structurally detects direct literals and logical/nullish/ternary fallbacks, and follows credential-tainted query/parameter variables into Cypher execution calls.
- TS-01 now also freezes Argo harness environment initialization: the initializer loads only the repository-relative `.argo/.env` into `process.env` before Neo4j projection/runtime checks, ignores root or alternate env files, preserves approved process precedence, and emits no secret-bearing diagnostics.
- TS-07 now also freezes canonical Neo4j projection names: `ARGO_NEO4J_DATABASE_URL`, `ARGO_NEO4J_DATABASE_USERNAME`, and `ARGO_NEO4J_DATABASE_PASSWORD` are the only accepted Neo4j environment names, while legacy `ARGO_NEO4J_URI`, `ARGO_NEO4J_USERNAME`, and `ARGO_NEO4J_PASSWORD` fail closed before driver/projection/index side effects and cannot override canonical names.
- The TS-07 Neo4j projection scenario Harness must return blocked legacy-alias evidence using the shared business outcome shape `{ status: "blocked", error: { category, field } }` so explicit entrypoints can use `assertBlocked()` consistently without production test-only adapters.
- `credential-source-policy.guard.js` self-tests the TS-07 source policy with prohibited bypass fixtures and one safe fixture so scanner regressions cannot silently weaken the boundary.
- `dependency-direction.guard.js` owns no command validator. It calls the production module's callback-scoped test composition so all safe/bypass fixtures traverse the same private validator. It inspects the adapter's complete own names/descriptors/symbols/prototype chain and each capability function, rejects any executor or callable-backdoor surface, and requires the composition return to be `undefined`. Captured adapters revoke on normal completion, callback throw, and async rejection; post-scope calls fail `TEST_SYSTEM_METADATA_ADAPTER_REVOKED` without executor increments, while nested/repeated compositions prove unique adapter and capability identities. Existing command bypasses still fail `SYSTEM_METADATA_COMMAND_PROHIBITED` with zero executor calls.
- TS-01-Native uses a Harness-owned query probe to prove exact request propagation, exactly one native-boundary call, and unchanged propagation of an unpredictable runtime-generated full result.
- `tests/harness/liveEmbeddingProviderHarness.js` hides live-network opt-in, approved process/`.argo/.env` secret preflight, controlled Neo4j setup/cleanup, write counting, safe error categories, and artifact scanning.
- TS-06-Provider-E2E uses a Harness-owned transport wrapper to observe one real HTTPS request, exact target/body, dynamic input, and raw response. The same public gate receives all invalid/error injections; no production scenario branch or self-reported live boolean is accepted.
- Controlled Neo4j evidence must match the transport vector and approved provider/model/qualification/dimensions plus dynamic canonical/content/index identities and versions; cleanup must leave zero records.
- TS-07-Provider-Secret-Isolation uses a generated canary—not the real provider credential—to scan captured error messages, stdout, stderr, logs, complete Cypher text/parameter values, complete graph evidence values, latest failure records, snapshots, and recursively generated test artifacts.
- Its synthetic-success probe records in-memory Cypher/parameter and graph-evidence channels, proves a canary in neutral values is detected in both channels, then verifies zero persisted records and zero generated artifacts after cleanup.
- Every frozen source fixture calls production `resolveApprovedLiveConfiguration()` with temporary files and injected filesystem/git/ACL adapters; no guard contains an alternative resolver. Fixtures assert selected source attribution, process precedence, exact rejection category, and zero fetch/driver/create/write effects.
- Accepted fixtures inject the complete approved nine-key configuration without defaults and compare the complete normalized result plus per-key attribution. Rejected fixtures derive from that complete configuration and mutate one named dimension only.
- Production provenance uses only the configuration module's internally created source adapter. Frozen fixtures provide raw operation behaviors to `withApprovedLiveConfigurationTestComposition`; that module-owned composition root creates a private registered adapter/capability and exposes only a resolver closure. Direct resolver calls with untrusted, cloned, or self-issued-true adapters fail closed.
- Source reads are frozen `{ value, trace }` process envelopes and `{ key, value, trace }` file records. The resolver enforces an exact five-field trace schema, frozen alias chain, field types, issued identity, requested-key/path equality, source/path/operation correlation, and direct one-hop aliases. Trusted mismatch fixtures cover key, path, missing field, extra field, and wrong type; each expects `SOURCE_TRACE_INVALID`.
- A frozen behavior self-test proves direct process and five prohibited operation methods return the identical value without exposing `isIssuedTrace`. Accepted fixtures require every module-issued trace to be consumed and privately validated; forged, mutable, malformed, unvalidated, or untrusted-adapter traces cannot authorize configuration.
- ACL fixtures cover same-principal explicit allow+deny, inherited allow/deny, explicit/inherited combinations, broad explicit/inherited allow, broad deny-only, broad allow+deny, and unverifiable output using principal-bound `icacls` ACE lines.
- Preflight resolves both `QWEN_KEY` and `ARGO_NEO4J_DATABASE_PASSWORD` before transport or Neo4j construction. Redaction fixtures cover process/file sources, conflicts, ACL errors, and connection-authentication errors for both canaries.
- A recording Neo4j adapter is injected through production `createApprovedNeo4jBoundary()`—never a Harness-private index boundary—to prove the database password reaches only `neo4j.auth.basic`; authentication failure produces no query and all recorded Cypher text/parameters remain canary-free.
- The auth probe preserves raw exceptions before classification and recursively scans message/stack/causes plus sanitized errors, all injected logger events, stdout/stderr, auth/driver/query calls, graph/persistence state, and recursive artifacts. Artifact serialization always returns a string for undefined, null, primitives, Error, AggregateError, objects, buffers, and cycles; undefined/null are empty while canaries in every value-bearing form remain detectable. A frozen accepted-fixture regression proves `rawError: undefined` cannot interrupt the source matrix. The 11-channel auth self-test remains mandatory, and the real probe must report zero leaks and zero persistence.
- Default/offline CI must fail both live entrypoints with explicit opt-in categories. Deterministic fakes protect negative paths but never substitutes for live provider evidence.
- DT-05 uses the shared seed entrypoint, and DT-04 shares that W4 entrypoint for channel independence. It freezes directly observable Element, Relationship, and View seed channels, independent per-channel thresholds, every above-threshold peer returned, valid zero-result behavior, no fixed result limits, ANN top-k as performance-only evidence, and no graph-closure, traversal-expansion, or neighborhood-closure output in this stage.
- DT-16 and DT-16-SemanticIndex share the mutation lifecycle entrypoint. The entrypoint freezes the nine mutation classes, version advancement, deleted-object absence, non-Aligned partial persistence, and complete semantic-index evidence fields for Element, ArchitectureRelationship, and View records.
- DT-17 freezes the unaligned-query boundary by requiring full canonical reads to remain available, pure semantic requests to fail with `SEMANTIC_INDEX_NOT_ALIGNED`, automatic full-snapshot fallback to be false, and explicit canonical-anchor reads to remain available.
- TS-09 is a corrected W3 blocking gate. `runEmbeddingProviderAdapterLifecycle.js` freezes `generateAffectedEmbeddings()` outcome evidence for `runtime: "nodejs"`, no required Neo4j GenAI Plugin, affected Element/ArchitectureRelationship/View persistence, complete model/version evidence, no credential exposure, and non-Aligned partial persistence.
- W3.1 uses `runApplyMutationEmbeddingVectorE2E.js` as the explicit live integration entrypoint. The entrypoint delegates all MCP mutation, live-provider, Neo4j vector, and semantic-query plumbing to `liveEmbeddingProviderHarness.js`; default/offline execution fails before side effects with `W31_MUTATION_VECTOR_E2E_OPT_IN_REQUIRED`. The Harness must make exactly one `applySystemArchitectureMutation` call, must not create or execute `createMutationEmbeddingVectorLifecycle()` itself, must not supply `expectedTouchedRecords`, and must assert `embeddingLifecycle` plus `alignment` from the MCP mutation response based on actual touched id arrays.
- DT-01 observes the complete legacy public envelope and absence of query metadata.
- DT-03 preserves all five legal purposes and covers missing/invalid purpose, missing/blank intent, and missing/blank audit subject with stable categories.
- DT-03 proves missing-purpose and audit-without-subject validation precedes retrieval by sharing one test-owned rejection probe whose invocation count remains zero.
- DT-12 first invokes a semantic positive control, then proves graph-tidy does not increment the same probe before checking bypass metadata and canonical equality.
- W5 DT-06 through DT-11 use `intentArchitectureQueryHarness.js` to assert deterministic purpose-policy closure without exposing Cypher, MCP response parsing, or caller plumbing in the entrypoint bodies. The Harness owns assertions for named policy ids, parameterized Cypher evidence, bound parameters, ArchiMate relationship semantics, caller-identity independence, category-specific included ranges, and explicit out-of-category exclusions.
- W6 DT-00-W6, DT-13, DT-14, and DT-15 use `intentArchitectureQueryHarness.js` to assert coherent-result version equality with the governing legacy graph version, non-empty same-version endpoint closure with endpoint id checks and structural error channels, complete non-cascading View closure with exact target/overlapping View IDs, exact member/relationship object sets, and parent viewpoint evidence, and exactly one ordered first-inclusion reason over explicit duplicate-path fixtures with non-overwriting policy parameters/anchors and index/version evidence. Entrypoints preserve GIVEN / WHEN / THEN readability and do not parse low-level MCP, filesystem, or environment plumbing.
- W7 DT-18 and TS-08 use `productionGraphRagHarness.js` to assert business benchmark and delivery-gate semantics without exposing runtime composition plumbing. DT-18 freezes the approved five-purpose benchmark id, non-empty mandatory key seed ids, actual recalled key seed ids, non-empty expected closure ids, actual observed closure ids, missing-key-seed emptiness, expected closure correctness, explicitly recorded unrelated-query forced-hit evidence, zero forced hits, recorded precision per purpose and aggregate inside `[0, 1]`, and absence of an invented release precision threshold. It also freezes negative boundaries for missing mandatory key seed fixtures, missing expected closure fixtures, missing actual recall observations, wrong same-count recalled ids, non-empty missing key seed evidence, missing actual closure observations, closure correctness below 100%, missing or negative unrelated forced-hit evidence, positive unrelated forced hits, empty benchmark content, incomplete benchmark purposes, and precision outside `[0, 1]`. TS-08 freezes prerequisite blocking for W2-W6, DT-18-required blocking after prerequisites are present, rejection of invalid W7 aggregate precision including `-99`, and overall-delivery permission only after the W7 quality benchmark passes.
- DT-19 uses `productionGraphRagHarness.js` to assert capacity evidence without exposing runtime composition plumbing. It freezes the real chain from `evaluatePhase1QualityBenchmark(request)` into `evaluateCapacityEvidence(request)`, requires actual DT-18 quality output to preserve explicit `observedResultIds` or equivalent result ids plus measured precision for every declared purpose, rejects closure fallback, requires missing quality/result evidence and result cardinality mismatch to block clearly, and rejects any silent cap, budget, pagination, truncation, continuation, top-k, token-budget, result-limit, or capacity-policy decision field.
- WP-P1 uses `productionSemanticPersistenceHarness.js` to hide canonical snapshot, structural-projection, provider, raw durable-driver recording, shipped JSON-RPC process invocation, runtime/MCP composition, restart, and test-cleanup plumbing behind business-readable scenario methods. Its default-composition probe starts the actual MCP server process and sends `tools/call` without injecting a runtime or runtime dependencies; secrets are absent so successful composition is observed through fail-closed external-configuration evidence without opening Neo4j. The deterministic lifecycle scenario injects only the raw driver below concrete production Neo4j projection/checkpoint factories; it never substitutes a test projection store or checkpoint store.
- SP-01 freezes the non-injected shipped JSON-RPC `tools/call` composition root in addition to explicit MCP operator exposure, exact opt-in, structural-before-semantic same-version ordering, missing-credential and missing-qualification fail-closed behavior before side effects, independent complete Element/ArchitectureRelationship/View enumeration, bounded batches, concrete durable per-channel checkpoints, interruption/resume, isolated record failure, stable identity, complete canonical/content/index/provider/model/version/dimensions/vector metadata, idempotent rerun, no fake canonical mutation, and alignment only after all channels complete. The default path must not fail with `productionGraphRagRuntime.runSemanticBackfill is required`, must reach `EXTERNAL_CREDENTIALS_REQUIRED` with no test injection, and must leave canonical JSON byte-identical. Resume correctness is derived from Harness-observed provider and durable-upsert identities; self-reported resume evidence is ignored.
- SP-02 freezes concrete production Neo4j adapter use across restart, changed-record stable-identity upsert, tombstone deletion, unrelated live-E2E cleanup isolation, exact four-method store surface, rejection of `runId`-bearing production records before persistence, missing-configuration/qualification blocking before side effects, complete metadata, canonical JSON authority, and Neo4j as subordinate projection/index.
- `tests/harness/productionDefaultRetrievalHarness.js` hides WP-P2 raw source behavior, filesystem/git/ACL metadata, provider transport, unified durable readiness, persistent Neo4j vector-window recording, public MCP invocation, and canonical snapshot comparison behind business-readable scenario methods. The unified-readiness control alternates Aligned/Failed revisions under one production recordId before invoking both exported routers. Aligned calls must traverse the accepted default retrieval boundary and prove threshold filtering, expanding-window exhaustion, purpose closure, endpoint/View closure, provenance, and canonical completion. Failed calls preserve stored redacted category/message/action and exact state/version/channel evidence while excluding an unknown diagnostic secret canary and performing zero provider/vector work. The module-owned raw-port composition still accepts exactly `sourceBehavior`, `sourceAdapters`, `transport`, and `neo4jDriver`; the production `readinessBoundary` is a factory dependency, not a test verdict or preassembled result.
- SP-03 freezes the accepted default retrieval path through approved operator composition: all required approved keys must be directly read, attributed, and ordered before readiness. All three legacy Neo4j aliases must be directly inspected; legacy-only and each mixed valid-canonical-plus-any-legacy-alias fixture reject `SECRET_SOURCE_PROVENANCE_PROHIBITED` without legacy attribution/selection or readiness/provider/vector work. Missing, unsafe, conflicting, test/default, and fallback sources also reject. One qualified query embedding supplies the exact vector to the exact named Element, ArchitectureRelationship, and View indexes through parameterized `db.index.vector.queryNodes`; expanding top-k and correlated raw offset/window/continuation/exhaustion evidence returns qualifying peers beyond the initial window. A valid all-zero result still proves readiness, embedding, and one exhausted sequence per channel. Closure assertions cover exact `w5.implementation-design.v1` bound parameters and ArchiMate direction; endpoint objects/versions; entire selected-View metadata, viewpoint binding, parent viewpoint, ordered versioned members and relationship endpoints; overlap exclusion; unique first-inclusion provenance for every returned object; canonical/content/index versions; and graph-tidy bypass.
- SP-04 freezes one total operation ledger with persistent readiness before provider/vector side effects. Structural-only `SemanticIndexPending`, partial View channel, stale canonical version, failed, unknown, explicit ArchitectureRelationship content-version mismatch, and explicit View index-version mismatch fixtures must expose their exact state/versions/missing/mismatched channels with `fullSnapshotFallback: false`, terminate at readiness, and produce zero provider/vector calls. Only complete aligned Element/ArchitectureRelationship/View evidence may retrieve. No-argument and graph-tidy controls remain exact canonical snapshots.
- `tests/harness/intentArchitectureQueryHarness.js` preserves inherited compatibility semantics while routing each semantic wrapper call through an approved operator adapter around the existing deterministic/probe retrieval boundary. No-argument and graph-tidy snapshots remain bypasses; semantic probes still measure the accepted boundary exactly once and cannot pass through exported raw fallback. The intent-query and production-default correctness guards use TypeChecker-bound property values and adapter call flow to require the live wrapper-fourth/inner-third journey dependency to reach the approved adapter factory and its WP-P2 retrieval parameter; undefined, dead conditional/decoy, raw substitution, and shadowed-factory fixtures fail.
- `tests/harness/productionSemanticOperatorJourneyHarness.js` hides WP-P3 operator command dispatch, canonical snapshot reading, workspace initialization, structural projection, approved-configuration resolution, semantic backfill, persistent readiness, and semantic-query plumbing behind business-readable methods. Every primary success/rejection composition injects one contract-faithful controlled readiness-attestation store that starts empty, records only explicit verification, clears on invalidation, and rejects exact readiness drift. It does not model filesystem/ACL trust; those assertions remain exclusively in the adapter lifecycle Harness, including the isolated missing-store construction probe. The deterministic composition otherwise supplies recording ports at accepted WP-P1/WP-P2 public boundaries and does not reproduce persistence, retrieval, credential, qualification, or readiness internals.
- SP-05 freezes two successful controlled fresh-project journeys whose complete fixture inputs differ only by automatic opt-in. Direct/CLI explicit backfill without `explicitOptIn: true` forwards missing consent to WP-P1 and has zero semantic effects. Automatic consent is valid only from factory-owned start after exact caller automatic opt-in and approved configuration.
- Query before explicit verification rejects with no implicit effects. A successful explicit-verification→query flow records exactly two WP-P2 readiness reads: one for verification and one current read for durable query-time revalidation. The entrypoint guard binds the live `assert.strictEqual` inside `assertVerifiedSoleAuthorization` to the exact filtered `semantic-readiness-read` count and rejects wrong counts, dead decoys, and wrong counters. Contradictory records prove verdict ownership: `state: Aligned, verified:false` rejects with exact diagnostics, while non-Aligned state with `verified:true` authorizes query. State, versions, completed/missing/mismatched channels, and fallback evidence remain byte-for-byte equivalent to the WP-P2 envelope.
- `tests/harness/productionSemanticOperatorAdapterLifecycleHarness.js` and its child-process fixture run real separate CLI processes, a same-process verify→drift→query process, package command expansion, exported System/unified `callTool` probes without an injected journey, injected operator controls, JSON-RPC error surfaces, and a missing-store factory probe. They freeze mandatory durable revalidation for every query; no volatile fallback; the same-user/local-workspace trust boundary; malformed/extra/tampered/file-presence-only and foreign-workspace records; symlink/reparse rejection; exact-current-user ownership; current-user/SYSTEM-only effective Windows ACLs with deny precedence; group owner, isolated file and parent cases for `BUILTIN\Users`, `BUILTIN\Administrators`, and Authenticated Users, Everyone, arbitrary foreign allow, absent current-user ACE, permissive parent, and live-host `.argo/temp` rejection; exact redacted ACL remediation; interrupted replacement; canonical-byte and independent readiness drift; init/backfill/actual MCP canonical-mutation invalidation; zero unauthorized retrieval; unchanged snapshots; and the exact eleven-field redacted error whitelist. Positive process workspaces pre-create `.argo/temp`, remove inherited Windows ACLs, grant only the current identity and SYSTEM, and verify no broad ACE remains. The controlled fixture adapter may override owner/negative-case metadata only inside the child composition; it cannot weaken production policy or depend on broad `os.tmpdir` inheritance.
- `tests/harness/automaticSemanticLifecycleHarness.js` owns the accepted superseding lifecycle fixtures. It invokes shipped uninjected argo init, actual batch/focused MCP write adapters, and exported System/unified query dispatch while controlled module-owned compositions provide only raw inward ports. SP-05 explicitly seeds a complete durable verified Aligned record before each isolated disabled/invalid/configuration scenario and before the interruption/resume/rerun chain. Every shipped init call must transform that same identity, `recordId`, and canonical version at a higher revision before its outcome: disabled becomes SemanticIndexPending/disabled; invalid/configuration/reconciliation failures become actionable redacted Failed/Stale; enabled valid work observes invalidated readiness before the provider/vector boundary and records verified Aligned only after queryability and global coherence. The interruption raw port injects a dedicated reconciliation secret canary; the Harness retains that unsanitized diagnostic separately, then requires exact safe category/message/action in both durable same-record evidence and exported init error, literal-false public fallback, and zero canary occurrence in all serialized durable/public evidence. Independent raw-port ledgers, not returned counters, freeze zero provider/vector effects for disabled, half-enabled, malformed, missing, and unsafe controls and prove no stale prior Aligned authorization survives. Resume/no-replay and rerun idempotence remain mandatory. Direct journey/lifecycle factories, synthetic touched-ID reconciliation, response-to-readiness fixture copying, returned self-report counters, Harness-side diagnostic sanitization, router-side WP-P2 algorithm duplication, and production use of test-only composition are prohibited acceptance controls.
- The automatic lifecycle Harness never supplies credentials, a readiness verdict, preassembled lifecycle results, expected touched records, a production runId, or cleanup authority. It supplies controlled external configuration outcomes and records calls at accepted ports. The protected W3.1 live branch runs only when both exact gates are `1`; default execution proves controlled code-complete behavior and is never blocked merely because a live provider/Neo4j environment is absent.
- `tests/explicit/entries/runMcpSemanticQueryContract.js` owns the BP MCP semantic query contract for WP-1 through WP-4. Its graph-mounted `#anchor` cases are executable through `ARGO_TESTCASE_ANCHOR` and preserve GIVEN/WHEN/THEN readability at the suite level. The request-shape and canonical-payload anchors also exercise no-anchor public `callTool` and unified MCP `tools/call` subcases, so the contract cannot be satisfied only by anchor-gated fixtures. The control points are public `getSystemArchitecture(query)` calls with forbidden response-shape controls, ordinary semantic subset fixtures, selected Relationship fixtures, selected View fixtures, and broken reference fixtures. The observation points are fail-closed request validation, canonical-only `document` subsets, no neighbor expansion for Element hits, minimal Relationship endpoint closure, complete non-cascading View closure, exact full-snapshot compatibility for omitted-query/graph-tidy, and stable failure categories `QUERY_RESPONSE_SHAPE_CONTROL_FORBIDDEN`, `SEMANTIC_SUBSET_RELATIONSHIP_MISSING`, and `SEMANTIC_SUBSET_VIEW_MISSING`.
- `tests/architecture/documentation/root-readme-reduction.guard.js` owns the WP2 root README reduction guard. It reads only `README.md` and verifies that adoption/product content and curated design links remain while deep semantic operator, credential, MCP lifecycle command, and live-environment detail is routed to stable `design/` references instead of duplicated inline.
- `tests/architecture/documentation/contributor-governance-entrypoint.guard.js` owns the WP3 contributor governance entrypoint guard. It reads only `CONTRIBUTING.md` and verifies fact-source priority, safe change surfaces, stage boundaries, validation commands, explicit testcase and frozen-file boundaries, stable reference links, documentation maintenance rules, and forbidden shortcuts while rejecting inline deep design-spec duplication.
- `operator-policy-provenance.guard.js` builds a TypeScript Program and follows bound aliases/assignments. It accepts the compliant automatic selector while rejecting `Boolean(1)` and nested consent aliases, dependency/key aliases plus bracket readiness access, renamed state/channel policy, and cosmetic `verified` references whose real decision uses state.
- `adapter-lifecycle-boundary.guard.js` uses TypeChecker bindings, public-dispatch call-graph traversal that stops only at the exact approved default operator factory, active writer reachability, executable ordering, return-before-validation rejection, and an executable private Windows trust-policy matrix. Adversarial fixtures reject direct, aliased, helper-delegated, and resolver-delegated raw fallback; same-process early retrieval; optional volatile stores; dead atomic helpers paired with active append/non-atomic writes; missing durability; group/foreign owners; missing-current-user or denied identity; isolated file/parent protected allows for `BUILTIN\Users`, `BUILTIN\Administrators`, Authenticated Users, Everyone, and foreign principals; permissive parent ACLs; malformed ACL evidence; unsafe wire/metadata/serializer behavior; and secret-bearing checkpoint fields.

## Local dependencies

- Harness code may depend on the public `argo-mcp-server.js` wrapper, deterministic Graph RAG runtime, and canonical graph fixture. Every explicit legacy compatibility and TS-00 typed-envelope semantic query passes a test-only approved `semanticOperatorJourney` through wrapper argument four in `callTool(name, args, progressToken, dependencies)`; the adapter delegates to the supplied probe or Harness-owned accepted retrieval boundary. The wrapper forwards it to argument three of `systemarchitecture-mcp-server.js` `callTool(name, args, dependencies)`. TypeChecker-bound correctness guards require the live journey property to reach the exact approved adapter factory and its bound retrieval parameter, so neither production text routing nor raw fallback can satisfy compatibility. WP-P2 default semantic scenarios use the same approved composition at the inner boundary while preserving their independent credential/readiness evidence.
- The frozen Harness owns the semantic retrieval spy and injects it through the in-process dependency override; its count changes only inside the spy's `retrieve()` method and never reads response telemetry.
- Explicit entrypoints depend only on Node assertions and Harness methods.
- Guardrails may inspect implementation contracts, graph metadata, and dependency declarations, but must not implement production behavior.
- The production Graph RAG Harness depends only on the public `createProductionGraphRagRuntime(dependencies)` boundary and injected business fakes; explicit entrypoints never open Neo4j connections or read environment variables directly.

## Owned tests

### Explicit entrypoints

- `tests/explicit/entries/runGraphQueryCompatibility.js`
- `tests/explicit/entries/runCanonicalGraphFullSnapshot.js`
- `tests/explicit/entries/runQueryPurposeValidation.js`
- `tests/explicit/entries/runGraphTidyFullSnapshot.js`
- `tests/explicit/entries/runProductionGraphRagRuntime.js` — `ExplicitAcceptanceTestcase-TS-01` Node.js production mainline plus harness environment initialization. The control point is a production semantic query and the harness initializer before projection/runtime checks. The observation point is Node.js/Neo4j-native evidence, no Python or GenAI plugin requirement, exact repository `.argo/.env` process loading before projection, root/alternate env-file rejection, process precedence preservation, and no secret diagnostics.
- `tests/explicit/entries/runNeo4jNativeRetrievalPlatform.js`
- `tests/explicit/entries/runEmbeddingQualificationGate.js`
- `tests/explicit/entries/runExternalCredentialBoundary.js` — `ExplicitAcceptanceTestcase-TS-07` external credential and canonical Neo4j naming boundary. The control point is start/query and projection configuration with missing credentials, canonical `ARGO_NEO4J_DATABASE_*` names, and legacy aliases. The observation point is safe blocking for missing credentials and legacy aliases, accepted canonical Neo4j URL/username/password normalization, no hardcoded/default credentials, no credential fallback expressions, and no credential-bearing Cypher.
- `tests/explicit/entries/runCanonicalProjectionAuthority.js`
- `tests/explicit/entries/runSevenWaveDeliveryGates.js`
- `tests/explicit/entries/runLiveEmbeddingProviderE2E.js`
- `tests/explicit/entries/runLiveEmbeddingProviderSecretIsolation.js` — `ExplicitAcceptanceTestcase-TS-07-Provider-Secret-Isolation` live/provider secret isolation and canonical Neo4j source policy. The control point is the approved live source matrix and controlled Neo4j authentication canary. The observation point is process/file source attribution for `QWEN_KEY` and canonical `ARGO_NEO4J_DATABASE_*` names, fail-closed legacy alias sources, zero side effects for rejected fixtures, password only reaching database auth, no Cypher/log/artifact leaks, and cleanup leaving zero persistence.
- `tests/explicit/entries/runApplyMutationEmbeddingVectorE2E.js`

The frozen `tests/explicit/entries/runEmbeddingProviderAdapterLifecycle.js` path is retained as the TS-09 explicit entrypoint. In corrected W3 handoffs it is in-scope acceptance evidence and a Coding target while remaining read-only during Coding/Repair.

The following globally mounted entrypoints are physicalized for the runner and
remain outside the compatible-query Coding targets:

- `tests/explicit/entries/runCoherentIntentReading.js`
- `tests/explicit/entries/runIndependentSemanticSeeds.js`
- `tests/explicit/entries/runPurposePolicyClosure.js`
- `tests/explicit/entries/runIntentDecisionClosure.js`
- `tests/explicit/entries/runImplementationDesignClosure.js`
- `tests/explicit/entries/runCodingRepairClosure.js`
- `tests/explicit/entries/runAuditProofClosure.js`
- `tests/explicit/entries/runRelationshipEndpointClosure.js`
- `tests/explicit/entries/runCompleteViewClosure.js`
- `tests/explicit/entries/runFirstInclusionProvenance.js`
- `tests/explicit/entries/runMutationIndexLifecycle.js`
- `tests/explicit/entries/runStaleSemanticQueryRejection.js`
- `tests/explicit/entries/runRetrievalQualityBenchmark.js`
- `tests/explicit/entries/runCapacityEvidence.js`
- `tests/explicit/entries/runProductionSemanticBackfill.js`
- `tests/explicit/entries/runPersistentSemanticProjectionLifecycle.js`

The W3 Index Lifecycle and Exact-Threshold Baseline handoff owns these mounted explicit entrypoints as Coding/Repair read-only files:

- `tests/explicit/entries/runIndependentSemanticSeeds.js` — `ExplicitAcceptanceTestcase-DT-05` threshold-all correctness before ANN comparison; the same physical script continues to protect DT-04 channel independence.
- `tests/explicit/entries/runMutationIndexLifecycle.js` — `ExplicitAcceptanceTestcase-DT-16` and `ExplicitAcceptanceTestcase-DT-16-SemanticIndex` all-mutation lifecycle plus complete semantic-index evidence.
- `tests/explicit/entries/runStaleSemanticQueryRejection.js` — `ExplicitAcceptanceTestcase-DT-17` unaligned pure semantic rejection with canonical-read continuity.
- `tests/explicit/entries/runEmbeddingProviderAdapterLifecycle.js` — `ExplicitAcceptanceTestcase-TS-09-EmbeddingProviderAdapter` and `ExplicitAcceptanceTestcase-TS-09-EmbeddingGeneration` Node adapter generation/persistence proof. DT scoped passes do not complete W3 without this entrypoint passing.

The W3.1 Mutation-Driven Live Vector Integration handoff owns this mounted explicit entrypoint as a Coding/Repair read-only file:

- `tests/explicit/entries/runApplyMutationEmbeddingVectorE2E.js` — `ExplicitAcceptanceTestcase-W3-1-MutationEmbeddingVectorE2E` single-call applySystemArchitectureMutation-to-automatic-embeddingLifecycle-to-real-Qwen-to-Neo4j-vector-query proof. Passing W3 and TS-09 evidence is prerequisite context but cannot satisfy this W3.1 live integration acceptance without the opt-in entrypoint proving the mutation response contains `embeddingLifecycle` and `alignment`.

The W4 Independent Three-Channel Seeds handoff owns this mounted explicit entrypoint as a Coding/Repair read-only file:

- `tests/explicit/entries/runIndependentSemanticSeeds.js` — `ExplicitAcceptanceTestcase-DT-04` and `ExplicitAcceptanceTestcase-DT-05` independent semantic seed retrieval proof. The control point is a purpose-declared semantic query before closure. The observation point is separate Element, Relationship, and View channels, independent channel gates, every qualifying candidate released, unrelated queries allowed to return zero forced hits, ANN performance-only evidence, and absence of closure-shaped output.

The W5 Deterministic Five-Purpose Closure handoff owns these mounted explicit entrypoints as Coding/Repair read-only files:

- `tests/explicit/entries/runPurposePolicyClosure.js` — `ExplicitAcceptanceTestcase-DT-06` and `ExplicitAcceptanceTestcase-DT-07` deterministic mandatory closure plus all five purpose dispatch categories. The control point is approved W4 seed anchors under each declared purpose. The observation point is named parameterized policy evidence, bound parameters, ArchiMate source/target semantics, mandatory low-similarity inclusion, generated-Cypher exclusion, caller-identity independence, four independent ordinary semantic category boundaries, and graph-tidy as the fifth canonical full-snapshot dispatch with zero semantic operations and no semantic closure policy id.
- `tests/explicit/entries/runIntentDecisionClosure.js` — `ExplicitAcceptanceTestcase-DT-08` intent-decision boundary. The control point is a capability-change intent decision with mandatory dissimilar lineage. The observation point is Why, What, business behavior, Acceptance, realization-state or absence evidence, and exclusion of implementation, repair, audit, and graph-tidy scope.
- `tests/explicit/entries/runImplementationDesignClosure.js` — `ExplicitAcceptanceTestcase-DT-09` implementation-design boundary. The control point is a target with upstream prerequisites and bounded downstream dependents. The observation point is dependency chains that stop at declared boundaries, acceptance semantics, guardrails, delivered-stop decisions, and exclusion of repair and graph-tidy scope.
- `tests/explicit/entries/runCodingRepairClosure.js` — `ExplicitAcceptanceTestcase-DT-10` coding-repair boundary. The control point is conflicting code evidence against approved intent. The observation point is authoritative intended behavior, guardrails, at-risk outcomes, acceptance semantics, and exclusion of unrelated similar capabilities and implementation-planning scope.
- `tests/explicit/entries/runAuditProofClosure.js` — `ExplicitAcceptanceTestcase-DT-11` audit-proof boundary. The control point is an explicit audit subject with an inside low-similarity violation and outside high-similarity candidate. The observation point is subject-scoped obligations, violations, evidence exceptions, missing-evidence false-pass prevention, out-of-subject exclusion, and missing-subject rejection.
- `tests/explicit/entries/runGraphTidyFullSnapshot.js` — `ExplicitAcceptanceTestcase-DT-12` graph-tidy full-snapshot bypass. The control point is anchored graph-tidy mutation preparation after a semantic positive control. The observation point is unchanged probe count (zero additional semantic operations), full-snapshot mode, semantic bypass, and complete canonical graph equality.

The W6 Structural Closure and Explainable Results handoff owns these mounted explicit entrypoints as Coding/Repair read-only files:

- `tests/explicit/entries/runCoherentIntentReading.js` — `ExplicitAcceptanceTestcase-DT-00-W6-CoherentResultRegression` coherent result version evidence. The control point is legacy and semantic-query tasks against the same canonical graph version. The observation point is complete legacy reading plus semantic `canonicalVersion` equality with the governing legacy graph version; missing version evidence fails `DT00_CANONICAL_VERSION_MISSING` and mismatched evidence fails `DT00_CANONICAL_VERSION_MISMATCH`.
- `tests/explicit/entries/runRelationshipEndpointClosure.js` — `ExplicitAcceptanceTestcase-DT-13` same-version relationship endpoint closure. The control point is a purpose-closed relationship range with endpoint closure fixtures for returned, dangling, and cross-version endpoints. The observation point is at least one relationship object with source/target ids, source/target Element objects whose ids match the relationship endpoints, same-version evidence tied to the governing result version, and explicit dangling/cross-version structural errors.
- `tests/explicit/entries/runCompleteViewClosure.js` — `ExplicitAcceptanceTestcase-DT-14` complete non-cascading View closure. The control point names target View `grag-integrity-explainability` and overlapping Views `grag-quality-capacity` and `grag-vertical-chain-b`, while requiring in-View relationships plus a parent viewpoint. The observation point is target View presence, absence of overlapping Views unless independently matched or explicitly requested, complete metadata, viewpoint binding, parent viewpoint, exact included element ids matching member object ids, exact relationship ids matching relationship object ids, endpoint-complete in-View relationships, and no overlapping-View cascade.
- `tests/explicit/entries/runFirstInclusionProvenance.js` — `ExplicitAcceptanceTestcase-DT-15` single ordered first-inclusion provenance. The control point is duplicate discovery fixtures through semantic seed, endpoint closure, purpose policy, and complete View closure. The observation point is exactly one ordered `firstInclusionReason`, expected supplementary reasons without overwrite, policy id plus parameters/anchors, and canonical/content/index/alignment evidence.

The W7 Phase 1 Business Acceptance handoff owns these mounted explicit entrypoints as Coding/Repair read-only files:

- `tests/explicit/entries/runRetrievalQualityBenchmark.js` — `ExplicitAcceptanceTestcase-DT-18` approved five-purpose business benchmark proof. The control point is the human-approved benchmark after W2-W6 acceptance evidence plus negative benchmark counterexamples. The observation point is non-empty mandatory seed fixtures, 100% key seed recall by mandatory-seed intersection, non-empty expected closure fixtures, 100% expected closure correctness from actual closure observations, explicitly recorded zero forced hits for unrelated queries, recorded precision evidence inside `[0, 1]`, no precision threshold substituting for recall or closure, and blocking categories for fabricated/missing observations, wrong same-count recall ids, missing key seeds, closure misses, positive unrelated hits, missing fixtures, missing/negative unrelated-hit evidence, or empty/incomplete benchmark content.
- `tests/explicit/entries/runSevenWaveDeliveryGates.js` — `ExplicitAcceptanceTestcase-TS-08` whole-delivery gate. The control point is W7 or whole-delivery attempts with missing prerequisite waves, missing/failing DT-18 evidence, invalid aggregate precision such as `-99`, and a passing ordered-delivery control case. The observation point is prerequisite blocking, W7-quality blocking, precision-range blocking, and delivery allowed only after W2-W6 plus DT-18 pass.

The DT-19 Capacity Evidence handoff owns this mounted explicit entrypoint as a Coding/Repair read-only file:

- `tests/explicit/entries/runCapacityEvidence.js` — `ExplicitAcceptanceTestcase-DT-05-R2-DT-19` capacity evidence recording. The control point is the W7 phase-1 capacity evidence request chained from real DT-18 quality evidence. The observation point is one capacity evidence record for every declared purpose, integer non-negative result cardinality derived from explicit observedResultIds/result ids, measured precision inside `[0, 1]`, explicit blocking for missing quality evidence, missing result evidence, closure fallback, and result cardinality mismatch, and absence of cap, budget, pagination, truncation, continuation, top-k, token-budget, result-limit, or capacity-policy decision fields.

The WP-P1 Persistent Projection and Full Backfill handoff owns these mounted explicit entrypoints as Coding/Repair read-only files:

- `tests/explicit/entries/runProductionSemanticBackfill.js` — `ExplicitAcceptanceTestcase-SP-01-FullBackfill`. Control point: shipped uninjected canonical argo init plus the concrete private runtime → production backfill → adapter/store/checkpoint composition after structural projection with bounded interruption, isolated failure, resume, and rerun. Observation point: public backfill routing is absent in both dispatchers; negative opt-in/version/configuration/qualification gates have zero side effects; complete independent three-channel records and metadata, durable checkpoints, independent no-replay observations, idempotence, no fake canonical mutation trigger, and final-only alignment are preserved.
- `tests/explicit/entries/runPersistentSemanticProjectionLifecycle.js` — `ExplicitAcceptanceTestcase-SP-02-PersistentProjection`. Control point: concrete production Neo4j adapter and exact-surface production store across restart, update, tombstone, prohibited runId input, missing release gates, and unrelated live-E2E cleanup. Observation point: durable stable identities, changed-record upsert, tombstone deletion, complete metadata, zero blocked-path side effects, no production cleanup/runId API or records, canonical authority, and subordinate Neo4j projection.

The WP-P2 Default Vector Retrieval and Readiness handoff owns these mounted explicit entrypoints as Coding/Repair read-only files:

- `tests/explicit/entries/runDefaultMcpNeo4jVectorRetrieval.js` — `ExplicitAcceptanceTestcase-SP-03-DefaultVectorRetrieval`. Control point: call the public default `getSystemArchitecture` semantic path with no runtime/retrieval dependency argument through approved and prohibited raw credential-source fixtures, aligned persistent records, multi-window candidates, and zero results. Observation point: complete direct approved-key reads and attribution before readiness; source-policy rejection before side effects; exact approved embedding vector reused by exact named-index parameterized Cypher; expanding top-k and per-channel exhaustion including the all-zero case; every qualifying peer beyond the initial window; exact policy/direction, endpoints, complete View/parent/member/endpoint metadata, overlap exclusion, unique per-object provenance, and versions; no synthetic/default/test shortcut; and unchanged graph-tidy snapshot.
- `tests/explicit/entries/runProductionSemanticReadinessGate.js` — `ExplicitAcceptanceTestcase-SP-04-FailClosedReadiness`. Control point: call the same public default path against exact persistent pending, partial, stale, failed, unknown, content-mismatch, index-mismatch, and aligned fixtures, then run no-argument and graph-tidy controls. Observation point: the total ledger places readiness before provider/vector work; every non-aligned call returns exact state/version/channel evidence and terminates with zero side effects; only complete alignment retrieves; both full-snapshot modes remain canonical.

Historical WP-P3 Operator Journey evidence (superseded by the canonical semantic lifecycle successor below) owned this mounted path:

- The prior attestation/CLI assertions remain historical fixtures only; they are no longer executed by `runNewProjectSemanticOperatorJourney.js` and do not define successor query authorization.

The canonical semantic lifecycle successor handoff reuses and refreezes these graph-mounted paths:

- `tests/explicit/entries/runNewProjectSemanticOperatorJourney.js` — canonical argo init exact dual-gate matrix from pre-existing durable verified Aligned readiness, same-record invalidation/failure/alignment, zero invalid-path semantic effects, automatic private WP-P1 reconciliation, actionable redaction, resume/no-replay/rerun, and no standalone public lifecycle tools.
- `tests/explicit/entries/runProductionSemanticBackfill.js` — private WP-P1 full reconciliation under argo init; the former public backfill name is absent from discovery and routing.
- `tests/explicit/entries/runPersistentSemanticProjectionLifecycle.js` — durable batch/focused exact-touched upsert/tombstone behavior through the production four-method store, with no production runId cleanup.
- `tests/explicit/entries/runMutationIndexLifecycle.js` — readiness-first exact touched-ID lifecycle for all 18 batch/focused add/update/remove combinations across Element, ArchitectureRelationship, and View.
- `tests/explicit/entries/runApplyMutationEmbeddingVectorE2E.js` — controlled code-complete incremental acceptance by default and additional protected live-release proof only under both exact gates.
- `tests/explicit/entries/runDefaultMcpNeo4jVectorRetrieval.js` and `tests/explicit/entries/runProductionSemanticReadinessGate.js` — ordinary queries freshly verify persistent readiness every call without a retired explicit-readiness authorization command; non-Aligned evidence remains fail-closed with `fullSnapshotFallback:false`.
- `tests/explicit/entries/runTypedMcpQueryContract.js` — `getSystemArchitecture` is the sole public architecture read/query semantic surface while no-argument and graph-tidy compatibility remain exact.
- `tests/explicit/entries/runMcpSemanticQueryContract.js#reject-response-shape-controls` — `ExplicitAcceptanceTestcase-BP-MCP-SEM-REQ`. Control point: ordinary semantic query carrying response-shape controls through anchored, no-anchor `callTool`, and no-anchor unified MCP paths. Observation point: rejection before retrieval with `QUERY_RESPONSE_SHAPE_CONTROL_FORBIDDEN` and no canonical subset/debug/evidence success.
- `tests/explicit/entries/runMcpSemanticQueryContract.js#preserve-full-snapshot-read-modes` — `ExplicitAcceptanceTestcase-BP-MCP-SEM-COMPAT`. Control point: omitted-query and graph-tidy reads. Observation point: exact canonical full snapshot, full-snapshot mode metadata for graph-tidy, semantic retrieval bypass, and no query metadata on omitted-query legacy reads.
- `tests/explicit/entries/runMcpSemanticQueryContract.js#canonical-object-subset-only` — `ExplicitAcceptanceTestcase-BP-MCP-SEM-PAYLOAD`. Control point: ordinary semantic query returning Elements, Relationships, and Views through anchored, no-anchor `callTool`, and no-anchor unified MCP paths. Observation point: successful payload exposes only canonical object collections and omits derived semantic/business fields.
- `tests/explicit/entries/runMcpSemanticQueryContract.js#element-hit-no-neighbor-expansion` — `ExplicitAcceptanceTestcase-BP-MCP-SEM-ELEMENT`. Control point: Element-only semantic hit. Observation point: only the canonical matched Element is returned, with no adjacent Relationships or owning Views.
- `tests/explicit/entries/runMcpSemanticQueryContract.js#relationship-endpoint-closure` — `ExplicitAcceptanceTestcase-BP-MCP-SEM-REL`. Control point: selected Relationship whose endpoints are not independent hits. Observation point: canonical Relationship plus both endpoint Elements and no unrelated endpoint neighbors or Views.
- `tests/explicit/entries/runMcpSemanticQueryContract.js#broken-relationship-endpoint-rejection` — `ExplicitAcceptanceTestcase-BP-MCP-SEM-BROKEN-REL`. Control point: selected Relationship with unresolved endpoint references. Observation point: fail-closed `SEMANTIC_SUBSET_RELATIONSHIP_MISSING` and no partial successful Relationship subset.
- `tests/explicit/entries/runMcpSemanticQueryContract.js#view-membership-closure` — `ExplicitAcceptanceTestcase-BP-MCP-SEM-VIEW`. Control point: selected View with declared members and included Relationships. Observation point: complete canonical View, member Elements, included Relationships, and endpoints for in-View Relationships.
- `tests/explicit/entries/runMcpSemanticQueryContract.js#no-overlapping-view-cascade` — `ExplicitAcceptanceTestcase-BP-MCP-SEM-NOCASCADE`. Control point: selected View sharing members with an overlapping non-matched View. Observation point: shared member objects may appear, but the overlapping View is excluded.
- `tests/explicit/entries/runMcpSemanticQueryContract.js#broken-view-reference-rejection` — `ExplicitAcceptanceTestcase-BP-MCP-SEM-BROKEN-VIEW`. Control point: selected View with broken member or in-View Relationship references. Observation point: fail-closed `SEMANTIC_SUBSET_VIEW_MISSING` and no incomplete View success.
- `tests/explicit/entries/runDocumentationInformationArchitectureDecision.js` — `ExplicitAcceptanceTestcase-DOC-01` through `ExplicitAcceptanceTestcase-DOC-04`. Control point: WP1 documentation information architecture acceptance for root README adoption routing, contributor governance routing, stable design reference routing, and content-boundary placement. Observation point: the intent graph and decision-tree evidence preserve the accepted dual-entry strategy, residual `CONTRIBUTING.md` filename coordination, design/mcp and design/validator authority routing, and single-source documentation layer boundaries without performing WP2-WP5 documentation edits.

The WP2 root README reduction handoff reuses `ExplicitAcceptanceTestcase-DOC-01`, `ExplicitAcceptanceTestcase-DOC-03`, and `ExplicitAcceptanceTestcase-DOC-04` as mounted explicit anchors, and adds the frozen README reduction guard as the concrete Coding repair signal for `README.md`.

The WP3 contributor governance handoff reuses `ExplicitAcceptanceTestcase-DOC-02` as the mounted explicit anchor and strengthens it to observe the physical root `CONTRIBUTING.md` guide. The control point is an internal maintainer preparing to change MCP, validator, tests, or docs from repository root. The observation point is that the maintainer can identify the authoritative file, safe edit surface, validation command, explicit testcase/frozen-file boundary, documentation maintenance rule, and forbidden shortcuts before editing. Before Coding creates `CONTRIBUTING.md`, the expected RED signal is `CONTRIBUTOR_ENTRYPOINT_MISSING`.

### Critical non-explicit guardrails

- `tests/architecture/intent-query/architecture-boundary.guard.js` — `ArchitectureBoundaryGuard`
- `tests/architecture/intent-query/dependency-direction.guard.js` — `DependencyDirectionGuard`
- `tests/architecture/intent-query/explicit-entrypoint-correctness.guard.js` — `ExplicitEntrypointCorrectnessGuard`
- `tests/architecture/intent-query/implementation-traceability.guard.js` — `KeyImplementationTraceabilityGuard`
- `tests/architecture/production-graph-rag/architecture-boundary.guard.js` — `ArchitectureBoundaryGuard`
- `tests/architecture/production-graph-rag/dependency-direction.guard.js` — `DependencyDirectionGuard`
- `tests/architecture/production-graph-rag/explicit-entrypoint-correctness.guard.js` — `ExplicitEntrypointCorrectnessGuard`
- `tests/architecture/production-graph-rag/implementation-traceability.guard.js` — `KeyImplementationTraceabilityGuard`
- `tests/architecture/production-graph-rag/coding-scope-authorization.guard.js` — `ArchitectureBoundaryGuard`
- `tests/architecture/production-graph-rag/credential-source-policy.guard.js` — `ExplicitEntrypointCorrectnessGuard`
- `tests/architecture/production-graph-rag/scoped-delivery-attribution.guard.js` — `KeyImplementationTraceabilityGuard`
- `tests/architecture/production-graph-rag/live-provider-contract.guard.js` — `ArchitectureBoundaryGuard`
- `tests/architecture/production-graph-rag/live-provider-opt-in.guard.js` — `ExplicitEntrypointCorrectnessGuard`
- `tests/architecture/production-graph-rag/live-provider-secret-isolation.guard.js` — `ExplicitEntrypointCorrectnessGuard`
- `tests/architecture/production-semantic-persistence/architecture-boundary.guard.js` — `ArchitectureBoundaryGuard`
- `tests/architecture/production-semantic-persistence/dependency-direction.guard.js` — `DependencyDirectionGuard`
- `tests/architecture/production-semantic-persistence/explicit-entrypoint-correctness.guard.js` — `ExplicitEntrypointCorrectnessGuard`
- `tests/architecture/production-semantic-persistence/implementation-traceability.guard.js` — `KeyImplementationTraceabilityGuard`
- `tests/architecture/production-default-retrieval/architecture-boundary.guard.js` — `ArchitectureBoundaryGuard`
- `tests/architecture/production-default-retrieval/dependency-direction.guard.js` — `DependencyDirectionGuard`
- `tests/architecture/production-default-retrieval/explicit-entrypoint-correctness.guard.js` — `ExplicitEntrypointCorrectnessGuard`
- `tests/architecture/production-default-retrieval/implementation-traceability.guard.js` — `KeyImplementationTraceabilityGuard`
- `tests/architecture/production-semantic-operator/architecture-boundary.guard.js` — `ArchitectureBoundaryGuard`
- `tests/architecture/production-semantic-operator/dependency-direction.guard.js` — `DependencyDirectionGuard`
- `tests/architecture/production-semantic-operator/explicit-entrypoint-correctness.guard.js` — `ExplicitEntrypointCorrectnessGuard`
- `tests/architecture/production-semantic-operator/implementation-traceability.guard.js` — `KeyImplementationTraceabilityGuard`
- `tests/architecture/production-semantic-operator/default-composition-wiring.guard.js` — `DependencyDirectionGuard`
- `tests/architecture/production-semantic-operator/authorized-production-safety.guard.js` — `ArchitectureBoundaryGuard`
- `tests/architecture/automatic-semantic-lifecycle/architecture-boundary.guard.js` — `ArchitectureBoundaryGuard`
- `tests/architecture/automatic-semantic-lifecycle/dependency-direction.guard.js` — `DependencyDirectionGuard`
- `tests/architecture/automatic-semantic-lifecycle/explicit-entrypoint-correctness.guard.js` — `ExplicitEntrypointCorrectnessGuard`
- `tests/architecture/automatic-semantic-lifecycle/implementation-traceability.guard.js` — `KeyImplementationTraceabilityGuard`
- `tests/architecture/automatic-semantic-lifecycle/lifecycle-invariants.guard.js` — `ArchitectureBoundaryGuard`
- `tests/architecture/mcp-semantic-query-contract/architecture-boundary.guard.js` — `ArchitectureBoundaryGuard`
- `tests/architecture/mcp-semantic-query-contract/dependency-direction.guard.js` — `DependencyDirectionGuard`
- `tests/architecture/mcp-semantic-query-contract/explicit-entrypoint-correctness.guard.js` — `ExplicitEntrypointCorrectnessGuard`
- `tests/architecture/mcp-semantic-query-contract/implementation-traceability.guard.js` — `KeyImplementationTraceabilityGuard`
- `tests/architecture/documentation/root-readme-reduction.guard.js` — `ArchitectureBoundaryGuard`
- `tests/architecture/documentation/contributor-governance-entrypoint.guard.js` — `ArchitectureBoundaryGuard`

The coding-scope authorization guard freezes mounted TS-08 and DT-19 evidence and prohibits seven-wave delivery or capacity-evidence targets unless the current approved handoff explicitly includes that testcase. The W7 quality handoff includes `ExplicitAcceptanceTestcase-TS-08`, `tests/explicit/entries/runSevenWaveDeliveryGates.js`, and `evaluateDeliverySequence(request)`, authorizing only the delivery sequence gate described there. The DT-19 capacity-evidence handoff includes `ExplicitAcceptanceTestcase-DT-05-R2-DT-19`, `tests/explicit/entries/runCapacityEvidence.js`, and `evaluateCapacityEvidence(request)`, authorizing only evidence recording without capacity-policy behavior. Corrected W3 handoffs may authorize TS-09 adapter/generation work through `codingTargets` and `taskExecutionPlan`.

For the W3 handoff, `architecture-boundary.guard.js`, `dependency-direction.guard.js`, `explicit-entrypoint-correctness.guard.js`, and `implementation-traceability.guard.js` are the critical non-explicit guards. They protect the W3 stable boundary, dependency direction, frozen entrypoint assertions, and mappings to `grag-seed-retrieval`, `grag-semantic-index`, `grag-index-lifecycle`, `grag-alignment-constraint`, `grag-embedding-provider-adapter`, and `grag-embedding-generation`.

For the W3.1 handoff, `architecture-boundary.guard.js`, `dependency-direction.guard.js`, `explicit-entrypoint-correctness.guard.js`, `implementation-traceability.guard.js`, `live-provider-contract.guard.js`, `live-provider-opt-in.guard.js`, and `live-provider-secret-isolation.guard.js` are critical non-explicit guards. They protect the W3.1 automatic mutation-triggered lifecycle boundary, dependency direction, explicit live opt-in, approved provider/secret contract, no-fake evidence rule, prohibition on Harness-side lifecycle execution or expected touched-record substitution, and mappings to `grag-wp-3-1`, `grag-index-lifecycle`, `grag-embedding-generation`, `grag-embedding-provider-adapter`, `grag-semantic-index`, `grag-alignment-constraint`, `grag-native-retrieval-service`, `grag-embedding-qualification`, and `grag-credential-boundary`.

For the W4 handoff, `architecture-boundary.guard.js`, `dependency-direction.guard.js`, `explicit-entrypoint-correctness.guard.js`, and `implementation-traceability.guard.js` are the critical non-explicit guards. They protect the production Graph RAG stable boundary, dependency direction, frozen W4 seed entrypoint assertions, and direct mapping to `grag-seed-retrieval` only. Closure, traversal expansion, neighborhood closure, and downstream graph completion remain excluded from W4 Coding targets.

For the W5 handoff, `tests/architecture/intent-query/architecture-boundary.guard.js`, `tests/architecture/intent-query/dependency-direction.guard.js`, `tests/architecture/intent-query/explicit-entrypoint-correctness.guard.js`, and `tests/architecture/intent-query/implementation-traceability.guard.js` are the critical non-explicit guards. They protect the getSystemArchitecture dispatch boundary, production-to-test dependency direction, frozen W5 entrypoint assertions, and direct mappings to `grag-purpose-closure`, `grag-intent-decision-policy`, `grag-implementation-policy`, `grag-repair-policy`, `grag-audit-policy`, and `grag-graph-tidy-policy`.

For the W6 handoff, `tests/architecture/intent-query/architecture-boundary.guard.js`, `tests/architecture/intent-query/dependency-direction.guard.js`, `tests/architecture/intent-query/explicit-entrypoint-correctness.guard.js`, and `tests/architecture/intent-query/implementation-traceability.guard.js` are the critical non-explicit guards. They protect the getSystemArchitecture version-evidence boundary, production-to-test dependency direction, frozen W6 entrypoint assertions, and direct mappings to `grag-coherent-context`, `grag-endpoint-closure`, `grag-view-closure`, and `grag-provenance`.

For the W7 handoff, `tests/architecture/production-graph-rag/architecture-boundary.guard.js`, `tests/architecture/production-graph-rag/dependency-direction.guard.js`, `tests/architecture/production-graph-rag/explicit-entrypoint-correctness.guard.js`, `tests/architecture/production-graph-rag/implementation-traceability.guard.js`, and `tests/architecture/production-graph-rag/coding-scope-authorization.guard.js` are the critical non-explicit guards. They protect the W7 quality benchmark boundary, delivery sequence gate, DT-19 capacity-evidence boundary when explicitly authorized, frozen DT-18/TS-08/DT-19 entrypoint assertions, direct mappings to `grag-quality-gate`, `grag-seven-wave-delivery`, and `grag-capacity-residual`, TS-08 and DT-19 as frozen evidence unless a current handoff explicitly targets them, and runner-owned `deliveryStatus` authority.

For WP-P1, the four `tests/architecture/production-semantic-persistence/` guards protect the separate durable production boundary, inward dependencies, frozen SP-01/SP-02 assertions and Harness, test-only cleanup isolation, exact graph entrypoint mappings, and direct mappings to `semprod-backfill-control`, `semprod-persistent-projection-requirement`, `grag-semantic-index`, `grag-index-lifecycle`, and `grag-embedding-provider-adapter`. Its dependency guard rejects every accepted persistence/runtime file as a Coding target but does not hard-code an obsolete successor handoff's unrelated adapter target set.

For WP-P2, the four `tests/architecture/production-default-retrieval/` guards protect the uninjected default composition, inward raw transport/driver dependency direction, frozen SP-03/SP-04 assertions and Harness, exact graph entrypoint mappings, and direct mappings to `semprod-default-vector-retrieval`, `semprod-readiness-requirement`, `grag-query-service`, `grag-mcp-interface`, `grag-native-retrieval-service`, `grag-seed-retrieval`, and `grag-purpose-closure`. They validate these delivered anchors against the graph and current frozen files rather than requiring the temporary handoff slot to remain permanently WP-P2-scoped.

For WP-P3, the nine `tests/architecture/production-semantic-operator/` guards additionally protect consent/readiness provenance and adapter lifecycle. Together they protect six function ports plus the sole attestation store, accepted reader compatibility, MCP operator routing, explicit package consent, structured wires, source safety, frozen SP-05 behavior, traceability, canonical authority, and runner-owned `deliveryStatus`. Windows ACL acceptance treats `F/M/RX/R/W/D/DE/RC/WDAC/WO/S/AS/MA/GR/GW/GE/GA/RD/WD/AD/REA/WEA/X/DC/RA/WA` as protected effective rights. Isolated production-process fixtures cover foreign allows and current-identity denies for the newly demonstrated `WD/AD/GW/GA/WEA/GR/RD` rights under deny precedence; the lifecycle guard exhaustively exercises every protected right on both file and parent ACLs, including compound advanced-right ACE parsing.

Historical production Graph RAG guards bind accepted TS-01/TS-06/TS-07/W3.1 behavior to graph-mounted acceptance paths and this persistent test contract. Only testcase work actually named by the current handoff is required to appear in its `explicitEntrypoints` and `frozenFiles`; the single temporary handoff slot is not a permanent registry for accepted predecessor scopes.

For the canonical semantic lifecycle successor, the six `tests/architecture/automatic-semantic-lifecycle/` guards protect stable composition, the exact five-file target set, inward dependency direction, all nine mounted testcase mappings across eight frozen physical entrypoints plus the shared Harness, and AST-bound actual init/write/query controls. The integration guard resolves the exported persistent lifecycle factory, rejects reachable runId/cleanup behavior, and requires both routers to compose `defaultSemanticRetrieval.js` with the unified production `readinessBoundary`. It rejects obsolete readiness identity literals and router-side provider/vector algorithm duplication. The suite also protects exact intent-path mappings, the two-gate/no-third-flag rule, external-credentials-only behavior, private WP-P1/WP-P2 consumption, actionable redacted category/message/action evidence, retired WP-P3 public exposure, test-only cleanup isolation, runner-owned delivery status, and the distinction between deterministic code-complete evidence and protected live-release evidence.

For the BP MCP semantic query contract WP-1 through WP-4 handoff, the four `tests/architecture/mcp-semantic-query-contract/` guards protect the public query/request boundary, production-to-test dependency direction, frozen `runMcpSemanticQueryContract.js#anchor` assertions including no-anchor public-path subcases, and exact mappings to `grag-mode-validation`, `grag-query-service`, `grag-canonical-graph`, `grag-endpoint-closure`, and `grag-view-closure`. They also freeze JavaScript fragment anchor execution as stage infrastructure for graph-mounted testcase entrypoints.

For the WP2 root README reduction handoff, `tests/architecture/documentation/root-readme-reduction.guard.js` protects the documentation content boundary: `README.md` may be changed only to become a concise adoption/product entry and router, while stable design, MCP, validator, lifecycle, contributor-governance, and domain-extension details stay behind curated links. The guard's expected pre-coding RED category is `ROOT_README_INLINE_SEMANTIC_LIFECYCLE_DETAIL`.

For the WP3 contributor governance handoff, `tests/architecture/documentation/contributor-governance-entrypoint.guard.js` protects the contributor safe-change boundary: Coding may create root `CONTRIBUTING.md` and should route maintainers to `design/README.md`, `design/argo-harness/README.md`, `design/mcp/意图架构 MCP 功能列表.md`, `design/validator/intent-architecture-mcp-validation.md`, `tests/ARCHITECTURE.md`, and `package.json` scripts without copying deep design specs inline. The guard's expected pre-coding RED category is `CONTRIBUTOR_ENTRYPOINT_MISSING`.

All explicit and critical paths listed here, plus `tests/harness/intentArchitectureQueryHarness.js`, `tests/harness/productionGraphRagHarness.js`, `tests/harness/liveEmbeddingProviderHarness.js`, `.argo/.env.example`, and `.gitignore`, are frozen during Coding/Repair unless the current handoff explicitly excludes them from `frozenFiles`.
