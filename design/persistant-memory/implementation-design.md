# View15 active-authority guide correction (2026-08-04)

- IntentDesign audit failed the prior handoff because the active-authority inventory omitted `design/mcp/意图架构 MCP 功能列表.md`, which still contains obsolete active seven-element wording.
- Expanded `tests/harness/viewCapacityPolicyHarness.js` and the active View15 contracts/traceability guard so `ExplicitAcceptanceTestcase-VIEW15-CONSISTENCY` covers active policy, graph descriptions, validation diagnostics, repair guidance, acceptance wording, current tests, validator docs, and the active MCP feature guide while preserving historical seven-element records.
- Reissued `.argo/temp/ImplementationToCodingHandoff.json` and `.argo/temp/view15-active-authority-expected-failures.json`. Focused execution of `node tests/explicit/entries/runView15Consistency.js` now records expected RED `VIEW15_CONSISTENCY_ACTIVE_SEVEN_AUTHORITY` for `design/mcp/意图架构 MCP 功能列表.md`; the four `tests/architecture/view-capacity-policy/` guards pass after the handoff target update.
- Corrected the handoff task completion wording after IntentDesign audit found a contradiction: `.argo/temp/view15-active-authority-expected-failures.json` remains frozen immutable pre-coding evidence, and CodingAndReparing must make `runView15Consistency.js` plus the four View15 guards pass without modifying that expected-failure record.
- `argo.validateStageHandoff` passed for `implementation-to-coding`. Full `argo.runArchitectureTests` baseline is 64/68: the in-scope expected failure is `ExplicitAcceptanceTestcase-VIEW15-CONSISTENCY`; existing out-of-scope failures remain `ExplicitAcceptanceTestcase-TS-07`, `ExplicitAcceptanceTestcase-W3-1-MutationEmbeddingVectorE2E`, and `ExplicitAcceptanceTestcase-SP-03-DefaultVectorRetrieval`. Runner refreshed `view15-active-authority-requirement` to `not_delivered`.

# View15 active-authority implementation handoff (2026-08-04)

- Consumed validated IntentDesign handoff for `view15-active-authority-requirement` / "Current Fifteen Wording With Historical Truth"; no intent graph mutation was required or performed.
- Updated root/runtime/test contracts and the View15 traceability guard so `view15-active-authority-requirement` maps to active diagnostics/remediation guidance and the frozen `ExplicitAcceptanceTestcase-VIEW15-CONSISTENCY` entrypoint. Strengthened `tests/harness/viewCapacityPolicyHarness.js` so the consistency entrypoint separately proves graph wording, active validator/guidance/current-test surfaces, and preserved historical seven-element evidence.
- Direct pre-handoff execution of `node tests/explicit/entries/runView15Consistency.js` plus the four `tests/architecture/view-capacity-policy/` guards passed. `.argo/temp/view15-active-authority-expected-failures.json` records no in-scope expected failures.
- Full `argo.runArchitectureTests` baseline is 65/68 with all View15 entries passing. Remaining failures are out of scope for this WorkPackage: `ExplicitAcceptanceTestcase-TS-07`, `ExplicitAcceptanceTestcase-W3-1-MutationEmbeddingVectorE2E`, and `ExplicitAcceptanceTestcase-SP-03-DefaultVectorRetrieval`.
- `.argo/temp/ImplementationToCodingHandoff.json` is regression-only for CodingAndReparing: run the frozen consistency entrypoint, make no code changes while it passes, and if it regresses repair only active authority surfaces while preserving historical decision records unchanged.

# View15 actual-write diagnostic audit correction (2026-08-04)

- Second IntentDesign audit found one remaining semantic gap: actual rejected write payloads were checked only for `status=failed` and `written=false`; maximum-15 and observed-count-16 diagnostics were asserted only on preview/dry-run payloads.
- Strengthened `assertActualWriteRejected()` in `tests/harness/viewCapacityPolicyHarness.js` so every actual rejected write path must also expose `must contain at most 15 elements` and `found 16`, with no old seven-element diagnostic. Direct execution now produces expected failures because current actual write responses are compact and omit the validation diagnostics.
- Refreshed `.argo/temp/ImplementationToCodingHandoff.json` and `.argo/temp/view15-expected-failures.json` to authorize Coding repair only in `.argo/scripts/systemarchitecture-mcp-server.js`: failed actual mutation write responses must carry the same maximum/observed diagnostics as preview/dry-run responses while preserving `written=false`, exact-15 persistence, and rejected-write non-persistence.

# View15 enforcement-completeness audit correction (2026-08-04)

- IntentDesign audit found the prior ImplementationDesign handoff under-proved the approved preview/write boundary: exact-15 was preview-only, overflow lacked observed-count/non-persistence assertions, indirect relationship growth was dry-run-only, and direct addElement/updateView growth routes were incomplete.
- Strengthened `tests/harness/viewCapacityPolicyHarness.js`, `runView15GlobalScope.js`, `runView15IndirectGrowth.js`, and the View15 explicit-entrypoint correctness guard. The global entrypoint now proves preview acceptance and actual write persistence at 15, maximum-15/observed-16 diagnostics plus actual write rejection/non-persistence at 16, and focused/batch direct addElement/updateView growth rejection. The indirect entrypoint now proves focused/batch relationship add/update diagnostics, actual write rejection, and unchanged temporary graph membership.
- Refreshed `.argo/temp/ImplementationToCodingHandoff.json` and `.argo/temp/view15-expected-failures.json`; direct execution of the strengthened View15 entrypoints and correctness guard passes, so CodingAndReparing still receives no behavior repair target from this handoff unless a future regression appears.

# View15 enforcement-completeness implementation handoff (2026-08-04)

- Consumed validated IntentDesign handoff for `view15-enforcement-completeness-requirement` from commit `0bf06705b149ae0d785576a9cc7bd047e1fb4345`; no intent graph mutation was required or performed.
- Updated implementation trace contracts to bind `view15-enforcement-completeness-requirement` to the canonical mutation validation boundary and frozen Query Acceptance Boundary entrypoints. `runView15GlobalScope.js` physically covers `ExplicitAcceptanceTestcase-VIEW15-BOUNDARY` and `ExplicitAcceptanceTestcase-VIEW15-OVERFLOW`; `runView15IndirectGrowth.js` covers `ExplicitAcceptanceTestcase-VIEW15-INDIRECT-GROWTH`.
- Direct pre-handoff execution of `node tests/explicit/entries/runView15GlobalScope.js` and `node tests/explicit/entries/runView15IndirectGrowth.js` passed. `.argo/temp/view15-expected-failures.json` now records baseline-pass classifications and `.argo/temp/ImplementationToCodingHandoff.json` contains no Coding repair targets; any future failure in the frozen View15 entrypoints or guards requires a new approved repair handoff.

# View15 second handoff audit correction (2026-08-04)

- IntentDesign re-audit of commit `78f6e506e9d06f1910169693d1cd4ec94df28df7` found two remaining ImplementationDesign coverage issues. Corrected only test/handoff artifacts; no production behavior or intent graph was changed.
- Global-scope candidate Views now include the required `Viewpoint: <ViewpointName>; Concern: <concern>; Purpose: <purpose>; Scope: <scope>; Rationale: <rationale>` description binding, so the multi-Viewpoint/category scenarios are otherwise-valid viewpoint-scoped View candidates.
- `design/validator/intent-architecture-mcp-validation.md` is now treated as active validator guidance in the consistency Harness. Historical preservation remains limited to the decision-tree history evidence; current validator guidance must be updated by Coding to the active 15-element authority.

# View15 handoff audit correction (2026-08-04)

- IntentDesign audit of commit `288dd8ca76337cd16665150a34c101cc59045590` found four ImplementationDesign coverage gaps. Corrected the Harness and handoff without changing production behavior or intent graph semantics.
- `runView15GlobalScope.js` now exercises the 8/15/16 boundary through multiple valid parent Viewpoints/member categories: stakeholder motivation with Requirement members, business behavior with Business Actor members, and application realization with Application Component members.
- `runView15NoMigration.js` now copies the canonical graph, captures every canonical View membership array, performs a dry-run activation marker, and compares all canonical memberships after the preview. This corrected entrypoint currently passes and is a frozen regression guard.
- `runView15Consistency.js` now verifies historical seven-element evidence remains present before checking active validator/guidance/test surfaces for stale seven-element authority. `runView15IndirectGrowth.js` now covers focused `addArchitectureRelationship`/`updateArchitectureRelationship` dry-runs as well as batch relationship add/update routes.

# View15 implementation handoff finalized (2026-08-04)

- IntentDesign repair commit `d8f2cc6e686543d84f8cd58bf329a2856b16877b` completed the executable acceptance trace repair. Re-read the repaired intent handoff and verified all graph-mounted View15 acceptanceCriteria now point to exact executable entrypoints.
- Finalized View15 contracts, explicit entrypoints, Harness, critical guards, `.argo/temp/view15-expected-failures.json`, and `.argo/temp/ImplementationToCodingHandoff.json`. The handoff freezes all seven View15 testcase anchors: global scope, relationships, no migration, consistency, boundary, overflow, and indirect growth.
- The superseded `design/KG/ImplementationToIntentTraceProposal.json` was removed because no remaining intent graph mismatch is active after the repair. Coding receives expected RED signals only for production behavior still using the old seven-element policy.

# View15 trace proposal corrected with exact anchors (2026-08-04)

- Corrected the second `design/KG/ImplementationToIntentTraceProposal.json` after IntentDesign rejected probabilistic mappings. Exact mappings now are: `VIEW15-CONSISTENCY -> tests/explicit/entries/runView15Consistency.js`, `VIEW15-BOUNDARY -> tests/explicit/entries/runView15GlobalScope.js`, `VIEW15-OVERFLOW -> tests/explicit/entries/runView15GlobalScope.js`, and `VIEW15-INDIRECT-GROWTH -> tests/explicit/entries/runView15IndirectGrowth.js`.
- Added `runView15Consistency.js` and `runView15IndirectGrowth.js` plus Harness methods `observeActiveAuthorityConsistency()` and `observeIndirectEndpointMembershipGrowth()`. Direct execution proves both are real entrypoints and currently RED for meaningful reasons: active validator/guidance/test surfaces still expose old seven-element authority, and indirect endpoint growth reports the old seven-element limit instead of maximum 15.
- `argo.validateTraceProposal` passed for the corrected proposal. ImplementationToCoding handoff emission, full architecture baseline, and commit remain intentionally paused until IntentDesign applies the remaining graph acceptanceCriteria repair.

# View15 implementation handoff second trace blocker (2026-08-04)

- Resumed after IntentDesign commit `1a26c032f04799364a1a68a716aafd96869df0a1`; the three WorkPackage testcase paths now point to `tests/explicit/entries/runView15GlobalScope.js`, `runView15Relationships.js`, and `runView15NoMigration.js`.
- Materialized View15 contracts, expected-failure record, critical guards, and draft `.argo/temp/ImplementationToCodingHandoff.json`. Direct View15 entrypoint execution classified all three WorkPackage signals as expected failures against the old seven-element validator; the four `tests/architecture/view-capacity-policy/` guards pass.
- `argo.validateStageHandoff stage=implementation-to-coding` is still blocked because four additional graph-mounted View15 testcases retain descriptive `acceptanceCriteria`: `ExplicitAcceptanceTestcase-VIEW15-CONSISTENCY`, `ExplicitAcceptanceTestcase-VIEW15-BOUNDARY`, `ExplicitAcceptanceTestcase-VIEW15-OVERFLOW`, and `ExplicitAcceptanceTestcase-VIEW15-INDIRECT-GROWTH`. Wrote and validated `design/KG/ImplementationToIntentTraceProposal.json` for this second IntentDesign repair. Full architecture test baseline and ImplementationDesign commit remain stopped until F1 can pass.

# View15 implementation handoff blocked by intent trace gap (2026-08-04)

- Intent-to-implementation handoff for `view15-global-limit-principle`, `view15-counting-semantics-requirement`, and `view15-prospective-stability-constraint` was read and accepted as the stage input. The intent testcases are mounted under the right elements, but their `acceptanceCriteria` values are descriptive business sentences instead of executable workspace-relative entrypoint paths.
- Materialized implementation-side anchor files only: `tests/harness/viewCapacityPolicyHarness.js`, `tests/explicit/entries/runView15GlobalScope.js`, `tests/explicit/entries/runView15Relationships.js`, and `tests/explicit/entries/runView15NoMigration.js`. Direct runs classify the current pre-coding signals as expected RED failures: 8-element global scope still fails the former seven-element limit, 15-element relationship-rich Views still fail the former seven-element limit, and pre-existing 10-member Views are still rechecked against the former limit during dry-run policy activation.
- Wrote and validated `design/KG/ImplementationToIntentTraceProposal.json` for IntentDesign to replace the three descriptive testcase `acceptanceCriteria` values with the concrete entrypoint paths. Because ImplementationDesign may not directly mutate `design/KG/SystemArchitecture.json`, `.argo/temp/ImplementationToCodingHandoff.json`, full `argo.runArchitectureTests`, `argo.validateStageHandoff stage=implementation-to-coding`, and the ImplementationDesign commit were intentionally stopped until the intent graph is repaired.

# Documentation link/fact verification WP5 handoff (2026-07-28)

- Materialized `.argo/temp/ImplementationToCodingHandoff.json` for `docsys-wp-link-fact-verification` from validated IntentDesign commits `d4fa7262fe9480b5a7cb85d47fbba6ac8aabde9c` and `66ef374471a142afc72f5fa4cd7464435be9ef70`. Scope is verification-only: README.md, CONTRIBUTING.md, design/README.md, and routed stable design references must remain truthful and link to existing authorities.
- Confirmed `OVERALL_ARCHITECTURE.md` exists and updated the root contract to declare the now-existing contributor governance entry and stable design documentation map without inventing absent authorities. `tests/ARCHITECTURE.md` now records WP5 as a frozen regression verification handoff over DOC-01 through DOC-04 and the three documentation guards.
- Ran DOC-01 through DOC-04 via `tests/explicit/entries/runDocumentationInformationArchitectureDecision.js` and all passed. Ran `tests/architecture/documentation/root-readme-reduction.guard.js`, `contributor-governance-entrypoint.guard.js`, and `design-navigation.guard.js`; all passed. Local routed-link verification across `README.md`, `CONTRIBUTING.md`, and `design/README.md` passed after correcting shell quoting in the ad hoc Node command.
- Wrote `.argo/temp/wp5-link-fact-verification-expected-failures.json` with no in-scope expected failures. Full architecture baseline is 54/56 with DOC-01 through DOC-04 passing; the only failures are out-of-scope `ExplicitAcceptanceTestcase-W3-1-MutationEmbeddingVectorE2E` (`W31_TOUCHED_RECORD_EXTRACTION_INCOMPLETE`) and `ExplicitAcceptanceTestcase-SP-03-DefaultVectorRetrieval` (`SP03_APPROVED_SOURCE_DIRECT_ATTRIBUTION_MISMATCH`). CodingAndReparing receives only a regression-only verification task; documentation repair requires a later approved handoff if DOC anchors, documentation guards, routed links, or root-contract truthfulness regress.

# Documentation design navigation WP4 handoff (2026-07-28)

- Materialized `.argo/temp/ImplementationToCodingHandoff.json` for `docsys-wp-design-navigation` from IntentDesign commit `7c23cbd0955a73577e00757d5c7c3a99fc857295`. Scope is only `design/README.md`; root `README.md` remains the adoption/product router, root `CONTRIBUTING.md` remains the contributor/governance entrypoint, and `design/README.md` must become the stable design/system map plus fact-source governance entry.
- Added frozen critical guard `tests/architecture/documentation/design-navigation.guard.js` and registered it in `tests/ARCHITECTURE.md` as the WP4 `ArchitectureBoundaryGuard`. DOC-03 and DOC-04 passed directly; the WP4 guard is intentionally RED before Coding with `DESIGN_NAVIGATION_ROUTE_MISSING` because `design/README.md` does not yet link the root product/governance entries.
- Wrote `.argo/temp/wp4-design-navigation-expected-failures.json` as the Coding repair signal. Coding is authorized to edit only `design/README.md`; explicit DOC entrypoints, documentation guards, `tests/ARCHITECTURE.md`, `design/KG/SystemArchitecture.json`, root README, CONTRIBUTING, package scripts, and handoff artifacts are frozen.
- No root `OVERALL_ARCHITECTURE.md` was observed or invented. `.argo/scripts/systemarchitecture-mcp-server.js` has an unrelated dirty change and remains outside WP4 staging, committing, and evidence.

# Documentation contributor governance WP3 corrective handoff (2026-07-28)

- Materialized `.argo/temp/ImplementationToCodingHandoff.json` for `docsys-wp-contributor-entrypoint` only. Coding scope is narrowed to creating root `CONTRIBUTING.md`; runner-owned `deliveryStatus` and `design/KG/test-failure-records.json` refreshes are allowed only as validation side effects.
- Corrective repair removed the WP3 root `OVERALL_ARCHITECTURE.md` dependency and restored that file to its pre-WP3 content. WP3 uses `tests/ARCHITECTURE.md` for DOC-02 test ownership and treats absent root-contract evidence as an honest gap outside the contributor guide Coding target.
- Strengthened `tests/explicit/entries/runDocumentationInformationArchitectureDecision.js#doc-02-contributor-governance-router` to observe the physical contributor guide. Both DOC-02 and the new guard intentionally RED before Coding with `CONTRIBUTOR_ENTRYPOINT_MISSING`.
- IntentDesign audit repair tightened `tests/architecture/documentation/contributor-governance-entrypoint.guard.js`: once `CONTRIBUTING.md` exists, any `OVERALL_ARCHITECTURE.md` mention fails with `CONTRIBUTOR_GOVERNANCE_ROOT_CONTRACT_AUTHORITY` so the guide cannot reintroduce the root contract as an active WP3 authority, dependency, or reference.
- No intent graph mutation or `ImplementationToIntentTraceProposal.json` is required because the validated intent context already designates root-level `CONTRIBUTING.md` for WP3. The corrective handoff does not claim root `OVERALL_ARCHITECTURE.md` as WP3 authority and does not require Coding to create, edit, or reference it.

# Documentation README reduction WP2 handoff (2026-07-28)

- Refreshed validated `docsys-wp-root-readme-reduction` intent handoff from IntentDesign commit `c19d161b2bbb39a0bd22bb7e54323b7264857041`. Scope remains WP2 only: keep root `README.md` as a concise adoption/product entry and router while retaining positioning, problem framing, certainty formula, three components, high-level delivery flow, quick start/deployment checks, entry selection, and curated next links.
- Root and test contracts now declare `README.md` as the Root Documentation Entry mapped to `docsys-root-readme-entry`, routing to `docsys-stable-design-reference`, and indirectly enforcing `docsys-content-boundary-requirement`. WP2 does not authorize `CONTRIBUTING.md` creation, full contributor guide work, design navigation calibration, intent graph edits, or behavior implementation.
- Added frozen critical guard `tests/architecture/documentation/root-readme-reduction.guard.js`. DOC-01, DOC-03, DOC-04, and the guard all pass in the refreshed ImplementationDesign classification, so CodingAndReparing should treat WP2 as verification-only unless `README.md` regresses with `ROOT_README_INLINE_SEMANTIC_LIFECYCLE_DETAIL` or missing adoption/router content.
- `.argo/temp/ImplementationToCodingHandoff.json` supersedes the prior WP1 temporary handoff and authorizes Coding only to verify or repair `README.md`. It freezes contracts, DOC entrypoints, the guard, intent graph, failure records, and stable design references. `argo.validateStageHandoff(stage="implementation-to-coding")` must pass after the refreshed pre-coding runner baseline; current out-of-scope failures remain W3.1 `W31_TOUCHED_RECORD_EXTRACTION_INCOMPLETE` and SP-03 `SP03_APPROVED_SOURCE_DIRECT_ATTRIBUTION_MISMATCH`.

# Documentation IA WP1 handoff (2026-07-28)

- WP1 `docsys-wp-information-architecture` is ImplementationDesign no-op verification scope: the accepted dual-entry information architecture is already represented in `design/KG/SystemArchitecture.json` and the decision tree, so Coding/Repair is not authorized to edit `README.md`, create `CONTRIBUTING.md`, change design navigation, or run link/fact verification under this handoff.
- Physicalized DOC-01 through DOC-04 at `tests/explicit/entries/runDocumentationInformationArchitectureDecision.js` with four anchors. The graph testcase `acceptanceCriteria` values now point to executable paths; business semantics, element/view counts, and delivery order remain unchanged.
- `tests/ARCHITECTURE.md` owns the new documentation information architecture entrypoint. `.argo/temp/ImplementationToCodingHandoff.json` freezes the no-coding task plan, expected failure records path, and WP2-next boundary.
- Residual risk is downstream only: root-level `CONTRIBUTING.md` remains the recommended WP3 filename unless later implementation evidence blocks it. `OVERALL_ARCHITECTURE.md` exists and is referenced honestly; no root contract was invented.

# Canonical init durable-transition correction (2026-07-27)

- Independent follow-up audit found reconciliation failure asserted only Failed/Stale. SP-05 now injects a dedicated raw reconciliation secret canary at the controlled runtime boundary, preserves the unsanitized diagnostic only in a private observer, and requires exact `SEMANTIC_RECONCILIATION_FAILED` category, safe message, and actionable remediation in both durable same-record evidence and exported init error. Public fallback is literal false; all durable/public serialization is scanned for zero canary leakage.
- Exact safe-message wire preservation narrowly reauthorizes `.argo/scripts/argo-mcp-server.js` beside `systemarchitecture-mcp-server.js` and `semanticOperatorJourney.js` for an init-specific error path. Shared `semanticOperatorError.js` query mapping remains frozen so its accepted exact field surface is not widened.
- Final IntentionDesign audit found that SP-05 began each gate/configuration scenario without prior durable authorization, so a stale production `Aligned` record could survive disabled or failed `argo init` even though all prior controlled tests passed.
- Frozen SP-05 now seeds a complete verified Aligned record before every isolated disabled/invalid/configuration scenario and before the interruption/resume/rerun chain. The shipped `initializeWorkspace` call must transform the same identity, `recordId`, and canonical version at a monotonically higher revision before every outcome.
- Both-disabled must durably become SemanticIndexPending/disabled. Half-enabled, malformed, missing/unsafe external configuration, and reconciliation failure must durably become actionable redacted Failed/Stale, with zero provider/vector effects on every invalid/configuration path and no surviving prior Aligned authorization.
- Enabled work is observed at the backfill boundary and must already see invalidated readiness before provider/vector work. Interruption records Failed/Stale; resume and rerun invalidate again; verified Aligned is written on the same record only after queryability and global coherence. Existing checkpoint resume/no-replay and idempotent rerun assertions remain.
- This correction authorizes only `.argo/scripts/systemarchitecture-mcp-server.js` and `.argo/scripts/graph-rag/semanticOperatorJourney.js`. Gateway, mutation lifecycle, WP-P2 retrieval, WP-P1 persistence/runtime, configuration, provider, contracts, intent, and all frozen acceptance files remain unchanged Coding dependencies.
- Final pre-coding evidence is 42/43 with exactly SP-05 RED at `SP05_DISABLED_DURABLE_READINESS_NOT_FAIL_CLOSED`; seven unique scoped entrypoints and all six lifecycle guards pass. The runner wrote one failure record and changed only `semprod-operator-journey-process` from delivered to not_delivered.

# Canonical lifecycle handoff-baseline correction (2026-07-27)

- Independent audit found the handoff still described the pre-implementation nine-RED baseline after Coding had completed C1-C3. Fresh evidence is 41/43: six unique scoped entrypoints pass, and only SP-03/SP-04 remain expected RED. Five lifecycle guards pass; `integration-control-points.guard.js` is intentionally RED on router-side WP-P2 duplication.
- Passing SP-05, SP-01, SP-02, DT-16/DT-16-SemanticIndex, controlled W3.1, and TS-00 entries no longer carry stale `failureReason` values. C1-C3 are explicitly completed regression prerequisites. Their task steps now preserve passing invariants instead of instructing Coding to reproduce obsolete failures.
- Handoff schema requires non-empty `failureSignal` and `nextAction` for every authorized target. Completed C1-C3 targets therefore carry explicit `REGRESSION_ONLY` passing-baseline signals and `No completed C… implementation work` constraints instead of old missing-production signals. `systemarchitecture-mcp-server.js` and `defaultSemanticRetrieval.js` carry only the current C4 SP-03/SP-04/integration evidence.
- The architecture-boundary guard now freezes the fresh six-pass/two-RED entrypoint set, exact SP-03/SP-04 signals, five-green/one-intentional-RED guard status, absence of stale failure reasons and obsolete target failure categories, regression-only completed-target instructions, and completed C1-C3 task status.
- The five-file target authorization and all unified readiness, WP-P2 algorithm-reuse, redaction, gate, persistence, cleanup, compatibility, runner-owned delivery, and code-complete/live-release constraints remain unchanged.

# Canonical lifecycle WP-P2 target-scope correction (2026-07-27)

- Coding correctly identified that the frozen `defaultSemanticRetrieval.js` private reader still hardcodes obsolete readiness identity `argo-production-semantic-index` and exposes no production `readinessBoundary` dependency. The prior four-file authorization made compliant reuse of the unified durable record impossible without router-side algorithm duplication.
- The authoritative minimal Coding target set is now five files: the prior four plus `.argo/scripts/graph-rag/defaultSemanticRetrieval.js`. Its authorization is narrow: accept/read the unified production readiness boundary, remove the obsolete identity, and preserve stored redacted category/message/action. Existing provider qualification, threshold filtering, expanding-window exhaustion, purpose closure, endpoint/View closure, provenance, canonical completion, and raw-port test composition remain frozen behavior.
- New controlled acceptance alternates Aligned/Failed revisions under one production readiness `recordId` and invokes both exported System/unified routers. Aligned calls must exercise the real WP-P2 provider/vector windows and complete closure; Failed calls must preserve the exact actionable whitelist, perform zero provider/vector work, and exclude an unknown diagnostic secret canary.
- The integration guard now rejects router-local `executeProductionSemanticQuery`, `queryProductionVectorChannels`, or Vector Index Cypher duplication, requires `createDefaultSemanticRetrieval({ readinessBoundary, ... })`, and rejects the obsolete readiness identity in WP-P2.
- This is an ImplementationDesign scope correction only. No production behavior or intent semantics are changed; runner-owned delivery status and failure records will be refreshed from the new frozen RED entrypoints.

# Canonical lifecycle readiness-order correction (2026-07-27)

- Coding safely stashed production WIP at `8abb9fb57f8a822fa133b5d9d2461b61a487f6ef`; ImplementationDesign did not inspect, apply, drop, or modify that stash or any production target.
- The shared-store assertion incorrectly used an all-occurrences phase comparison: the maximum index of every durable read had to precede the minimum `init-aligned`, while a separate assertion required a durable read after alignment. That made the accepted post-init success reads impossible.
- Frozen ordering now partitions by the first and final alignment. Exactly two System/unified failure reads must precede the first alignment and contain the Failed/Stale record; each resume/rerun alignment must have queryability then global coherence in its own segment; no exported read may occur between repeated init alignments; and exactly two restored reads must follow the final alignment and contain the transformed Aligned record.
- Stable readiness identity, `recordId`, canonical version, increasing revision, exact pre/post snapshots, provider correlation, and all previous shared-store assertions remain unchanged. The correction removes only contradictory index aggregation and tightens the previously ambiguous repeated-alignment semantics.

# Canonical lifecycle third audit correction (2026-07-27)

- This correction removes the last seam-copy in incremental recovery evidence. Actual mutation lifecycle readiness writes, exported System and unified rejection reads, shipped `argo init` reconciliation, and restored System/unified success reads now use one filesystem-backed raw readiness record. The Harness does not reconstruct readiness from public mutation evidence.
- The backing record has one stable identity and random `recordId`; every write is atomic temporary-file replacement with a monotonically increasing revision. Frozen assertions require exact pre-init failure record reads through both exported routers, unchanged record bytes during rejection, stable identity/recordId/canonicalVersion through init, queryability → global coherence → Aligned write ordering, and exact post-init Aligned record reads plus provider execution through both routers.
- The integration guard now resolves the exact `module.exports`/`exports` production symbol `createPersistentMutationEmbeddingLifecycle`, including direct functions, inline functions, alias exports, and transitive identifier aliases. Its reachable call graph follows aliased delegated helpers and rejects cleanup/runId anywhere reachable.
- Executable guard fixtures prove a compliant alias export resolves; an unsafe alias export, aliased-helper cleanup, delegated-arrow cleanup, and missing export all reject. The repository guard now intentionally reports `production factory export createPersistentMutationEmbeddingLifecycle not resolved` instead of silently passing while Coding has not supplied the accepted factory. Five other lifecycle guards remain pre-Coding green; this one is a frozen production RED input.
- Coding authorization and the four-file target set are unchanged. No production code or intent graph is modified by this correction.

# Canonical lifecycle second audit correction (2026-07-27)

- This section supersedes lifecycle-test evidence details in the correction immediately below. No production behavior or intent graph was changed; Coding remains authorized for exactly the same four production targets.
- Init controls now use the shipped uninjected JSON-RPC `tools/call initializeWorkspace` path. Zero provider/vector evidence comes from deltas on a concrete production runtime and durable store, never absent response fields or `|| 0`. Missing/unsafe raw configuration ports retain an unsanitized secret canary separately; assertions inspect actual public JSON-RPC evidence for actionable fields and prove the canary is absent without Harness sanitization.
- WP-P1 controlled init exposes raw final-readiness ports. Resume must produce exactly queryability verification → global-coherence verification → Aligned recording after concrete checkpoint/interruption/no-replay work; rerun remains idempotent.
- Incremental acceptance independently logs raw readiness, configuration, provider, store, queryability, coherence, and readiness-record operations. Disabled, half-enabled, malformed, missing, and unsafe controls require exact zero provider/store-vector operations. Provider/persistence/queryability/coherence failures require actionable redacted public evidence, a separately persisted Failed/Stale record, canonical-byte authority, and later exported semantic-query rejection with `fullSnapshotFallback:false`. Readiness invalidation is the first lifecycle operation and precedes configuration; successful order is provider → persistence → queryability → coherence → Aligned. Batch preview plus all nine focused adapter dry-runs are inert.
- Exported uninjected System and unified queries cover SemanticDisabled, SemanticIndexPending, Partial, Stale, Failed, and repeated Aligned calls. Every call carries an independently measured readiness/provider/vector delta: one readiness read always, no provider/vector work for rejection, and provider/vector work only for Aligned. Both no-argument and anchored graph-tidy controls retain exact canonical bytes with zero semantic effects.
- The integration AST guard now follows reachable function declarations and arrow functions, resolves destructured and transitive factory aliases, ties adapter names to the returned invocation lookup rather than dead literals, and traverses production lifecycle delegation for cleanup/runId. Executable alias, dead-array, arrow-factory, and delegated-cleanup fixtures must fail.
- Current scoped RED categories are `SP05_CANONICAL_ARGO_INIT_LIFECYCLE_MISSING`, `SP01_BACKFILL_TOOL_NOT_PRIVATE`, `SP02_ACTUAL_MUTATION_TEST_COMPOSITION_MISSING`, `DT16_ACTUAL_MUTATION_TEST_COMPOSITION_MISSING`, `W31_ACTUAL_MUTATION_TEST_COMPOSITION_MISSING`, `SP03_SYSTEM_SEMANTICDISABLED_READINESS_NOT_FRESH`, `SP04_SYSTEM_SEMANTICDISABLED_READINESS_NOT_FRESH`, and `TS00_SYSTEM_SEMANTICDISABLED_READINESS_NOT_FRESH`. They identify missing production integration, not Harness wiring.

# Canonical lifecycle audit correction (2026-07-27)

- This correction supersedes the WP-P3 outward-routing and eight-target statements immediately below. The authoritative minimal Coding target set is exactly `.argo/scripts/argo-mcp-server.js`, `.argo/scripts/systemarchitecture-mcp-server.js`, `.argo/scripts/graph-rag/semanticOperatorJourney.js`, and `.argo/scripts/graph-rag/mutationEmbeddingVectorLifecycle.js`; accepted configuration, WP-P1 persistence/backfill/checkpoint/Neo4j adapter, WP-P2 retrieval, runtime, provider, and live-test cleanup modules are frozen inward dependencies.
- Rule 26 in `OVERALL_ARCHITECTURE.md` is explicitly historical and superseded: WP-P1 `runSemanticBackfill` is reachable only as a private canonical `argo init` port. SP-01 again exercises concrete production backfill, four-method store, Neo4j adapter, and durable checkpoint behavior with interruption/resume, independent no-replay observations, complete three-channel evidence, idempotent rerun, canonical-byte authority, and final-only alignment.
- SP-05 invokes shipped uninjected `argo-mcp-server.callTool("initializeWorkspace")` for disabled, half-enabled, malformed non-`"1"`, missing, and unsafe controls. Controlled enabled/resume/rerun uses a module-owned callback composition around the same shipped call and a real `createProductionGraphRagRuntime` over concrete WP-P1 adapters; no private journey factory or self-reporting backfill result is accepted.
- SP-02, DT-16, and W3.1 no longer call the proposed lifecycle or fabricate touched IDs. Their frozen Harness requires a module-owned callback-scoped raw-port composition, then executes all 18 actual batch/focused add/update/remove Element/ArchitectureRelationship/View MCP adapters. It independently records readiness, provider, four-method persistence, queryability, and coherence operations; freezes exact touched arrays, remove-to-tombstone, preview zero effects, disabled/malformed/half-enabled/configuration/failure matrices, canonical bytes after semantic failure, later actual init recovery, and a production persistent factory with no runId/cleanup surface.
- SP-03, SP-04, and TS-00 invoke exported System and unified `getSystemArchitecture(query)` without a supplied journey. Four calls in one controlled WP-P2 composition must each read durable readiness before provider/vector work. No-argument and graph-tidy compatibility remain frozen, and retired routing is checked through both dispatchers.
- Six lifecycle guards now pass. The new TypeScript AST guard binds acceptance to shipped init, all actual write adapters, exported uninjected queries, and concrete WP-P1 composition; comments-only, direct private-factory, injected-query, and persistent runId/cleanup counterfixtures fail.
- Current scoped RED categories are `SP05_CANONICAL_ARGO_INIT_LIFECYCLE_MISSING`, `SP01_BACKFILL_TOOL_NOT_PRIVATE`, `SP02_ACTUAL_MUTATION_TEST_COMPOSITION_MISSING`, `DT16_ACTUAL_MUTATION_TEST_COMPOSITION_MISSING`, `W31_ACTUAL_MUTATION_TEST_COMPOSITION_MISSING`, `SP03_SYSTEM_EXPORTED_QUERY_REQUIRES_INJECTED_JOURNEY`, `SP04_SYSTEM_EXPORTED_QUERY_REQUIRES_INJECTED_JOURNEY`, and `TS00_SYSTEM_EXPORTED_QUERY_REQUIRES_INJECTED_JOURNEY`. These are missing-production signals after valid control-point wiring.
- Final pre-coding runner refresh invoked unified `argo.callTool("runArchitectureTests")`: 43 total, 34 passed, 9 scoped expected failures, 0 missing criteria, and 0 out-of-scope failures. `design/KG/test-failure-records.json` was refreshed; `SystemArchitecture.json` has no new diff from the previously committed 28-element runner-owned `not_delivered` baseline.

# WP-P3 final bypass correction (2026-07-27)

- TS-00 typed MCP contract had the same inherited dependency defect after the first migration: its semantic success scenario still passed `semanticRetrievalBoundary` directly to exported wrapper `callTool`. It now injects an approved Harness-owned `semanticOperatorJourney` adapter around the same controlled document boundary, preserving every typed and legacy envelope assertion. The intent-query correctness guard binds wrapper argument four, the journey property, exact top-level adapter factory, and bound retrieval parameter; raw, missing, undefined, dead-decoy, and shadowed-factory fixtures fail.
- Inherited SP-03/SP-04/compatibility Harnesses previously called exported semantic `callTool` through a raw-retrieval dependency shape. After public raw fallback is removed, that composition fails before exercising accepted WP-P2 behavior. Both Harnesses now inject a test-owned approved `semanticOperatorJourney` adapter around the same accepted default/deterministic/probe retrieval boundaries and preserve the established MCP envelope; assertions and no-argument/graph-tidy behavior are unchanged. Both inherited correctness guards now use TypeChecker symbol identity and call flow: wrapper-fourth/inner-third property values must reach the exact top-level approved adapter factory, whose query must call `retrieve` on its bound WP-P2 parameter. Missing/undefined journey values, dead conditionals/decoys, raw-boundary substitution, and shadowed factories are executable negative fixtures.
- Coding correctly found that the frozen primary Harness still omitted the mandatory store and therefore depended on the exact volatile fallback that acceptance prohibits. All primary success/rejection compositions now receive one contract-faithful controlled store with record/read/clear and exact drift validation. It never substitutes for filesystem/ACL trust: the adapter Harness retains the isolated missing-store rejection and all durable owner/ACL/link/atomic scenarios. A structural negative self-fixture rejects any future removal of the primary store binding.
- Final coding audit against `96f0fc2` proved exported System/unified `callTool` could select raw semantic retrieval whenever no `semanticOperatorJourney` dependency was supplied. Frozen acceptance invokes both exported functions with no journey and a raw-retrieval trap. The guard follows TypeChecker-resolved aliases/helpers from `callTool`, permits only the injected/default operator query chain, and has executable direct, alias, helper, and resolver-delegation bypass fixtures.
- A process-local `readinessVerified` shortcut also bypassed the durable record and current WP-P2 drift after same-process verification. The store is now a mandatory seventh object dependency, volatile fallback is prohibited, and every query must perform durable read → current WP-P2 read → exact validation → retrieval.
- Windows trust is exact principal/effective-access policy rather than substring rejection. Owner equals current identity only; file and parent ACLs require effective current-user modify/full access after deny precedence, permit protected allows only for that identity and SYSTEM, and reject group ownership, isolated file/parent allows for BUILTIN Users, BUILTIN Administrators, and Authenticated Users, broad/foreign allows, absent or denied current identity, malformed evidence, and permissive parents.
- Exact safe remediation is frozen without raw metadata: `Restrict .argo/temp and semantic-readiness-attestation.json ownership and ACLs to the current OS identity and SYSTEM, then run semantic readiness again`. The current host `.argo/temp` includes `BUILTIN\Users`; it remains release-blocking until operators harden that ACL and rerun readiness. Positive Windows fixtures pre-create `.argo/temp`, remove inheritance, grant only current identity/SYSTEM, and inspect the result. Fixture-local metadata injection remains limited to controlled owner/negative evidence and cannot weaken production policy.
- Coding authorization remains the same eight production paths. No intent semantics changed and no trace proposal is needed.

# WP-P3 adapter attestation trust correction (2026-07-27)

- Follow-up audit required the guard to prove the active writer rather than inventory atomic calls anywhere in the module. `record()` must resolve through TypeChecker symbols to the exclusive write/file-fsync/close/rename path; a dead atomic helper beside active append/write is an explicit rejection fixture. Supported platforms must fsync the parent directory. Windows must explicitly verify same-directory rename and record its unsupported-directory-fsync fallback; omission is not accepted.
- Structured error enforcement now covers value provenance, not merely keys. Each safe field must derive from its identically named error diagnostic under the exact frozen normalization; `verified` is an exact-true comparison and `fullSnapshotFallback` is literal false. Constant and cross-field substitutions are adversarial fixtures.
- Runtime trust coverage independently corrupts the committed file ACL, parent-directory ACL, and observed owner identity. Windows owner inspection requires one closure-bound, fixed non-interactive Get-Acl Owner command in the existing sole system command adapter; no caller script/path/command is accepted. Coding authorization remains exactly eight production paths.
- A deterministic digest is integrity evidence only. It cannot establish that explicit readiness verification occurred. WP-P3 therefore adopts the canonical-authority trust assumption explicitly: same OS user plus local workspace. Malicious code under that same identity can also rewrite canonical JSON and is outside this boundary; other identities, permissive ACL/mode, links/reparse points, malformed/extra/tampered/foreign records, and interrupted writes fail closed.
- Authorization is created only by the guarded `verifyReadiness()` path after exact WP-P2 `verified === true`, and the record carries `authorizationOperation: verifyReadiness`. Record uses exclusive same-directory temporary creation, flush/close, ownership/ACL verification, and atomic rename. Query never treats file presence or an orphan temporary file as authorization.
- Windows Node filesystem metadata cannot inspect ACLs. The narrow compatible extension is a separate `createReadinessAttestationMetadataAdapter({ repositoryRoot })` in the existing sole `child_process` boundary. It closes over exact `.argo/temp` directory/file paths and exposes only zero-argument `whoami`/`icacls` reads; the accepted four-capability credential adapter is unchanged. This evidence requires expanding Coding scope from seven to eight files.
- Frozen runtime now covers malformed/extra/tampered/presence-only/foreign records, symlink/reparse points, permissive ACL/mode, both interrupted replacement outcomes, canonical-byte drift, init/backfill/actual MCP mutation invalidation, and independent canonical/content/index/channel drift. CLI/MCP errors have exactly eleven safe fields and exclude raw message, stack, secrets, and extras.

# Implementation Design Memory

## 2026-07-24 compatible query boundary

- Accepted stable decomposition: unified MCP gateway delegates to a deep intent-query runtime module; canonical JSON remains authoritative; tests depend inward through a business-readable Harness.
- Public contract: `getSystemArchitecture` keeps its no-argument complete canonical response and accepts an optional nested `query` with explicit `purpose`, `intent`, and audit `subject`.
- Physicalized four shared explicit entrypoints for eight mounted testcases covering DT-01, DT-02, DT-03, and DT-12.
- DT-01 freezes the exact `{ status, graphPath, document }` envelope and absence of query metadata.
- The shared Harness is frozen in the coding handoff, and the explicit-entrypoint correctness guard protects its assertions and probe implementation.
- Second audit correction replaces response-derived retrieval telemetry with a frozen Harness-owned injected spy. Positive-control semantic requests prove probe wiring; DT-03 invalid requests and DT-12 graph-tidy must not increment that independent boundary count.
- DT-03 now freezes the full validation matrix and stable categories: `QUERY_PURPOSE_REQUIRED`, `QUERY_PURPOSE_INVALID`, `QUERY_INTENT_REQUIRED`, and `AUDIT_SUBJECT_REQUIRED`, while preserving all five legal purposes.
- Four critical guardrails passed: architecture boundary, dependency direction, explicit entrypoint correctness, and key implementation traceability.
- IntentionDesign correction commits `f59c383db8a935721be54d67044ce05aea56e3bc` and `33798f749fa43aebb227e09999a7ed61733b394b` mapped all 24 mounted testcases to executable paths without changing semantics.
- Physicalized the remaining 14 unique out-of-scope paths as frozen design evidence because implementation handoff validation globally requires every mounted path to exist.
- Refreshed design-test baseline: 24 total, 5 passed, 19 expected failures, 0 invalid criteria, and 0 missing paths. DT-01/02 remain green; DT-03/12 remain the only failures targeted by this compatible-query handoff.
- The other 16 failures are explicitly out of scope and remain frozen evidence for later implementation slices; Coding/Repair must not broaden this handoff to address them.

## 2026-07-25 production Graph RAG W2 boundary

- Accepted stable decomposition: `.argo/scripts/graph-rag/` owns one Node.js production composition boundary with inward external-configuration, embedding-qualification, Neo4j-native retrieval, and canonical-projection-authority modules.
- Approved intent mappings are direct: TS-01 → `grag-production-runtime`, TS-01-Native → `grag-native-retrieval-service`, TS-06 → `grag-embedding-qualification`, TS-07 → `grag-credential-boundary`, and canonical snapshot/authority → `grag-canonical-graph`.
- Physicalized six approved executable paths: five new expected-failure entrypoints plus the already-passing canonical full-snapshot entrypoint. All use business-readable Harness abstractions and freeze missing qualification, missing configuration, implicit defaults, and projection conflict as release-blocking behavior.
- Physicalized TS-08 and TS-09 as frozen expected-failure evidence only because global handoff validation requires every mounted acceptance path to exist. They are not Coding targets in this handoff.
- Added four production Graph RAG critical guards for architecture boundary, dependency direction, explicit entrypoint correctness, and key implementation traceability; all pass before Coding.
- Existing code reality risk: `.argo/scripts/neo4j-system-architecture-store.js` contains hardcoded Neo4j URI/username/password defaults. Coding must remove these through the external configuration boundary; Implementation Design does not edit production behavior.
- No intent mismatch was found, so no `ImplementationToIntentTraceProposal.json` is required.

## 2026-07-25 testcase audit correction

- Removed the runtime-composition masking defect: C1 external configuration, C2 embedding qualification, C3 canonical authority, and C4 Neo4j-native retrieval now target independently callable public boundaries and fail with four distinct missing-boundary signals.
- TS-06 freezes isolated provider, model identity, version, and dimensions omissions plus unapproved and implicit-default cases.
- TS-07 freezes four isolated credential/configuration omissions, credential-free startup and semantic query, hardcoded/default source scanning, and Cypher credential-boundary scanning.
- TS-01-Native freezes a Harness-owned query probe, exact request propagation, and exactly one invocation so a hardcoded `neo4j-native` label cannot satisfy acceptance.
- Added `coding-scope-authorization.guard.js`: TS-08/TS-09 remain mounted and frozen, but their testcase names and adapter/lifecycle implementation cannot appear in codingTargets, task targets/steps, or completion conditions.

## 2026-07-25 second testcase audit correction

- TS-06 now proves exact boolean approval, rejects undefined/string/object/array/number approval impostors, rejects undefined and whitespace identity fields, and freezes a no-coercion positive-integer dimensions matrix.
- TS-07 replaced keyword-distance scanning with a structured executable policy that detects credential literals, logical/nullish/ternary fallback expressions, tainted variable propagation, and credential-bearing Cypher execution arguments.
- `credential-source-policy.guard.js` self-tests direct-literal, fallback, ternary, direct-parameter, transitive-variable, and variableized query/parameter bypass fixtures plus a safe fixture.
- TS-01-Native now generates an unpredictable runtime sentinel and complete multi-field result inside the injected probe, then requires exact deep propagation after one call.
- Coding scope authorization now examines `taskExecutionPlan.executionStrategy` and any equivalent top-level strategy/completion/authorization fields in addition to coding targets and task details.

## 2026-07-25 scoped delivery attribution correction

- The Implementation Design defect was requiring five global delivered anchors as this slice's completion condition. No intent graph mutation or trace proposal is required for a handoff-level scoped completion rule.
- Revised completion: six approved scoped explicit entrypoints, seven frozen critical guards, zero baseline delivered regression, and passing TS-07 constitute slice completion. Global credential delivery remains runner-owned.
- Scoped attribution relies only on committed mounted TS-07 evidence, handoff scope, runner failure records, and committed runner-owned delivery evidence; uncommitted parallel intent relationships are not evidence.
- Added a frozen critical traceability guard for this attribution and a W2-C7 resume task that authorizes Coding only to verify and commit the already implemented C1-C6 files.
- Because the handoff completion semantics and execution plan changed, global human reapproval is required before Coding resumes.
- Clean-checkout validation showed TS-09 is not mounted in the committed source intent graph. The later `ea32117f` intent handoff explicitly excludes it, so stale TS-09 runner evidence is no longer carried in the handoff; the legacy physical path remains frozen and unauthorized.

## 2026-07-25 approved live provider E2E extension

- Approved profile: provider `alibaba-cloud-model-studio-openai-compatible-cn-beijing`, Beijing compatible-mode endpoint, model `qwen3.7-text-embedding`, qualification label `qualification-2026-07-25` with alias drift acknowledged, and explicit dimensions `1024`.
- Added two independent frozen entrypoints for mounted TS-06-Provider-E2E and TS-07-Provider-Secret-Isolation plus a dedicated frozen Harness. Default execution fails with explicit opt-in categories before production loading, secret access, network, or Neo4j.
- Added three frozen critical guards: exact provider/.env/Neo4j contract, opt-in and no-fake substitution, and synthetic secret artifact scanning. The expanded slice therefore has eight scoped explicit entrypoints and ten critical guards while preserving the original six/seven checkpoint.
- `.argo/.env.example` is the only committed example and contains empty approved-key placeholders. The unique ignored/untracked `.argo/.env` or direct process variables may supply approved configuration after preflight.
- Controlled Neo4j evidence uses `ARGO_NEO4J_DATABASE_URL`, `ARGO_NEO4J_DATABASE_USERNAME`, and `ARGO_NEO4J_DATABASE_PASSWORD`, a disposable run marker, one success write, cleanup, and zero-write observations for provider/configuration/vector failures.
- C1-C6 remain uncommitted Coding-owned worktree state. W2-C7 checkpoints them unchanged before W2-C8 configuration, W2-C9 provider/index gate, and W2-C10 protected live verification.
- TS-09 remains outside intent and Coding scope. No intent graph change or trace proposal is required.
- Full pre-coding runner baseline: 34 total, 14 passed, 20 failed. Both live entrypoints honestly fail with their explicit opt-in categories. The newly mounted live TS-06 evidence changed runner-owned `grag-embedding-qualification` from delivered to not_delivered; this is the expected new acceptance baseline, not a Coding regression. No other delivery status changed.

## 2026-07-25 live E2E evidence hardening

- Replaced implementation-reported `liveProviderCall` evidence with a frozen Harness-owned transport wrapper. It records actual request count, origin/path, method, explicit model/dimensions, dynamic input, protected-header presence, and the raw response vector; gate output and persisted vector must match that observation.
- Removed the production `executeFailureScenario` contract. Success and every invalid/error injection now use `createLiveEmbeddingIndexGate(dependencies).executeApprovedEmbedding(input)`.
- Controlled Neo4j evidence now includes approved provider/model/qualification/dimensions, complete vector, and dynamic canonical/content/index identities and versions. Both success and failure cleanup must prove zero remaining records.
- The frozen opt-in guard executes both live entries with explicit opt-in and no `QWEN_KEY`, requiring the safe `QWEN_KEY_REQUIRED` category before production loading, transport, or Neo4j.
- The frozen secret guard self-tests source rejection for dotenv, `.env`, and configuration-object `QWEN_KEY` loading without reading a secret value. Redaction uses an ephemeral canary and covers errors, stdout, stderr, logs, latest failure records, snapshots, and recursive artifacts.
- `preCodingBaseline` is explicit in the handoff: 34 total / 14 passed / 20 failed, with only `grag-embedding-qualification` delivered-to-not_delivered accepted as the mounted-live pre-coding transition.
- Loader enforcement now uses provenance propagation rather than keyword co-occurrence. Named safe fixtures allow parsed file configuration for exactly five non-sensitive fields plus direct `process.env.QWEN_KEY`; bypass fixtures reject dotenv process mutation, parsed/file/JSON/YAML/config sources, aliases, fallbacks, destructuring, computed access, indirect secret assignments, and non-allowlisted file fields.
- Redaction now has both a provider-error probe and a synthetic-success probe. A recording in-memory boundary captures complete Cypher/parameter and graph-evidence values, demonstrates canary detection in neutral fields, clears all records, and verifies zero post-cleanup persistence or generated artifacts.
- Intent commit `76a54e9` approves exactly two secret keys, `QWEN_KEY` and `ARGO_NEO4J_DATABASE_PASSWORD`, from direct process variables or exact `.argo/.env`. Process wins; differing duplicates, unsafe path/git/file/reparse/ACL state, missing/blank/duplicate/unknown keys, and CLI/literal/default/fallback/alias/indirect sources fail before side effects.
- Coding reported that metadata-only preflight now passes after `.argo/.env` ACLs were tightened to Administrator/Administrators/SYSTEM. No value was read; the file remains ignored, untracked, absent from HEAD, and excluded from commits.
- Source fixtures no longer contain a guard-owned resolver. They create temporary synthetic files and call the future production `resolveApprovedLiveConfiguration()` with injected filesystem/git/ACL adapters, asserting attribution, precedence, exact categories, and zero side effects. Before Coding adds that export the matrix honestly reports `LIVE_PROVIDER_CONFIGURATION_BOUNDARY_MISSING`.
- ACL cases are principal-bound `icacls` ACE matrices for current allow/deny, inherited broad allow, broad deny-only, and unverifiable output. A recording Neo4j adapter separately proves the database-password canary reaches only `neo4j.auth.basic` and never Cypher.
- Accepted source fixtures now inject the complete nine-key approved configuration and compare the complete normalized result plus per-key attribution. Provenance labels were removed from the resolver API; injected source adapters record source kind/path/key/read operation/alias chain and the production resolver must decide from those traces.
- Frozen fixtures now provide only raw operation behaviors. The production configuration module wraps those behaviors and internally issues immutable `{ value, trace }` process and `{ key, value, trace }` file records; identical direct/CLI/literal/fallback/alias/indirect values remain distinguishable without fixture-authored traces or trust verdicts.
- Trust ownership moved inside `liveEmbeddingProviderConfig.js`: production resolution creates its adapter internally, while frozen tests supply only raw operation behaviors to a module-owned callback composition root. Private adapter/capability identity never leaves that closure; caller-provided, cloned, or self-issued-true adapters fail `SOURCE_ADAPTER_UNTRUSTED`, and trusted key/path/missing/extra/type mismatch traces fail `SOURCE_TRACE_INVALID`.
- The auth canary no longer calls a Harness-private index boundary. It requires the future production `createApprovedNeo4jBoundary({ configuration, neo4j, logger })`, with the frozen recording adapter observing auth, driver, and query calls.
- Auth redaction now captures raw exceptions before safe classification and serializes message, stack, nested causes, and aggregate errors. The frozen probe scans raw/sanitized errors, injected logger events, stdout/stderr, auth/driver/query recordings, graph/persistence observations, and recursive artifacts; a channel self-test proves every channel detects a canary while the real probe requires zero leaks and persistence.
- Artifact serialization is total over undefined, null, primitives, Error, AggregateError, objects, buffers, and cycles: it always returns a string, treats undefined/null as empty, and preserves every value-bearing canary. A frozen accepted-source regression specifically protects `rawError: undefined` from interrupting the matrix.
- System metadata is a dedicated inward boundary rather than a Python dependency: only `systemMetadataCommandAdapter.js` may import Node `child_process`, and it exposes four zero-argument capabilities over fixed `git check-ignore`, `git ls-files`, `whoami`, and closure-bound `icacls` templates. The guard owns no validator; safe/bypass fixtures use a callback-scoped production test composition and must traverse the adapter's same private policy, with every rejected mutation producing `SYSTEM_METADATA_COMMAND_PROHIBITED` and zero executor calls.
- Test executor authority is scope-bound: composition returns `undefined`; its frozen null-prototype adapter reflects exactly four immutable capabilities and no symbols/backdoors; `finally` revokes escaped adapters after normal, throwing, or async callbacks. Nested/repeated roots allocate unique adapter/function identities, and every post-scope call fails `TEST_SYSTEM_METADATA_ADAPTER_REVOKED` without executor activity.

## 2026-07-25 W3 index lifecycle and exact-threshold baseline

- Materialized the W3 ImplementationToCoding handoff from validated intent commit `b3cbbe786b473824367f4a84a05c83e8de9ac1a3`, scoped to `grag-seed-retrieval`, `grag-semantic-index`, `grag-index-lifecycle`, and `grag-alignment-constraint`.
- Root and local contracts now state that W3 completion requires DT-05 threshold-all correctness before ANN performance comparison, DT-16 all-mutation lifecycle/version evidence, DT-16 semantic-index evidence, and DT-17 unaligned pure semantic rejection with canonical-read continuity.
- Frozen explicit entrypoints are `runIndependentSemanticSeeds.js` for DT-05, `runMutationIndexLifecycle.js` for DT-16 and DT-16-SemanticIndex, and `runStaleSemanticQueryRejection.js` for DT-17. Direct execution classified expected failures as `DT04_ELEMENT_SEED_CHANNEL_MISSING; DT05_THRESHOLD_ALL_EVIDENCE_MISSING`, `DT16_MUTATION_CLASSES_MISSING`, and `DT17_ALIGNMENT_ERROR_MISSING`.
- Four W3 critical guards passed before handoff: production boundary, dependency direction, explicit entrypoint correctness, and implementation traceability.
- Full pre-coding runner baseline refreshed through `argo.runArchitectureTests`: 35 total, 14 passed, 21 failed, 0 missing criteria. Runner-owned delivery status changed `grag-semantic-index` from absent to `not_delivered`, `grag-native-retrieval-service` from `delivered` to `not_delivered`, and `grag-embedding-qualification` from `delivered` to `not_delivered`; these are committed as the baseline for Coding/Repair to compare against rather than manually edited.
- No intent mismatch was found, so no `ImplementationToIntentTraceProposal.json` is required.

## 2026-07-25 corrected W3 TS-09 ImplementationDesign handoff

- Regenerated `.argo/temp/ImplementationToCodingHandoff.json` from corrected intent commit `57a99e4f13a811343d55ac15a3f12e58749cee86`, adding `grag-embedding-provider-adapter` and `grag-embedding-generation` to the W3 implementation scope.
- DT-05, DT-16, DT-16-SemanticIndex, and DT-17 are now recorded as scoped-passed evidence only. W3 remains blocked until `runEmbeddingProviderAdapterLifecycle.js` passes both `ExplicitAcceptanceTestcase-TS-09-EmbeddingProviderAdapter` and `ExplicitAcceptanceTestcase-TS-09-EmbeddingGeneration`.
- Contracts and guards were corrected so TS-09 and `generateAffectedEmbeddings()` are authorized Coding targets for corrected W3 handoffs; TS-08 remains out of scope.
- Direct entrypoint classification before handoff: `runIndependentSemanticSeeds.js`, `runMutationIndexLifecycle.js`, and `runStaleSemanticQueryRejection.js` passed; `runEmbeddingProviderAdapterLifecycle.js` failed with `TS09_NODE_ADAPTER_REQUIRED` because `generateAffectedEmbeddings()` does not expose outcome `runtime: "nodejs"`.
- Corrected W3 critical guards passed: production boundary, coding-scope authorization, dependency direction, explicit entrypoint correctness, and implementation traceability. Handoff schema validation passed for `implementation-to-coding`.

## 2026-07-25 W3.1 mutation-driven live vector integration

- Materialized W3.1 from intent commit `e318f9e9b480864d7509a1cb23b20080a81c714e`, scoped to `grag-wp-3-1` plus the existing lifecycle, embedding generation/provider, semantic index, alignment, native retrieval, qualification, and credential anchors.
- Root and local contracts now require `applySystemArchitectureMutation` as the canonical mutation control point, exact touched Element/ArchitectureRelationship/View extraction, real approved Qwen `qwen3.7-text-embedding` generation at 1024 dimensions, Neo4j vector-index queryability, and Aligned only after the queryability proof. Offline/fake evidence cannot count as live W3.1 acceptance.
- Physicalized `tests/explicit/entries/runApplyMutationEmbeddingVectorE2E.js` and added `runApplyMutationEmbeddingVectorE2E()` to the live Harness. Default execution intentionally fails before secrets or side effects with `W31_MUTATION_VECTOR_E2E_OPT_IN_REQUIRED`; protected live execution additionally requires `ARGO_LIVE_PROVIDER_E2E=1`, `ARGO_W31_LIVE_MUTATION_VECTOR_E2E=1`, approved `QWEN_KEY`, approved `ARGO_NEO4J_DATABASE_PASSWORD`, and controlled Neo4j vector cleanup.
- W3.1 critical guards passed: architecture boundary, dependency direction, explicit entrypoint correctness, implementation traceability, coding-scope authorization, live-provider contract, live opt-in/no-fake, and live secret isolation.
- Full pre-coding runner baseline refreshed through `argo.runArchitectureTests`: 37 total, 21 passed, 16 failed or missing, 0 missing acceptance criteria. Runner-owned delivery status changed 12 elements to `not_delivered`, including new `grag-wp-3-1`; this is committed baseline evidence for Coding/Repair rather than a manual delivery-status edit.
- No intent mismatch was found, so no `ImplementationToIntentTraceProposal.json` is required.

## 2026-07-25 corrected W3.1 automatic mutation-triggered lifecycle

- Corrected Implementation Design from intent commit `d29c63a228c8a491100a248a49a536880e6b82c8`, replacing the previous manual post-mutation lifecycle acceptance with a single-call mutation-write acceptance boundary.
- The W3.1 explicit entrypoint now requires exactly one `applySystemArchitectureMutation` call, response fields `embeddingLifecycle` and `alignment`, actual `touchedElementIds`/`touchedRelationshipIds`/`touchedViewIds` as the lifecycle driver, and explicit rejection of Harness-created lifecycle execution or `expectedTouchedRecords` substitution.
- Contracts and guards now protect the automatic mutation-triggered boundary while preserving live safety: explicit opt-in, approved Qwen `qwen3.7-text-embedding` at 1024 dimensions, approved `QWEN_KEY` and `ARGO_NEO4J_DATABASE_PASSWORD` sources, protected Neo4j vector evidence, no fake/offline evidence, and no credential leakage.
- Coding/Repair is authorized to update `.argo/scripts/systemarchitecture-mcp-server.js`, `.argo/scripts/graph-rag/mutationEmbeddingVectorLifecycle.js`, `.argo/scripts/graph-rag/liveEmbeddingIndexGate.js`, `.argo/scripts/graph-rag/liveEmbeddingProviderClient.js`, `.argo/scripts/graph-rag/liveEmbeddingNeo4jBoundary.js`, and `.argo/scripts/graph-rag/productionGraphRagRuntime.js` only as needed to satisfy the corrected automatic response contract.
- No intent mismatch was found beyond the corrected handoff; no `ImplementationToIntentTraceProposal.json` is required.

## 2026-07-25 W4 independent semantic seed retrieval

- Materialized the W4 ImplementationToCoding handoff from the validated `IntentToImplementationHandoff.json`, scoped only to `grag-seed-retrieval`, `ExplicitAcceptanceTestcase-DT-04`, and `ExplicitAcceptanceTestcase-DT-05`.
- Contracts now state that W4 owns only relevance-discovery seeds: separate Element, ArchitectureRelationship, and View channels; independent channel thresholds; every qualifying candidate returned; zero-result outcomes valid; ANN performance-only; no graph closure, traversal expansion, neighborhood closure, downstream graph completion, or global cross-channel ranking.
- Tightened `runIndependentSemanticSeeds.js` with no-closure assertions, independent threshold assertions, and exact returned-versus-qualifying peer equality while preserving Harness abstraction and GIVEN/WHEN/THEN readability.
- Direct W4 entrypoint execution passed. Critical production Graph RAG guards passed for architecture boundary, dependency direction, explicit entrypoint correctness, and implementation traceability.
- Full pre-coding runner baseline refreshed through `argo.runArchitectureTests`: 37 total, 24 passed, 13 failed or missing, 0 missing acceptance criteria. DT-04 and DT-05 are passed; remaining failures belong to DT-00, DT-06 through DT-11, DT-13 through DT-15, DT-18, DT-05-R2-DT-19, and TS-08, which are outside this W4 seed-retrieval scope.
- No intent mismatch was found, so no `ImplementationToIntentTraceProposal.json` is required.

## 2026-07-25 W5 deterministic five-purpose closure

- Materialized the W5 ImplementationToCoding handoff from the validated `IntentToImplementationHandoff.json`, scoped to `grag-purpose-closure`, `grag-intent-decision-policy`, `grag-implementation-policy`, `grag-repair-policy`, `grag-audit-policy`, and `grag-graph-tidy-policy`.
- Contracts now require mandatory closure decisions to use named parameterized Cypher policies, bound parameters, and ArchiMate relationship source/target semantics. Free-generated Cypher, Agent identity, text similarity, arbitrary traversal depth, and connected-component expansion cannot decide mandatory closure.
- Tightened DT-06 through DT-11 entrypoints with Harness-owned assertions for closurePolicy evidence, purpose-category boundaries, category-specific observations, and out-of-category exclusions. DT-12 remains the graph-tidy full-snapshot bypass and already passes.
- Direct W5 entrypoint execution classified DT-06 through DT-11 as expected failures: `DT06_PURPOSE_CLOSURE_POLICY_MISSING`, `DT08_INTENT_DECISION_POLICY_MISSING`, `DT09_IMPLEMENTATION_DESIGN_POLICY_MISSING`, `DT10_CODING_REPAIR_POLICY_MISSING`, and `DT11_AUDIT_PROOF_POLICY_MISSING`; DT-07 is blocked by the shared DT-06 policy-missing signal until policy evidence exists.
- W5 critical intent-query guards passed after handoff materialization: architecture boundary, dependency direction, explicit entrypoint correctness, and implementation traceability.
- Full pre-coding runner baseline refreshed through `argo.runArchitectureTests`: 37 total, 24 passed, 13 failed or missing, 0 missing acceptance criteria. W5 DT-06 through DT-11 fail with policy-missing signals, DT-12 passes, and failure records are in `design/KG/test-failure-records.json`.
- No intent mismatch was found, so no new `ImplementationToIntentTraceProposal.json` is required.

## 2026-07-25 W6 structural closure and explainable results

- Materialized the W6 ImplementationToCoding handoff from the validated and approved `IntentToImplementationHandoff.json`, scoped to `grag-coherent-context`, `grag-endpoint-closure`, `grag-view-closure`, and `grag-provenance`.
- Contracts now require W6 as structural result completion after W5 purpose-policy range selection: semantic responses expose governing canonical version evidence, relationships include same-version endpoint Elements or structural errors, matched Views are complete and non-cascading, and every returned object has one ordered first-inclusion reason plus policy/index/version evidence.
- Tightened DT-00-W6, DT-13, DT-14, and DT-15 entrypoints with Harness-owned assertions and preserved GIVEN/WHEN/THEN readability. Direct execution classified expected failures as `DT00_CANONICAL_VERSION_MISSING`, `DT13_ENDPOINT_CLOSURE_MISSING`, `DT14_VIEW_CLOSURE_MISSING`, and `DT15_PROVENANCE_EVIDENCE_MISSING`.
- W6 critical intent-query guards passed after handoff materialization: architecture boundary, dependency direction, explicit entrypoint correctness, and implementation traceability. The shared guard also freezes earlier W1/W5 intent-query entrypoints because it protects the broader intent-query acceptance boundary.
- Full pre-coding runner baseline refreshed through `argo.runArchitectureTests`: 38 total, 30 passed, 8 failed or missing, 0 missing acceptance criteria. W6 fails with `DT00_CANONICAL_VERSION_MISSING`, `DT13_ENDPOINT_CLOSURE_MISSING`, `DT14_VIEW_CLOSURE_MISSING`, and `DT15_PROVENANCE_EVIDENCE_MISSING`; out-of-scope DT-18, DT-19, and TS-08 remain failed. Runner-owned delivery status changed `grag-coherent-context` from absent to `not_delivered`.
- Coding/Repair is authorized to update `.argo/scripts/systemarchitecture-mcp-server.js` and `.argo/scripts/graph-rag/productionGraphRagRuntime.js` only as needed to satisfy W6. W4 seed retrieval, W5 purpose-policy selection, graph-tidy full-snapshot bypass, W7 quality scoring, and deliveryStatus remain out of scope.
- No intent mismatch was found, so no `ImplementationToIntentTraceProposal.json` is required.

## 2026-07-26 W6 testcase design repair

- Repaired the W6 Implementation Design artifacts after IntentDesign audit found weak assertions. No intent graph mutation was required; the defects were in Harness assertions, explicit fixture expectations, handoff text, and contract wording.
- DT-00-W6 now compares semantic canonicalVersion to the governing legacy graph version, using graph version when present and a Harness-defined canonical fingerprint otherwise; mismatches fail `DT00_CANONICAL_VERSION_MISMATCH`.
- DT-13 now requires a non-empty endpointClosure relationship set, source/target ids, endpoint object id matching, and same-version evidence tied to the governing result version.
- DT-14 now requires exact included element ids to match member object ids, exact included relationship ids to match relationship objects, non-empty in-View relationship fixtures, complete endpoints, and parent viewpoint evidence when expected.
- DT-15 now carries explicit duplicate-path fixtures and asserts ordered first-reason selection, supplementary non-overwrite, policy id, policy parameters/anchors, canonical/content/index versions, and alignment state.

## 2026-07-26 W6 DT-14 non-cascade repair

- Repaired the remaining DT-14 loophole after human review: a false `overlappingViewCascade` flag is no longer sufficient evidence.
- `runCompleteViewClosure.js` now names target View `grag-integrity-explainability` and overlapping Views `grag-quality-capacity` and `grag-vertical-chain-b`, with no independently matched or explicitly requested overlaps in this fixture.
- `assertCompleteViewClosure()` now requires target View presence and fails `DT14_OVERLAPPING_VIEW_RETURNED` if either overlapping View id appears without explicit allowance.

## 2026-07-26 W7 Phase 1 business acceptance

- Materialized the W7 ImplementationToCoding handoff from validated intent commit `fa71dc060d424508d283f3c58c8a2001ba38c223`, scoped to `grag-quality-gate` and `grag-seven-wave-delivery` with DT-00 retained as coherent-reading context.
- Contracts now require W7 to evaluate the approved five-purpose business benchmark after W2-W6: key seed recall 100%, closure correctness 100%, unrelated queries zero forced hits, recorded precision, and no invented precision release threshold.
- Tightened `runRetrievalQualityBenchmark.js` to require `evaluatePhase1QualityBenchmark(request)` evidence with benchmark id, per-purpose mandatory/recalled/missing seed observations, closure correctness, unrelated forced hits, per-purpose precision, and aggregate precision.
- Tightened `runSevenWaveDeliveryGates.js` to require `evaluateDeliverySequence(request)` evidence that blocks incomplete W2-W6 prerequisites, blocks whole delivery when DT-18 is missing or failing, and allows delivery only after W2-W6 plus DT-18 pass.
- Direct entrypoint classification before handoff: DT-18 fails with `DT18_PHASE1_QUALITY_BENCHMARK_BOUNDARY_MISSING`; TS-08 fails with `TS08_DELIVERY_SEQUENCE_BOUNDARY_MISSING`.
- W7 critical guards passed: production Graph RAG architecture boundary, dependency direction, explicit entrypoint correctness, implementation traceability, and coding-scope authorization.
- Full pre-coding runner baseline refreshed through `argo.runArchitectureTests`: 38 total, 35 passed, 3 failed, 0 missing criteria. W7 DT-18 fails with `DT18_PHASE1_QUALITY_BENCHMARK_BOUNDARY_MISSING`; TS-08 fails with `TS08_DELIVERY_SEQUENCE_BOUNDARY_MISSING`; DT-19 remains failed with `DT19_CAPACITY_EVIDENCE_MISSING` and is outside this W7 Phase 1 implementation scope.
- `argo.validateStageHandoff(stage=implementation-to-coding)` passed for `.argo/temp/ImplementationToCodingHandoff.json`.
- No intent mismatch was found, so no `ImplementationToIntentTraceProposal.json` is required.

## 2026-07-26 W7 negative-boundary repair

- Human review found an ImplementationDesign gap: the approved W7 entrypoints did not prove that DT-18 rejects fabricated quality evidence or that TS-08 rejects invalid aggregate precision. No intent graph mutation is required; this is a handoff/test-contract repair.
- DT-18 now freezes negative boundaries for missing actual recall observations, missing actual closure observations, empty benchmarks, incomplete five-purpose benchmark content, and precision outside `[0, 1]`. Expected seed ids and expected closure ids cannot be substituted as observed evidence.
- TS-08 now freezes the whole-delivery counterexample where W2-W6 are accepted and `qualityBenchmark.aggregatePrecision` is `-99`; delivery must remain blocked with the W7 quality gate.
- The repair handoff authorizes Coding only to update `.argo/scripts/graph-rag/productionGraphRagRuntime.js` for these W7 DT-18/TS-08 loopholes. DT-19 capacity evidence, test/Harness files, contracts, and runner-owned `deliveryStatus` remain frozen and out of scope.
- Direct RED evidence before Coding: `runRetrievalQualityBenchmark.js` fails with `DT18_ACTUAL_RECALL_EVIDENCE_MISSING`; `runSevenWaveDeliveryGates.js` fails with `TS08_PRECISION_OUT_OF_RANGE_ACCEPTED`.
- `argo.validateStageHandoff(stage=implementation-to-coding)` passed. Full pre-coding runner baseline refreshed through `argo.runArchitectureTests`: 38 total, 35 passed, 3 failed, 0 missing criteria. Failures are DT-18 (`DT18_ACTUAL_RECALL_EVIDENCE_MISSING`), TS-08 (`TS08_PRECISION_OUT_OF_RANGE_ACCEPTED`), and out-of-scope DT-19 (`DT19_CAPACITY_EVIDENCE_MISSING`). Runner-owned delivery status changed `grag-quality-gate` and `grag-seven-wave-delivery` from `delivered` to `not_delivered` as the new pre-coding baseline.

## 2026-07-26 W7 DT-18 evidence completeness repair

- Human review found a second ImplementationDesign gap in DT-18: purpose fixtures with empty `mandatoryKeySeedIds` or `expectedClosureIds` could pass through `ratio(0, 0)` and empty `.every()`, and missing/negative `unrelatedForcedHits` could be normalized to zero. No intent graph mutation is required; this is a test-contract and handoff repair.
- `runRetrievalQualityBenchmark.js` now freezes negative categories `DT18_MANDATORY_KEY_SEEDS_MISSING`, `DT18_EXPECTED_CLOSURE_EVIDENCE_MISSING`, `DT18_UNRELATED_FORCED_HITS_EVIDENCE_MISSING`, and `DT18_UNRELATED_FORCED_HITS_NEGATIVE`. Explicit `unrelatedForcedHits: 0` remains valid only as recorded fixture evidence.
- The repair handoff authorizes Coding only to update `.argo/scripts/graph-rag/productionGraphRagRuntime.js` for these DT-18 evidence-completeness loopholes. TS-08 remains a frozen regression check; DT-19 capacity evidence, tests/Harnesses, contracts, and runner-owned `deliveryStatus` remain frozen and out of scope.
- Direct RED evidence before Coding: `runRetrievalQualityBenchmark.js` fails with `DT18_MANDATORY_KEY_SEEDS_MISSING`; `runSevenWaveDeliveryGates.js` remains passed.
- `argo.validateStageHandoff(stage=implementation-to-coding)` passed. Full pre-coding runner baseline refreshed through `argo.runArchitectureTests`: 38 total, 36 passed, 2 failed, 0 missing criteria. Failures are DT-18 (`DT18_MANDATORY_KEY_SEEDS_MISSING`) and out-of-scope DT-19 (`DT19_CAPACITY_EVIDENCE_MISSING`). TS-08 remains passed. Runner-owned delivery status changed `grag-quality-gate` from `delivered` to `not_delivered` as the new pre-coding baseline.

## 2026-07-26 W7 DT-18 quality-decision repair

- Human review found a third ImplementationDesign gap in DT-18: after completeness validation, quality failures were reported as evidence but did not block `status: "passed"`. Recall used recalled-id count rather than mandatory-seed intersection, so wrong same-count ids could produce aggregate recall 1 while `missingKeySeedIds` was non-empty.
- `runRetrievalQualityBenchmark.js` now freezes negative categories for wrong same-count recall ids (`DT18_KEY_SEED_RECALL_NOT_100_PERCENT`), closure correctness below 1 (`DT18_CLOSURE_CORRECTNESS_NOT_100_PERCENT`), and positive unrelated forced hits (`DT18_UNRELATED_FORCED_HITS`), while preserving all previous DT-18 completeness and precision boundaries.
- The repair handoff authorizes Coding only to update `.argo/scripts/graph-rag/productionGraphRagRuntime.js` for DT-18 quality-decision loopholes. TS-08 remains a frozen regression check; DT-19 capacity evidence, tests/Harnesses, contracts, and runner-owned `deliveryStatus` remain frozen and out of scope.
- Direct RED evidence before Coding: `runRetrievalQualityBenchmark.js` fails with `DT18_KEY_SEED_RECALL_NOT_100_PERCENT`; `runSevenWaveDeliveryGates.js` remains passed.
- `argo.validateStageHandoff(stage=implementation-to-coding)` passed. Full pre-coding runner baseline refreshed through `argo.runArchitectureTests`: 38 total, 36 passed, 2 failed, 0 missing criteria. Failures are DT-18 (`DT18_KEY_SEED_RECALL_NOT_100_PERCENT`) and out-of-scope DT-19 (`DT19_CAPACITY_EVIDENCE_MISSING`). TS-08 remains passed. Runner-owned delivery status changed `grag-quality-gate` from `delivered` to `not_delivered` as the new pre-coding baseline.

## 2026-07-26 DT-19 W7 capacity evidence RED boundary

- Materialized the DT-19 ImplementationToCoding handoff from validated intent commit `1e0de615986493092684656da7436d03899ae141`, scoped only to `grag-capacity-residual` and `ExplicitAcceptanceTestcase-DT-05-R2-DT-19`.
- Contracts already declare DT-19 as evidence-only: `evaluateCapacityEvidence(request)` must record phase-1 result cardinality and measured precision by declared purpose, and must not introduce cap, budget, pagination, truncation, continuation, top-k, token-budget, result-limit, or capacity-policy decisions.
- The handoff authorizes Coding only to update `.argo/scripts/graph-rag/productionGraphRagRuntime.js` for the missing capacity-evidence boundary. DT-18 quality scoring, TS-08 delivery sequence behavior, tests, Harnesses, contracts, and runner-owned `deliveryStatus` remain frozen.
- The coding-scope authorization guard now permits DT-19 only when the current handoff explicitly targets `runCapacityEvidence.js` and `evaluateCapacityEvidence(request)`; otherwise DT-19 remains frozen out-of-scope evidence.
- Direct RED evidence before Coding: `runCapacityEvidence.js` fails with `DT19_CAPACITY_EVIDENCE_BOUNDARY_MISSING`; the five production Graph RAG critical guards pass.
- `argo.validateStageHandoff(stage=implementation-to-coding)` passed. Full pre-coding runner baseline refreshed through `argo.runArchitectureTests`: 38 total, 37 passed, 1 failed, 0 missing criteria. The only failure is DT-19 (`DT19_CAPACITY_EVIDENCE_BOUNDARY_MISSING`); DT-18 and TS-08 now pass and remain frozen context.
- No intent mismatch was found, so no `ImplementationToIntentTraceProposal.json` is required.

## 2026-07-26 DT-19 true-chain RED correction

- Human review found an ImplementationDesign gap: the first DT-19 RED boundary let `runCapacityEvidence.js` pass through Harness-synthesized `qualityEvidence.resultCardinality` instead of chaining real DT-18 output into DT-19.
- Corrected `runCapacityEvidence.js` now runs `evaluatePhase1QualityBenchmark({ benchmark })`, passes `qualityOutcome.qualityEvidence` into `evaluateCapacityEvidence()`, and expects the current production chain to fail with `DT19_RESULT_CARDINALITY_NOT_RECORDED: intent-decision` until DT-18 preserves actual result ids/cardinality.
- `productionGraphRagHarness.evaluateCapacityEvidence()` no longer supplies synthetic quality-evidence defaults. Missing quality evidence is now a frozen negative boundary that must block with `DT19_QUALITY_EVIDENCE_REQUIRED` once Coding repairs the earlier true-chain failure.
- The corrected handoff still authorizes Coding only to update `.argo/scripts/graph-rag/productionGraphRagRuntime.js`; tests, Harnesses, contracts, guardrails, and runner-owned `deliveryStatus` remain frozen during Coding/Repair.
- `argo.validateStageHandoff(stage=implementation-to-coding)` passed. Full pre-coding runner baseline refreshed through `argo.runArchitectureTests`: 38 total, 37 passed, 1 failed, 0 missing criteria. The only failure is DT-19 (`DT19_RESULT_CARDINALITY_NOT_RECORDED: intent-decision`), and runner-owned delivery evidence changed `grag-capacity-residual` from `delivered` back to `not_delivered`.

## 2026-07-26 DT-19 explicit result-id correction

- Human review found a second DT-19 RED gap: production could fabricate `observedResultIds` from `observedClosureIds`, so capacity cardinality still did not prove real phase-1 query result evidence.
- The positive benchmark fixture now contains explicit `observedResultIds` and matching `resultCardinality`; `runCapacityEvidence.js` adds negative boundaries for missing result IDs (`DT18_RESULT_EVIDENCE_REQUIRED`) and result-cardinality mismatch (`DT18_RESULT_CARDINALITY_MISMATCH`).
- Current production correctly fails the repaired RED boundary with `DT18_MISSING_RESULT_EVIDENCE_ACCEPTED` because it accepts missing result IDs by falling back to closure IDs. Coding remains authorized only to update `.argo/scripts/graph-rag/productionGraphRagRuntime.js`.
- `argo.validateStageHandoff(stage=implementation-to-coding)` passed. Full pre-coding runner baseline refreshed through `argo.runArchitectureTests`: 38 total, 37 passed, 1 failed, 0 missing criteria. The only failure is DT-19 (`DT18_MISSING_RESULT_EVIDENCE_ACCEPTED`), and runner-owned delivery evidence changed `grag-capacity-residual` from `delivered` back to `not_delivered`.

## 2026-07-26 harness env and canonical Neo4j names

- Materialized the revised intent handoff without adding new explicit entrypoints. Existing `runProductionGraphRagRuntime.js`, `runExternalCredentialBoundary.js`, and `runLiveEmbeddingProviderSecretIsolation.js` now carry the TS-01/TS-07 extensions for approved `.argo/.env` harness loading and canonical `ARGO_NEO4J_DATABASE_*` configuration names.
- Direct RED evidence before Coding: `runProductionGraphRagRuntime.js` fails with `TS01_HARNESS_ENV_FILE_LOADER_MISSING`; `runExternalCredentialBoundary.js` fails with `TS07_CANONICAL_NEO4J_ENV_NAMES_REQUIRED:EXTERNAL_CREDENTIALS_REQUIRED`; `runLiveEmbeddingProviderSecretIsolation.js` passes and includes the legacy-Neo4j-alias-only fixture.
- The handoff authorizes Coding only to update `.argo/scripts/ensureArgoHarnessEnvironment.js`, `.argo/scripts/neo4j-system-architecture-store.js`, and `.argo/scripts/graph-rag/externalProductionConfig.js` as needed. Tests, Harnesses, contracts, live provider behavior outside legacy-alias rejection, W7/DT-19, TS-09, W3/W3.1 lifecycle, and runner-owned deliveryStatus remain frozen.
- `argo.validateStageHandoff(stage=implementation-to-coding)` passed. Full pre-coding runner baseline refreshed through `argo.runArchitectureTests`: 38 total, 36 passed, 2 failed, 0 missing criteria. Failures are TS-01 (`TS01_HARNESS_ENV_FILE_LOADER_MISSING`) and TS-07 (`TS07_CANONICAL_NEO4J_ENV_NAMES_REQUIRED:EXTERNAL_CREDENTIALS_REQUIRED`). Runner-owned delivery evidence changed 14 elements to `not_delivered`, including `grag-production-runtime` and `grag-credential-boundary`.

## 2026-07-26 harness blocked-outcome repair

- Coding surfaced an ImplementationDesign frozen-test conflict after correctly making legacy Neo4j aliases throw `UNSUPPORTED_LEGACY_NEO4J_ENV_ALIAS`: `captureNeo4jProjectionConfig()` returned `{ status, category, field }`, while frozen `runExternalCredentialBoundary.js` uses shared `assertBlocked()` and requires `{ status, error: { category, field } }`.
- Repaired the ImplementationDesign-owned Harness, not production code, by normalizing Neo4j projection blocked scenarios through the shared `blockedOutcome()` shape. Coding targets remain `.argo/scripts/ensureArgoHarnessEnvironment.js`, `.argo/scripts/neo4j-system-architecture-store.js`, and `.argo/scripts/graph-rag/externalProductionConfig.js`; tests/Harnesses remain frozen after this repair.
- Direct repair evidence with Coding's scoped source edits present: `runExternalCredentialBoundary.js` passes after the Harness shape correction.
- `argo.validateStageHandoff(stage=implementation-to-coding)` passed after the repair. Full `argo.runArchitectureTests` with Coding's uncommitted scoped source edits present passed 38 total / 38 passed / 0 failed / 0 missing criteria; this is repair validation evidence, while the original committed pre-coding baseline remains 38 total / 36 passed / 2 failed.

## 2026-07-26 WP-P1 executable-path intent blocker

- Persistent stage/session ID: `implementation-semprod-wp-p1-20260726T2055+08`.
- ImplementationDesign accepted only the five-element `semprod-wp-persistence-backfill` handoff scope and did not start WP-P2, WP-P3, or Coding/Repair.
- The SP-01 and SP-02 mounted `acceptanceCriteria` values are descriptive `Suggested: ...` strings rather than exact executable paths. The architecture runner would classify them as invalid criteria, and the implementation handoff validator requires exact graph-to-entrypoint equality plus a physical path.
- Stopped before contracts, testcase assets, `ImplementationToCodingHandoff.json`, runner baseline, or stage commit, as required when implementation design reveals an intent graph correction.
- Replaced the stale trace-proposal artifact with a focused `design/KG/ImplementationToIntentTraceProposal.json` requesting only these path corrections: SP-01 → `tests/explicit/entries/runProductionSemanticBackfill.js`; SP-02 → `tests/explicit/entries/runPersistentSemanticProjectionLifecycle.js`.
- No acceptance semantics, relationships, views, approvals, or deliveryStatus changes are proposed. No Neo4j synchronization evidence is claimed; incoming evidence remains `neo4jUri is required for start`.

## 2026-07-26 WP-P1 global validator blocker

- IntentionDesign commit `9a5899fffbe53342ebe738a85524b8870f3911d9` correctly repaired the two approved WP-P1 paths without changing semantics.
- Drafted WP-P1 contracts, the `productionSemanticPersistenceHarness`, SP-01/SP-02 RED entrypoints, four critical guards, and the coding handoff. Direct RED results are `SP01_PRODUCTION_BACKFILL_BOUNDARY_MISSING` and `SP02_PRODUCTION_PERSISTENCE_BOUNDARY_MISSING`.
- Mandatory `implementation-to-coding` validation remains blocked globally by descriptive mounted acceptanceCriteria for unapproved, out-of-scope SP-03, SP-04, and SP-05. Physicalizing those entries would violate the explicit prohibition on starting WP-P2/WP-P3.
- Replaced the applied proposal with a focused validated proposal recommending that IntentionDesign defer those three testcase mounts while preserving their elements and future acceptance semantics, unless the human explicitly authorizes a broader slice.
- IntentionDesign audit commit `7326636cab44ae23984deb8345589fead1c56584` rejected/deferred the SP-03/SP-04/SP-05 proposal because changing those mounts is outside WP-P1 authorization; no graph mutation occurred.
- Corrected only the WP-P1 local contract wording so the architecture guard recognizes `delete tombstones` and `Canonical JSON remains authority` without changing either requirement. All four WP-P1 critical guards now pass.
- Rerun RED evidence remains intentional and exact: SP-01 exits 1 with `SP01_PRODUCTION_BACKFILL_BOUNDARY_MISSING`; SP-02 exits 1 with `SP02_PRODUCTION_PERSISTENCE_BOUNDARY_MISSING`.
- IntentionDesign sequencing commit `5ed151d9f6c21f44e92778641cb379215d74769c` unmounted future SP-03/SP-04/SP-05 acceptance boundaries while preserving their semantics and ownership; SP-01/SP-02 remain mounted and approved. The rejected temporary trace proposal was removed after this canonical disposition.
- Fresh pre-handoff rerun evidence remains intentional and exact: SP-01 exits 1 with `SP01_PRODUCTION_BACKFILL_BOUNDARY_MISSING`; SP-02 exits 1 with `SP02_PRODUCTION_PERSISTENCE_BOUNDARY_MISSING`; all four WP-P1 critical guards exit 0.
- Coding/Repair remains unapproved and unstarted. Neo4j synchronization remains blocked by `neo4jUri is required for start`.

## 2026-07-26 WP-P1 testcase audit correction

- IntentionDesign audit commit `edd90aecf5ed961f5f7840833692143bf9cfb505` rejected the first WP-P1 stage commit because injected test stores/checkpoints, self-reported resume evidence, incomplete negative gates, weak no-cleanup/no-runId checks, and absent runtime/MCP composition could false-pass without a durable production path.
- Corrected the frozen Harness and explicit entries to require concrete production Neo4j projection and checkpoint factories over a deterministic recording raw driver, explicit MCP operator → runtime composition, independent provider/upsert identity replay observations, negative opt-in/version/configuration/qualification gates with zero side effects, exact four-method store reflection, and pre-persistence rejection of runId-bearing records.
- Added Coding targets for `productionSemanticNeo4jAdapter.js` and `productionSemanticCheckpointStore.js`; contracts now require those factories, `runSemanticBackfill(request)`, explicit `backfillSystemArchitectureSemanticProjection` exposure, and no structural/fake-mutation trigger.
- The handoff records the committed `40 total / 38 passed / 2 expected RED / 0 missing acceptanceCriteria` baseline and all 21 runner-owned delivery transitions, including the 19 named dependency cascades. Coding/Repair, WP-P2, WP-P3, future-wave mounts, live-E2E cleanup implementation, and manual deliveryStatus edits remain prohibited.

## 2026-07-26 WP-P1 default MCP composition correction

- Final delivery audit found that deterministic SP-01 passed only through direct `callTool` injection of `productionGraphRagRuntime`; the shipped JSON-RPC `tools/call` dispatch called `callTool` without a default composition and failed `productionGraphRagRuntime.runSemanticBackfill is required`.
- Added a frozen Harness probe that launches the actual MCP server process, submits JSON-RPC `tools/call` for `backfillSystemArchitectureSemanticProjection`, removes external Neo4j/provider secrets, supplies no runtime/dependency injection, and compares canonical JSON byte-for-byte before and after.
- SP-01 now requires the default path to construct production composition and reach `EXTERNAL_CREDENTIALS_REQUIRED` before real Neo4j/provider side effects; the exact pre-repair RED is `SP01_DEFAULT_MCP_PRODUCTION_COMPOSITION_MISSING: productionGraphRagRuntime.runSemanticBackfill is required`. Existing injected raw-driver acceptance and SP-02 assertions remain unchanged.
- The repair handoff authorizes only `.argo/scripts/systemarchitecture-mcp-server.js`; `productionGraphRagRuntime.js`, all four semantic-persistence implementation modules, live-E2E cleanup, tests, contracts, WP-P2/P3, and runner-owned delivery status are frozen.
- Corrected pre-repair runner evidence is `40 total / 39 passed / 1 expected RED / 0 missing acceptanceCriteria`; only SP-01 is recorded. Runner-owned transitions are exactly `semprod-backfill-control` and `semprod-persistent-projection-requirement` delivered-to-not_delivered; the 19 elements restored by IntentionDesign dependency correction `4e01094b56429991b32b0826968da0bea9f93b0e` remain delivered.

## 2026-07-26 WP-P2 default retrieval and readiness RED design

- Persistent stage/session ID: `implementation-semprod-wp-p2-20260726T2343+08`.
- Accepted corrected intent commits are `b242a07538f0be908f72fbeda1a7dc017154535b` and `6f0d1e04bb31c5bb23272501d6afdeb573af11f9`; the authoritative scope is exactly `semprod-default-vector-retrieval`, `semprod-readiness-requirement`, `grag-query-service`, `grag-mcp-interface`, `grag-native-retrieval-service`, `grag-seed-retrieval`, and `grag-purpose-closure`.
- Root and local contracts add one deep `defaultSemanticRetrieval.js` composition boundary: approved external query embedding, persistent fail-closed three-channel readiness, independent Neo4j Vector Index threshold-all retrieval, then accepted deterministic purpose/ArchiMate, endpoint, selected View, provenance, and canonical/version completion.
- The module-owned deterministic test composition may inject only raw provider transport and raw Neo4j driver operations below production adapters. The public `getSystemArchitecture` call receives no dependency argument, and no Harness-provided semantic retrieval boundary, preassembled result, seed list, readiness verdict, approved-test profile, default credential, or synthetic empty seed can satisfy acceptance.
- Physicalized `tests/explicit/entries/runDefaultMcpNeo4jVectorRetrieval.js` and `tests/explicit/entries/runProductionSemanticReadinessGate.js` with business-readable Harness methods and frozen no-argument/graph-tidy compatibility controls.
- Direct RED is intentional and exact: SP-03 exits 1 with `SP03_DEFAULT_VECTOR_RETRIEVAL_BOUNDARY_MISSING: create production defaultSemanticRetrieval.js`; SP-04 exits 1 with `SP04_DEFAULT_READINESS_BOUNDARY_MISSING: create production defaultSemanticRetrieval.js`. Both failures occur after syntax-valid loading and expose missing WP-P2 production behavior, not environment setup.
- Existing no-argument and graph-tidy controls pass. All four new `production-default-retrieval` critical guards and the five existing production Graph RAG guard regressions pass against the draft handoff.
- `.argo/temp/ImplementationToCodingHandoff.json` intentionally supersedes the temporary WP-P1 handoff because WP-P1 is accepted and delivered. It freezes WP-P1, SP-05/WP-P3, production cleanup, CLI/docs/release work, canonical intent semantics, and manual `deliveryStatus` edits.
- Before stage validation, full runner refresh, or commit, the complete handoff still requires explicit global human approval. Incoming `neo4jUri is required for start` remains environment evidence only and does not weaken fail-closed behavior or constitute live proof.
- Delegated IntentionDesign audit rejected the first WP-P2 RED design: the Harness injected a ready-made environment object, top-k completeness relied on result claims rather than raw window exhaustion, closure checks were shape-only, readiness evidence accepted arbitrary arrays without an exact matrix or total operation ordering, and the explicit-entrypoint guard mostly froze assertion-name strings.
- Corrected the Harness composition surface to exactly raw `sourceBehavior`, `sourceAdapters`, `transport`, and `neo4jDriver`; it now prohibits environment/configuration objects and freezes approved process plus missing, unsafe-file ACL, conflicting dual, legacy alias, test/default, and fallback credential-source cases with zero downstream side effects on rejection.
- Corrected threshold-all evidence to raw two-record initial windows with per-channel offset/window/continuation/exhaustion correlation and qualifying Element, ArchitectureRelationship, and View peers beyond the initial window.
- Corrected completion assertions to deep equality for `w5.implementation-design.v1` bound parameters, exact Realization direction, `semprod-rel-default-query-service` endpoint objects/versions, exact `semprod-wp2-default-retrieval-readiness` members/relationships with `semprod-wp2-vector-seed-closure` exclusion, semantic-seed-first ordered provenance, and canonical/content/index versions.
- Corrected readiness to eight exact fixtures: pending, partial missing View, stale canonical, failed, unknown, ArchitectureRelationship content mismatch, View index mismatch, and aligned. One ledger proves source resolution before readiness, readiness before provider, provider before vector windows, and rejected states terminate at readiness.
- The strengthened explicit-entrypoint guard imports and deep-compares a frozen raw evidence contract covering composition keys, credential cases, raw pagination fields, exact closure identifiers/order, readiness matrix, and operation ordering. Full handoff validation, architecture baseline, and commit remain intentionally unrun pending delegated re-audit.
- A second independent audit found four executable false-pass paths: approved-source success could perform zero reads, an all-zero result could perform zero vector queries, vector operations did not prove exact index/Cypher/provider-vector correlation, and selected-View/provenance checks covered only subsets.
- The corrected approved case now requires direct process reads and result attribution for all nine approved embedding-profile, Neo4j, and provider-secret keys, with every read preceding the sole readiness operation. Prohibited test/default/fallback source operations remain excluded.
- The corrected all-zero case now requires one readiness read, one embedding, and one exact independently exhausted query sequence for Element, ArchitectureRelationship, and View. Non-empty cases require expanding top-k from two through raw continuation and exhaustion.
- Raw vector evidence now freezes the exact three production index names, exact parameterized `db.index.vector.queryNodes($indexName, $topK, $vector)` Cypher, request index/channel/top-k parameters, and deep equality between the raw provider vector and every Neo4j query vector.
- View completion now checks every canonical metadata field, viewpoint binding/description, full parent viewpoint, ordered versioned member Elements, ordered versioned member Relationships and both endpoints. Provenance must contain exactly one first-inclusion record for every returned typed object.
- The explicit-entrypoint correctness guard invokes seven negative self-tests proving rejection of zero approved reads, zero vector queries, wrong index, unparameterized Cypher, mismatched provider/query vector, incomplete View metadata, and missing returned-object provenance. Scoped evidence is: both direct entries retain only their intended missing-module RED causes; all four WP-P2 guards, both snapshot compatibility controls, JavaScript syntax checks, handoff JSON parsing, and `git diff --check` pass. Validation, full baseline, and commit remain intentionally unrun.

## 2026-07-27 WP-P2 compatibility injection correction

- Coding-delivery audit found that `tests/harness/intentArchitectureQueryHarness.js` imported the unified four-argument gateway but attempted to supply a deterministic compatibility probe as if the downstream System Architecture boundary's third argument were dependencies. The override was ignored.
- The Harness now imports the public `systemarchitecture-mcp-server.js` query boundary directly and calls `callTool(name, args, dependencies)`. Its frozen correctness guard requires the direct boundary, requires the third-argument call, and rejects the ignored-fourth-argument form.
- SP-03 now includes an uninjected semantic request containing `threshold-all`, `semantic seed`, and an explicit anchor under an absent-secret source fixture. The current coding commit intentionally fails `SP03_LEGACY_CONTROL_WORDS_BYPASSED_PRODUCTION_GATE`, proving that content/anchor routing reaches the deterministic runtime instead of production credentials.
- The correction handoff narrows CodingAndReparing authorization to `.argo/scripts/systemarchitecture-mcp-server.js`: remove every content-, lifecycle-, subject-, control-word-, and anchor-based deterministic selector. Only no-argument and graph-tidy full-snapshot bypasses remain; deterministic compatibility is available only through explicit wrapper-fourth dependencies forwarded to inner argument three.
- Coding's full runner falsified the preceding injection correction: the frozen compatibility Harness traverses `.argo/scripts/argo-mcp-server.js`, whose signature is `callTool(name,args,progressToken,dependencies)`. Passing the probe at wrapper argument three treats it as a progress token, so the inner dependency remains absent.
- Corrected API contract: `intentArchitectureQueryHarness.js` uses wrapper argument four; `argo-mcp-server.js` forwards that value to `systemarchitecture-mcp-server.js` argument three. The WP-P2 production Harness remains independently uninjected and calls the inner server with only name and args, so its legacy-control-word/anchor regression still exposes the b13 routing bypass.
- Both correctness guards now freeze wrapper-fourth to inner-third forwarding and forbid confusing wrapper argument three with dependencies. No production change to `argo-mcp-server.js` is authorized.
- A second runtime audit showed that correct argument position alone was insufficient: no-probe calls still passed `undefined` at wrapper argument four, leaving sixteen legacy deterministic compatibility testcases dependent on the prohibited production text router.
- Every explicit compatibility query now injects a test-only boundary at wrapper argument four: the caller's probe when supplied, otherwise a Harness-owned boundary over the deterministic Graph RAG runtime. No-argument full snapshots remain uninjected, and the separate WP-P2 production Harness still calls the inner server with no dependencies.
- Both correctness guards behaviorally execute a no-probe purpose query and require exactly one Harness-owned boundary invocation. This counter proves wrapper forwarding and cannot pass through production content routing alone.

## 2026-07-27 WP-P2 final delivery RED correction

- Final IntentionDesign audit found two production defects after the semantic router repair: `defaultSemanticRetrieval.js` supplies `requiredOptIns: []`, which the trusted live configuration contract rejects, and graph-tidy bypass remains conditional on having no anchors.
- The source contract now freezes a proposed `production-semantic-query` use case: all nine approved values and issued direct process/protected-file traces are mandatory, neither E2E opt-in is required or attributed, and every existing test/default/legacy/fallback/conflict/file/trace rejection remains unchanged.
- SP-03 behaviorally requires realistic source resolution to proceed in order through readiness, provider, and Neo4j vector boundaries. SP-04 requires anchored graph-tidy to return the exact full snapshot with zero credential, readiness, provider, or vector operations.
- Independent audit rejected the first credential RED because the Harness manually invoked readiness/provider/vector after resolving configuration. The corrected scenario patches only raw trusted source and external transport/Neo4j boundaries, reloads the actual production modules, invokes the uninjected System Architecture MCP path, and requires production code itself to request `production-semantic-query` and emit all downstream events.
- The dependency-direction guard now deep-equals Coding authorization to exactly `liveEmbeddingProviderConfig.js`, `defaultSemanticRetrieval.js`, and `systemarchitecture-mcp-server.js`, rejects any authorized/frozen overlap, and excludes `productionGraphRagRuntime.js`.

## 2026-07-27 WP-P2 frozen compatibility conflict correction

- Coding's R3/R4 proof exposed a frozen contradiction: `runPurposePolicyClosure.js` still classified anchored graph-tidy as one of the deterministic semantic closure categories even though the accepted WP-P2 contract makes graph-tidy an unconditional full-snapshot bypass.
- Independent audit rejected delegating graph-tidy coverage to DT-12/SP-04 because DT-07 itself owns all five dispatch categories. DT-06/DT-07 now retain deterministic policy evidence for four ordinary semantic categories (`intent-decision`, `implementation-design`, `coding-repair`, and `audit`) and directly assert graph-tidy as the fifth canonical full-snapshot category with zero semantic operations and no semantic closure policy id.
- The compatibility Harness handles anchored graph-tidy as a test-owned canonical-snapshot compatibility path; the independently uninjected SP-04 production Harness remains the sole RED observation of the committed production dispatch defect.
- The intent-query correctness guard requires graph-tidy in DT-07's five-category dispatch list while excluding it from the four semantic closure-policy categories; it also freezes the zero-operation, no-policy-id, and canonical-snapshot assertions.
- A follow-up audit found that policy-id uniqueness alone could accept one missing id when the other three were distinct. DT-07 now invokes the complete parameterized closure-policy assertion for every ordinary semantic category before uniqueness, thereby requiring four non-empty policy ids while preserving graph-tidy's explicit no-policy behavior; the correctness guard freezes this assertion ordering.
- The WP-P2 handoff restores frozen, non-authorizing metadata for the existing provider E2E, secret-isolation, and W3.1 opt-in entrypoints. The exact Coding authorization remains only `liveEmbeddingProviderConfig.js`, `defaultSemanticRetrieval.js`, and `systemarchitecture-mcp-server.js`; runner-owned status and failure-record governance is unchanged.

## 2026-07-27 WP-P2 final R3 mixed-source security correction

- Final coding audit falsified the 42/42 result with a mixed-source counterexample: valid canonical production-query credentials plus any legacy `ARGO_NEO4J_URI`, `ARGO_NEO4J_USERNAME`, or `ARGO_NEO4J_PASSWORD` alias were accepted because the use-case path skipped legacy-key inspection.
- SP-03 now freezes three mixed canonical-plus-one-legacy fixtures. Production-semantic-query must directly inspect all three legacy keys for every resolution and reject `SECRET_SOURCE_PROVENANCE_PROHIBITED` when any is present; direct inspection is allowed and required, but attribution, selection, normalization, returned configuration evidence, and downstream readiness/provider/vector use are forbidden.
- Coding authorization is narrowed to `.argo/scripts/graph-rag/liveEmbeddingProviderConfig.js`. Delivered default use-case requesting, R4 graph-tidy bypass, retrieval/readiness/closure, DT-06/DT-07/DT-12, WP-P1 security, and SP-05/WP-P3 remain frozen.
- Handoff governance re-audit found stale `openGaps` still describing the superseded use-case and R4 defects plus three-file authorization. The gaps now record only the mixed-legacy SP-03 RED, explicitly mark R4 delivered, and repeat the one-file authorization. The coding-scope guard scans authorization-bearing prose and rejects any attempt to re-authorize default retrieval, System Architecture MCP, Graph RAG runtime, or unified MCP files.
- A follow-up audit found the scope guard split prose at every period, including the period in `.js`, so forbidden filenames were separated from their authorization sentence. The parser now protects `.js` suffixes while splitting sentences, and executable negative self-tests require rejection of explicit prose authorization for `defaultSemanticRetrieval.js`, `systemarchitecture-mcp-server.js`, `productionGraphRagRuntime.js`, and `argo-mcp-server.js`.

## 2026-07-27 WP-P3 operator journey RED design

- Persistent stage/session ID: `implementation-semprod-wp-p3-20260727T0241+08`.
- Consumed independently audited intent commits `022f4fab7cf4479ea525a94bb5752fa64fe57f9e` and `0ef1615e`, scoped only to `semprod-wp-operator-release`; implementation-bearing anchors are exactly `semprod-operator-journey-process`, `grag-query-service`, and `grag-credential-boundary`.
- Added one stable Production Semantic Operator Journey composition contract over accepted WP-P1/WP-P2 public ports. Coding may add only `semanticOperatorJourney.js`, its CLI/MCP/package adapters, and README guidance; persistence, checkpoint, Neo4j adapter, provider configuration, default retrieval, Graph RAG runtime, tests, contracts, canonical intent, and `deliveryStatus` remain frozen.
- Physicalized `tests/explicit/entries/runNewProjectSemanticOperatorJourney.js` with a business-readable Harness and four critical guards. Direct execution intentionally fails with `SP05_OPERATOR_JOURNEY_BOUNDARY_MISSING: create production semanticOperatorJourney.js`; all four WP-P3 guards pass.
- SP-05 freezes equivalent approved external configuration across both successful paths, no-opt-in actionable `SemanticIndexPending` and zero auto-start, explicit backfill/readiness/query commands, opted-in configuration-before-auto-start, progress/checkpoint/failure/resume evidence, readiness-before-query, missing/unsafe/unapproved zero-side-effect controls, redaction, exact no-argument snapshots, and canonical JSON byte authority.
- Accepted SP-01 through SP-04 plus snapshot, graph-tidy, external credential, provider-secret isolation, and canonical-authority regressions pass before handoff.
- Full pre-coding runner baseline is `43 total / 42 passed / 1 failed / 0 missing acceptanceCriteria`; SP-05 is the sole expected RED. Runner-owned status changed `grag-credential-boundary` and `semprod-readiness-requirement` from `delivered` to `not_delivered` and added `semprod-operator-journey-process=not_delivered`, yielding a snapshot of 34 delivered and 3 not_delivered attributes.
- `.argo/temp/ImplementationToCodingHandoff.json` explicitly supersedes the tracked WP-P2 temporary slot because WP-P3 is the authorized successor; it does not reopen accepted WP-P1/WP-P2 behavior.
- Live Neo4j remains unavailable with `neo4jUri is required for start`. Deterministic production-composition acceptance does not claim live evidence, and `semprod-ready-plateau` remains blocked as a target-only release context until protected live gates can run.

## 2026-07-27 WP-P3 independent-audit correction

- Consumed and validated IntentDesign commit `b41bdbd3c9c249f7bfcb6b45daeaa3a53511fee3`. `semprod-rel-journey-credentials` and `semprod-rel-journey-readiness` are now non-directional Associations, so SP-05 observes accepted policy but cannot propagate its RED status backward into protected WP-P1/WP-P2 delivery.
- Corrected SP-05 so the two successful fixture inputs deep-equal except for `automaticBackfillOptIn`; controlled interruption/resume is a separate recovery scenario. Both event ledgers require approved configuration validation before explicit/automatic backfill start, provider, and database effects.
- Redaction evidence now recursively captures the complete observable production Error—including message, stack, own properties, nested causes, arrays, and cycles—before checking secret and unsafe-source canaries. Harness-side field dropping can no longer create a false pass.
- Rejected missing/unsafe/unapproved opt-ins now read a post-rejection snapshot and require exact equality with the pre-rejection `{ status, graphPath, document }` envelope, canonical bytes, and zero automatic, explicit-backfill, provider, database, or canonical-mutation effects.
- Added frozen `default-composition-wiring.guard.js` for exact accepted factory/tool composition and six inward operator ports, plus `authorized-production-safety.guard.js` with executable unsafe fixtures for embedded credentials/URIs, sensitive fallbacks, implicit provider defaults, and duplicated accepted internals. WP-P3 now has six critical guards.
- Corrected runner baseline remains `43 total / 42 passed / 1 failed / 0 missing acceptanceCriteria`; SP-05 alone is the expected missing-production RED. Runner-owned topology recovery changed `grag-credential-boundary` and `semprod-readiness-requirement` from `not_delivered` back to `delivered`; `semprod-operator-journey-process` alone remains `not_delivered`, yielding 36 delivered and 1 not_delivered attributes.
- No production implementation was added. Live Neo4j remains unavailable, so deterministic composition evidence still cannot claim `semprod-ready-plateau`.

## 2026-07-27 WP-P3 structural guard correction

- Replaced bypassable source-text matching in `default-composition-wiring.guard.js` with TypeScript compiler AST inspection. The guard now proves exact CommonJS exported-symbol/module imports, real accepted factory calls, literal-name backfill/readiness/query tool calls, one operator factory call, one dependency object, and exactly six function-valued authorized ports.
- Executable wiring counterexamples prove that comments-only text, imported-but-unused identifiers, a missing factory invocation, a missing named tool invocation, and an extra dependency port all fail. The missing production operator still leaves the explicit SP-05 entrypoint RED without making the pre-coding guard fail.
- Replaced narrow provider-default regex matching with AST expression policy over assignments, aliases, object properties, logical `||`, nullish `??`, ternary defaults, and factory arguments for provider/model/credential values. A positive fixture preserves direct approved external configuration plumbing, avoiding rejection of legitimate boundary delegation.
- Corrected WP-P3-C3 to name and require all six frozen WP-P3 guards rather than the stale four-guard count.

## 2026-07-27 WP-P3 binding and default-flow correction

- Wiring evidence now creates an in-memory TypeScript Program and resolves every call through the TypeChecker. Accepted factories must resolve to their exact CommonJS BindingElement; initialization/structural callbacks must resolve to exact top-level bindings; configuration must resolve to the imported approved resolver; and backfill/readiness/query callbacks must directly return the unshadowed top-level `callTool` with exact literal tool mapping.
- Added executable correct-import-plus-shadowed-factory, shadowed-operator-factory, and shadowed-`callTool` counterexamples. Identifier text without binding identity no longer counts.
- Safety taint now treats every literal/default as a possible source regardless of spelling and propagates through generic variable/assignment alias chains. Destructuring initializers and parameter initializers are default sources; sensitive forms fail directly, while generic forms fail when they later reach provider/model/credential assignments, objects, or factory arguments.
- Approved externally supplied values remain legal only through direct member access, aliasing, or destructuring without any literal/default initializer.

## 2026-07-27 WP-P3 public readiness compatibility correction

- Coding/Repair correctly stopped because the six-port operator contract named `readSemanticReadiness`, while accepted WP-P2 exposed the persistent readiness read/evaluation only inside `defaultSemanticRetrieval.js`. Calling an outward operator tool recursively or reproducing its Neo4j query would violate dependency direction.
- The smallest stable boundary is `createDefaultSemanticRetrieval(dependencies).readReadiness()`. It is a backward-compatible method on the already-declared Default Semantic Retrieval Composition, not a raw helper export or new persistence adapter. `retrieve()` and `readReadiness()` must share one private helper over the existing credentialed composition, readiness Cypher, normalization, and alignment evaluator.
- `defaultSemanticRetrieval.js` is unfrozen only for this method/private helper extraction. A frozen seventh WP-P3 guard requires one query authority, shared retrieval/read path, the exact safe readiness envelope, unchanged module exports, and executable duplicate/bypass/private-export failures. The operator still exposes exactly six ports; default wiring maps the readiness port only to the accepted reader result.

## 2026-07-27 WP-P3 readiness provenance guard correction

- Envelope-key checks and identifier text do not prove a safe public readiness boundary. The guard now resolves the shared helper, persistent read, evaluator, evidence variables, mapping function, and alignment parameter through the TypeChecker.
- Every public property has one approved source member, `fullSnapshotFallback` is literal `false`, and read-only paths reject provider/embed/vector/retrieval references. Executable fixtures cover raw-secret and arbitrary mappings, true fallback, provider and vector effects, and a locally shadowed fake shared helper.

## 2026-07-27 WP-P3 consent and explicit-readiness repair

- Coding commit `0782694` exposed two use-case defects missed by initial acceptance: explicit backfill promoted absent caller consent to literal true, and query implicitly executed the readiness port while locally re-evaluating WP-P2 channel policy.
- Frozen SP-05 now runs direct and CLI missing-consent controls, query-before-verification controls both before and after aligned backfill, and a non-aligned diagnostics fixture. It requires no implicit read/query/provider/vector effects and exact state/version/channel/fallback propagation.
- The architecture guard rejects literal consent promotion, query-time readiness calls, local required-channel/alignment verdict code, and failure to consume WP-P2 `verified`. Repair authorization is narrowed to `semanticOperatorJourney.js`; all completed adapters and the accepted readiness reader are frozen.

## 2026-07-27 WP-P3 policy provenance hardening

- Name and literal checks were bypassable by `Boolean(1)` aliases, aliased/bracket dependency reads, renamed state/channel helpers, and cosmetic `verified` references. A dedicated TypeChecker guard now follows local variable/assignment bindings and requires exact consent and verdict provenance.
- Automatic consent is not a blanket prohibition: the positive shape is a factory-owned selector between `request.automaticBackfillOptIn === true` and unchanged `request.explicitOptIn`, reached only after the start guard and approved configuration.
- Runtime acceptance now uses contradictory records. Aligned with `verified:false` rejects with exact diagnostics; non-Aligned with `verified:true` authorizes. The operator therefore cannot infer authorization from state, versions, or channels.

## 2026-07-27 WP-P3 adapter lifecycle correction

- Production audit after `587ee41` exposed four adapter gaps: System MCP semantic requests called retrieval directly, process-local readiness could not survive separate CLI commands, the package script injected consent, and CLI/MCP catches emitted stacks while dropping readiness diagnostics.
- The lifecycle design keeps explicit verification as the sole authorization creator. It adds one repository-local attestation store beside the six function ports. The record contains only graph path/digest, verified flag, canonical/content/index versions, channel arrays, literal false fallback, schema version, and integrity digest; no configuration, provider value, credential, token, raw error, or secret is stored.
- Query without a record fails before WP-P2 readiness/provider/vector/query operations. Query with a record may invoke the accepted public WP-P2 reader once only to compare current versions/channels; this is stale-evidence validation, not implicit authorization. Missing, malformed, graph-drifted, init-invalidated, backfill-invalidated, mutation-invalidated, or readiness-drifted evidence is cleared and rejected before retrieval.
- Public System and unified semantic dispatch must enter the operator. A private raw semantic delegate supplies the final inward query port and avoids recursion. No-argument and graph-tidy paths remain exact canonical snapshot bypasses.
- Frozen SP-05 now starts real child CLI processes and inspects actual System/unified dispatch plus wire envelopes. The ninth guard has executable direct-retrieval, hidden-consent, stack-wire, and secret-checkpoint counterexamples. Coding is limited to the operator, new store/error deep modules, CLI, two MCP adapters, and package script; README was corrected and refrozen in ImplementationDesign.
- No intent trace proposal is needed: explicit readiness-before-query semantics are unchanged. The durable evidence and adapter routing are implementation boundaries required to realize that existing sequence across process lifetimes.
- Scoped regression exposed stale WP-P1/WP-P2 guards that deep-equaled the shared temporary handoff slot to historical repair scopes and even required future SP-05 absence. They now enforce stable delivered invariants: accepted persistence/retrieval/runtime files cannot be targeted, SP-01 through SP-04 entries remain mounted and frozen, and graph mappings remain present. This removes successor-handoff coupling without weakening either boundary.

## 2026-07-27 final ACL and inherited-guard correction

- Final audit found that the accepted Windows ACL parser enumerated only basic rights. The frozen policy now treats `F/M/RX/R/W/D/DE/RC/WDAC/WO/S/AS/MA/GR/GW/GE/GA/RD/WD/AD/REA/WEA/X/DC/RA/WA` as protected effective `icacls` rights. Runtime creates isolated foreign-allow and current-identity-deny cases for `WD/AD/GW/GA/WEA/GR/RD`; the guard exhaustively executes every protected right against file and parent ACLs plus compound ACEs.
- Three production Graph RAG guards incorrectly treated the single current handoff as a permanent TS-01/TS-06/TS-07/W3.1 registry. Historical acceptance is now bound to graph-mounted entrypoints, executable failure/opt-in assertions, and `tests/ARCHITECTURE.md`; only genuinely current-scope testcases must appear in the active handoff.

## 2026-07-27 canonical automatic semantic lifecycle RED design

- Consumed accepted intent commits `2f2b7da` and `1494a01` with no remaining intent or user decision gap. The stable successor keeps `semanticOperatorJourney.js` as private composition, preserves WP-P1/WP-P2 internals, retires WP-P3 public start/backfill/readiness tools, and makes canonical argo init plus sole public `getSystemArchitecture` the two outward control points.
- Root and local contracts freeze the exact existing dual gate with no third flag: both enabled plus safe external-only configuration performs idempotent/resumable durable three-channel reconciliation; both disabled is structural-only Pending/Disabled with zero provider/vector effects; either half-enabled or missing/unsafe configuration fails closed and redacted.
- Added `tests/harness/automaticSemanticLifecycleHarness.js` and refroze nine accepted graph-mounted testcase mappings across eight physical entrypoint files. Independent initial RED signals are SP05 automatic init reconciliation missing, SP01 backfill tool still public, SP02/DT16/DT16-SemanticIndex/W3.1 persistent incremental lifecycle boundary missing, SP03/SP04 query still requires retired explicit readiness, and TS-00 retired lifecycle tool still public.
- The incremental contract is one `createPersistentMutationEmbeddingLifecycle(dependencies).reconcile({ canonicalWrite, preview, gates })` deep boundary over readiness, external configuration, qualified provider, the existing four-method production projection store, touched-record queryability, and global coherence. It covers all 18 batch/focused add/update/remove combinations, preview zero effects, exact touched IDs, upsert/tombstone, complete evidence, failure state, canonical authority, and no production runId cleanup.
- Controlled W3.1 code-complete acceptance now runs before optional live gates; absent provider/Neo4j does not mask missing implementation. Live release remains separately blocked unless both exact gates, safe external credentials, real finite 1024-dimensional provider output, durable Neo4j records, vector queryability, and global coherence are available.
- Added five canonical lifecycle guards covering architecture boundary, dependency direction, explicit entrypoint correctness, implementation traceability, and safety invariants. `.argo/temp/ImplementationToCodingHandoff.json` records nine testcase mappings, five guards, ten production targets, and dependency-ordered tasks CSL-C1 through CSL-C5. `design/KG/test-failure-records.json` remains the real runner-owned expected-failure path.
- MCP discovery for the project Argo server returned server error during this stage. Local `node .argo/scripts/validateStageHandoff.js implementation-to-coding` is the repository-equivalent validator and passed; the MCP validation attempt remains an environment/tool availability blocker to report rather than a design gap.
- Final pre-coding full runner baseline is `43 total / 34 passed / 9 failed / 0 missing acceptanceCriteria`. All nine failures are the scoped RED signals above; there are zero out-of-scope failures. Runner-owned propagation changed 28 dependency-connected elements from `delivered` to `not_delivered`; those changes are preserved in `design/KG/SystemArchitecture.json` and enumerated in the handoff.

## 2026-07-28 BP MCP semantic query contract WP-1 through WP-4

- Persistent stage/session ID: `implementation-bp-mcp-semantic-query-contract-wp1-wp4-20260728T0131+08`.
- Consumed approved `.argo/temp/IntentToImplementationHandoff.json` for `grag-mode-validation`, `grag-query-service`, `grag-canonical-graph`, `grag-endpoint-closure`, and `grag-view-closure`. No upstream intent graph change is requested.
- Materialized root and local contracts for the BP MCP semantic query contract: WP-1 rejects forbidden response-shape controls, WP-2 returns canonical object subsets only, WP-3 applies minimal Relationship endpoint closure with broken-endpoint fail-closed behavior, and WP-4 applies complete non-cascading View closure with broken-reference fail-closed behavior.
- Repaired the physical testcase anchor name from stale `broken-relationship-reference-rejection` to graph-mounted `broken-relationship-endpoint-rejection`, and added JavaScript `file.js#anchor` execution support through `ARGO_TESTCASE_ANCHOR` in the default architecture test executor plus validator path normalization.
- Added four focused critical guards under `tests/architecture/mcp-semantic-query-contract/` for architecture boundary, dependency direction, explicit entrypoint correctness, and implementation traceability. All four focused guards pass, and `argo.validateStageHandoff(stage=implementation-to-coding)` passes.
- Direct anchored RED evidence: `preserve-full-snapshot-read-modes` passes; the eight scoped RED signals are request-shape controls not rejected, canonical subset payload still exposing `result`, Element-only/Relationship/View positives still exposing the business-summary envelope, no-cascade lacking canonical document, and broken Relationship/View references passing instead of fail-closing.
- Coding authorization is narrowed to `.argo/scripts/systemarchitecture-mcp-server.js`. `defaultSemanticRetrieval.js`, WP-P2/SP-03, W3.1, production Graph RAG internals, tests, contracts, handoff guardrails, and runner-owned `deliveryStatus` remain frozen unless a later approved handoff explicitly reopens them.
- Full runner baseline after the design artifacts is `52 total / 42 passed / 10 failed / 0 missing acceptanceCriteria`. The eight BP-MCP-SEM failures are scoped expected REDs; inherited out-of-scope failures remain `ExplicitAcceptanceTestcase-W3-1-MutationEmbeddingVectorE2E` and `ExplicitAcceptanceTestcase-SP-03-DefaultVectorRetrieval`.

## 2026-07-28 BP MCP semantic query no-anchor correction

- Post-delivery verification exposed an ImplementationDesign gap: the accepted BP-MCP-SEM tests proved anchor-activated and injected fixture paths, while real public ordinary `getSystemArchitecture(query)` calls without `query.anchors` still returned legacy business-summary/debug responses.
- The corrected design keeps the intent semantics unchanged and strengthens implementation contracts, `runMcpSemanticQueryContract.js`, the explicit-entrypoint correctness guard, and `.argo/temp/ImplementationToCodingHandoff.json` so WP-1/WP-2 apply to no-anchor public `callTool` and unified MCP `tools/call` paths.
- Revised expected RED scope is only `reject-response-shape-controls` and `canonical-object-subset-only` through their no-anchor public subcases. The previously delivered compatibility, Element, Relationship endpoint, broken Relationship, View membership, no-cascade, and broken View anchors remain passing frozen regression coverage. Coding remains limited to `.argo/scripts/systemarchitecture-mcp-server.js`; no intent trace proposal is needed.

## 2026-07-29 BP-AUTOALIGN implementation design

- Consumed accepted BP-AUTOALIGN intent graph scope for `bp-autoalign-write-completion`, `bp-autoalign-query-recovery`, `bp-autoalign-automation-boundary`, and `bp-autoalign-diagnostic-requirement`; no upstream intent trace proposal is required because acceptanceCriteria already point at graph-mounted implementation entry scripts.
- Revised root and local contracts to supersede direct unaligned ordinary-query failure: governed writes are business-complete only after embedding generation, durable semantic projection, touched Element/ArchitectureRelationship/View queryability, global coherence, and readiness Aligned; ordinary semantic queries must auto-align and retry once; unrecoverable alignment fails closed with `fullSnapshotFallback:false`.
- Materialized `tests/explicit/entries/runBusinessReliableSemanticLifecycle.js` plus BP assertions in `automaticSemanticLifecycleHarness.js` and `productionDefaultRetrievalHarness.js`. Graph-mounted scripts `runMutationIndexLifecycle.js`, `runProductionSemanticReadinessGate.js`, and `runTypedMcpQueryContract.js` now expose the accepted RED signals while matching intent graph acceptanceCriteria.
- Wrote `.argo/temp/ImplementationToCodingHandoff.json` and `.argo/temp/bp-autoalign-expected-failures.json`. `validateStageHandoff(stage=implementation-to-coding)` passed through both local script and MCP after graph-path alignment.
- Pre-coding full runner baseline is `61 total / 50 passed / 11 failed / 0 missing acceptanceCriteria`; BP-AUTOALIGN failures are expected REDs. Inherited out-of-scope failures remain W3.1 touched-record extraction and SP-03 approved-source attribution. Runner-owned deliveryStatus changes are preserved in `design/KG/SystemArchitecture.json`.

## 2026-07-29 BP-AUTOALIGN testcase-design audit correction

- IntentDesign audit found the query tests conflated recoverable auto-alignment and unrecoverable alignment failure by asserting both outcomes against the same `runReadinessMatrix()` output. The corrected design adds separate recoverable and unrecoverable BP fixtures in `productionDefaultRetrievalHarness.js`.
- Recoverable fixtures now require exactly one observable `semantic-auto-alignment-attempt`, preservation of the original query, exactly one retry after successful alignment, two readiness reads total, and provider/vector work only after alignment. The current RED signal is `BP_AUTOALIGN_RECOVERABLE_ALIGNMENT_NOT_ATTEMPTED:semantic-disabled`.
- Unrecoverable fixtures now require exactly one failed alignment attempt, preserved original query evidence, `SEMANTIC_AUTO_ALIGNMENT_FAILED`, `fullSnapshotFallback:false`, and zero provider/vector work after failed alignment. The current RED signal is `BP_AUTOALIGN_UNRECOVERABLE_ALIGNMENT_NOT_ATTEMPTED:semantic-disabled`.
- Updated `.argo/temp/ImplementationToCodingHandoff.json`, `.argo/temp/bp-autoalign-expected-failures.json`, `tests/ARCHITECTURE.md`, and both BP query entrypoint paths. `validateStageHandoff(stage=implementation-to-coding)` passes by script and MCP after the correction.

## 2026-07-29 BP-AUTOALIGN CodingAndReparing blocker correction

- Classified the reported Agent-unaware and SP04 failures as an ImplementationDesign test/handoff gap, not a Coding bug: the production code was returning accepted BP-AUTOALIGN auto-alignment diagnostics, while SP04 still froze pre-BP `APPROVED_SECRET_REQUIRED` evidence and Agent-unaware still required provider work for every non-Aligned state.
- Repaired `productionDefaultRetrievalHarness.js` so SP04 unified failure evidence uses an unrecoverable script-owned alignment fixture, expects `SEMANTIC_AUTO_ALIGNMENT_FAILED`, verifies one alignment attempt, and preserves `fullSnapshotFallback:false` with no secret leakage.
- Repaired `automaticSemanticLifecycleHarness.js` so Agent-unaware accepts stable script-owned auto-alignment failure diagnostics when alignment cannot complete, while still rejecting old manual/direct-readiness behavior and fallback.
- Updated `.argo/temp/ImplementationToCodingHandoff.json` and `.argo/temp/bp-autoalign-expected-failures.json`: all five BP-AUTOALIGN entrypoints now have current status `passed` and no BP expected failures remain. Full runner baseline after correction is `61 total / 60 passed / 1 failed / 0 missing acceptanceCriteria`; the only remaining failure is out-of-scope `ExplicitAcceptanceTestcase-SP-03-DefaultVectorRetrieval`.

## 2026-07-29 BP-AUTOALIGN handoff critical guard correction

- Classified the critical guard failures as an ImplementationDesign handoff/frozen-set completeness gap. The old guards assumed `codingTargets[].path`, previous CSL/SP-05 entrypoint sets, and frozen WP-P2 `defaultSemanticRetrieval.js`; BP-AUTOALIGN correctly uses grouped `targetPaths[]` and reopens default retrieval for query auto-alignment.
- Updated the five affected critical guards to normalize `path` and `targetPaths`, require the exact BP-AUTOALIGN authorized production target set, freeze unrelated provider/runtime/persistence boundaries, and trace only the five BP-AUTOALIGN explicit entrypoints.
- Cleaned `.argo/temp/ImplementationToCodingHandoff.json`: removed stale passing-entry `failureReason` fields, froze the non-BP production boundaries, preserved runner-owned `deliveryStatus`, and replaced stale BP open gaps with the remaining out-of-scope SP-03 failure.
- Affected critical guards now pass individually, and `validateStageHandoff(stage=implementation-to-coding)` passes. Full runner was not rerun because these changes are handoff/guard metadata and do not alter graph-mounted acceptance evidence or deliveryStatus.

## 2026-07-29 BP-AUTOALIGN Coding delivery audit

- Audited CodingAndReparing final evidence against `.argo/temp/ImplementationToCodingHandoff.json` and the accepted BP-AUTOALIGN implementation contract. Expected failures are empty, all five explicit BP entrypoints are marked passed, six critical guards are scoped to BP-AUTOALIGN, and `design/KG/test-failure-records.json` contains only the out-of-scope SP-03 attribution failure.
- Fresh focused audit commands passed: `runBusinessReliableSemanticLifecycle.js`, `runMutationIndexLifecycle.js`, `runProductionSemanticReadinessGate.js`, `runTypedMcpQueryContract.js`, all six handoff critical guards, and `validateStageHandoff(stage=implementation-to-coding)`.
- Audit result is PASS for BP-AUTOALIGN handoff-scoped delivery. Final IntentDesign delivery audit may proceed; remaining SP-03 default vector retrieval failure is outside the BP-AUTOALIGN handoff scope.

## 2026-07-29 BP-AUTOALIGN pressure-test repair audit

- Audited the CodingAndReparing pressure-test repair for the persistent incremental lifecycle. The production change keeps canonical content available for embedding but strips nested `content` from persistable Neo4j records before `upsertRecords`, preserving primitive-only projection properties while keeping businessComplete/Aligned write semantics.
- Reflected the new support regressions in `.argo/temp/ImplementationToCodingHandoff.json` and `tests/ARCHITECTURE.md`: `bp-autoalign-incremental-record-primitive-properties.test.js` protects primitive-only write-side projection persistence, and `bp-autoalign-fresh-mcp-pressure.test.js` protects fresh-process public MCP write/query/restore pressure behavior.
- Query-side fail-closed behavior remains owned by `defaultSemanticRetrieval.js` and unchanged by the repair: unrecoverable automatic alignment still returns `SEMANTIC_AUTO_ALIGNMENT_FAILED` with `fullSnapshotFallback:false` and no full-snapshot fallback. The long-lived Cursor MCP process can remain stale until restarted because Node module cache/server lifetime can retain the pre-repair lifecycle implementation.

## 2026-08-04 View15 active-authority consistency handoff

- Consumed `.argo/temp/IntentToImplementationHandoff.json` for `view15-active-authority-requirement` / Current Fifteen Wording With Historical Truth. Intent context confirmed the Motivation and Implementation/Migration viewpoints, the `view15-rel-authority-goal` Realization, and the mounted `ExplicitAcceptanceTestcase-VIEW15-CONSISTENCY`; no upstream intent graph change or `ImplementationToIntentTraceProposal` is needed.
- Materialized the ImplementationDesign handoff as verification-only because `node tests/explicit/entries/runView15Consistency.js` exited 0. The active-authority contracts bind current Fifteen/15 wording and diagnostics to `.argo/scripts/graph-semantics.js`, `.argo/scripts/systemarchitecture-mcp-server.js`, `design/validator/intent-architecture-mcp-validation.md`, and `tests/mcp/systemarchitecture-mcp.test.js`, while the historical decision record remains frozen as historical truth.
- Updated `tests/architecture/view-capacity-policy/implementation-traceability.guard.js` so the active handoff slot protects `view15-active-authority-requirement`, `ExplicitAcceptanceTestcase-VIEW15-CONSISTENCY`, the shared View15 Harness, and the four View15 critical guards rather than the prior overflow/indirect-growth handoff scope.
- Wrote `.argo/temp/ImplementationToCodingHandoff.json` and `.argo/temp/view15-consistency-expected-failures.json`; expected failures are empty. `argo.validateStageHandoff(stage=implementation-to-coding)` passed after normalizing the handoff file to UTF-8 without BOM, and the four View15 critical guards passed.
- Full `argo.runArchitectureTests` pre-coding baseline completed through MCP: `68 total / 65 passed / 3 failed / 0 missing acceptanceCriteria`. `ExplicitAcceptanceTestcase-VIEW15-CONSISTENCY` passed and `view15-active-authority-requirement` remains runner-owned `deliveryStatus=delivered`. The three global failures are out of this WorkPackage scope: `ExplicitAcceptanceTestcase-TS-07`, `ExplicitAcceptanceTestcase-W3-1-MutationEmbeddingVectorE2E`, and `ExplicitAcceptanceTestcase-SP-03-DefaultVectorRetrieval`; details are refreshed in `design/KG/test-failure-records.json`.

## 2026-08-04 View15 active-authority audit correction

- IntentDesign audit found the active MCP feature guide `design/mcp/意图架构 MCP 功能列表.md` was omitted from the active-authority consistency inventory and still contains current seven-element wording. This is an ImplementationDesign coverage gap, not a Coding/Repair implementation to perform in this stage.
- Accepted the useful candidate corrections in the working tree: the Harness now includes the MCP guide in `activeAuthoritySurfaceEvidence()`, local contracts name the guide as an active authority surface, and the traceability guard requires the guide in handoff target authorization.
- Strengthened `tests/architecture/view-capacity-policy/explicit-entrypoint-correctness.guard.js` to protect the new MCP guide coverage marker and the active-authority failure categories `VIEW15_CONSISTENCY_GRAPH_AUTHORITY_MISMATCH`, `VIEW15_CONSISTENCY_ACTIVE_SEVEN_AUTHORITY`, and `VIEW15_CONSISTENCY_ACTIVE_FIFTEEN_AUTHORITY_MISSING`.
- Updated `.argo/temp/ImplementationToCodingHandoff.json` and `.argo/temp/view15-active-authority-expected-failures.json`: `ExplicitAcceptanceTestcase-VIEW15-CONSISTENCY` is now an expected RED with `VIEW15_CONSISTENCY_ACTIVE_SEVEN_AUTHORITY` for `design/mcp/意图架构 MCP 功能列表.md`; CodingAndReparing is authorized to repair only that active guide wording while preserving historical records.
- Focused evidence: `node tests/explicit/entries/runView15Consistency.js` fails as expected on `VIEW15_CONSISTENCY_ACTIVE_SEVEN_AUTHORITY`; the four View15 critical guards pass. Full `argo.runArchitectureTests` pre-coding baseline is `68 total / 64 passed / 4 failed / 0 missing acceptanceCriteria`; the in-scope failure is `ExplicitAcceptanceTestcase-VIEW15-CONSISTENCY`, and out-of-scope failures remain `ExplicitAcceptanceTestcase-TS-07`, `ExplicitAcceptanceTestcase-W3-1-MutationEmbeddingVectorE2E`, and `ExplicitAcceptanceTestcase-SP-03-DefaultVectorRetrieval`. Runner-owned `deliveryStatus` for `view15-active-authority-requirement` is now `not_delivered` until Coding repairs the active MCP guide.

## 2026-08-04 View15 active-authority contract inventory correction

- IntentDesign re-audit accepted the MCP guide correction but found that three frozen active authority contracts were still missing from the explicit consistency inventory: `OVERALL_ARCHITECTURE.md`, `.argo/scripts/ARCHITECTURE.md`, and `tests/ARCHITECTURE.md`.
- Updated `activeAuthoritySurfaceEvidence()` to include those contracts with phrase-level Fifteen/15 assertions, and updated the explicit-entrypoint correctness guard so the contract paths remain protected inventory coverage.
- Updated `.argo/temp/ImplementationToCodingHandoff.json` control-point wording and `.argo/temp/view15-active-authority-expected-failures.json` activeAuthorityInventory. Coding authorization remains narrowed to `design/mcp/意图架构 MCP 功能列表.md`; the contract surfaces are frozen evidence and not Coding repair targets.
- Focused evidence remains the intended RED: `node tests/explicit/entries/runView15Consistency.js` fails only on `VIEW15_CONSISTENCY_ACTIVE_SEVEN_AUTHORITY` for the MCP guide. The four View15 critical guards pass.
