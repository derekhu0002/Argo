# MCP Semantic Query Contract Decision Tree

Task-tidy session: mcp-semantic-query-contract-current-session

Business Partner consensus timestamp: 2026-07-28 00:21 UTC+8

Selected viewpoint: Application Usage Viewpoint

Stakeholder concern: Agent-facing ARGO MCP architecture reads must expose only canonical intent architecture full snapshots or structurally closed canonical subsets.

Modeling purpose: deciding and handoff preparation

View rationale: The affected behavior is the application usage path from Agent request semantics through `getSystemArchitecture` to consumer-readable intent architecture context. Existing relevant views already use Application Usage Viewpoint for request semantics, semantic availability, coherent result closure, and graph-tidy full-read boundaries.

| id | parentId | level | question | MECE dimension | branchStatus | recommendedAnswer | humanDecision | businessRationale | dependencyPremises | risks | acceptanceControlPoint | acceptanceObservationPoint | horizontalConcern | verticalDependency | evidenceSource |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| BP-MCP-SEM-ROOT |  | 0 | What is the SMART business problem for ARGO MCP semantic retrieval? | Root problem definition; all later branches refine request contract, success payload, structural closure, and failure handling. These four concerns are independent because they decide different lifecycle moments: request admission, successful content domain, closure completion, and invalid graph handling. Together they cover the public semantic retrieval contract. | accepted | Ordinary semantic retrieval through `getSystemArchitecture` must return only the original intent architecture full graph or a structurally closed subset of original graph objects. Debug and derived evidence output must be removed from the public semantic query contract. | accepted by human after challenge and refinement | Agents need a trustworthy architecture read boundary whose successful content is canonical intent architecture, not retrieval internals, summaries, or debug evidence. | `getSystemArchitecture` is the sole public read/query surface; canonical JSON is authoritative; ordinary semantic queries are governed by readiness and retrieval, while omitted-query and graph-tidy remain full snapshot paths. | Ambiguous wording of "partial" could permit field-level truncation or summaries unless explicitly forbidden. | User states desired interface semantics; task-tidy reconstructs decision tree from Business Partner consensus. | Decision tree persisted and integrated into intent architecture without re-litigating accepted business decisions. | Public MCP read contract; canonical graph authority; semantic result coherence | Root precedes all request, payload, closure, and validation decisions. | Human requirement in chat; `OVERALL_ARCHITECTURE.md`; `design/KG/SystemArchitecture.json`; `.argo/scripts/systemarchitecture-mcp-server.js`; `tests/ARCHITECTURE.md` |
| BP-MCP-SEM-REQ | BP-MCP-SEM-ROOT | 1 | May callers control response shape or evidence detail? | Partition by presence of response-shape control. A request either has no such field, has ordinary business query fields, or contains response-shape/debug/evidence controls; no request can be outside these categories. | accepted | Reject `responseProfile`, `detail`, `outputMode`, and equivalent debug/full/evidence controls as illegal query contract fields. Do not ignore or downgrade them to success. | accepted by human: "既然如此，为何还能让调用方传入这些字段和值呢" led to stricter rejection; then "同意". | A public contract that forbids debug/evidence output should close the input door as well as the output door. Rejecting is more auditable than silently ignoring. | Existing implementation has `shouldReturnDebugSemanticResult` and `expandWith` guidance, so the graph must state the stronger business rule. | Silent ignore would allow future regression; accepting unknown shape fields would keep a hidden debug contract alive. | Acceptor calls ordinary semantic query with `responseProfile`, `detail`, `outputMode`, and values `debug`, `full`, `evidence`, including case variants and combinations. | Every such request is rejected as illegal query contract; semantic retrieval is not executed; response contains no debug evidence, vector, queryTemplate, parameterContract, archimateSemantics, expandWith, and no success canonical subset. | MCP interface request validation | Must be enforced before semantic retrieval, canonical subset selection, endpoint closure, or View closure. | Chat consensus; `.argo/scripts/systemarchitecture-mcp-server.js` lines containing debug profile logic; `tests/explicit/entries/runBusinessSemanticSummaryProfile.js` |
| BP-MCP-SEM-COMPAT | BP-MCP-SEM-REQ | 2 | Are omitted-query and graph-tidy still allowed to return full snapshots? | Dispatch category partition: omitted-query full snapshot, graph-tidy full snapshot, ordinary semantic query. These are mutually exclusive public read modes and cover known public successful read paths. | accepted | Preserve omitted-query and graph-tidy as canonical full-snapshot reads; classify them as full original architecture reads, not debug or evidence modes. | accepted by human implicitly with no objection to compatibility branch. | Full snapshot reads satisfy "original intent architecture full graph" and are needed for graph-tidy and exact canonical reads. | Existing contracts require no-argument and graph-tidy exact snapshot compatibility. | Misclassifying graph-tidy as debug could break accepted compatibility; allowing graph-tidy to carry debug controls would break request validation. | Acceptor calls omitted-query and graph-tidy, including anchored graph-tidy. | Response is exact canonical full snapshot with zero semantic retrieval/debug/evidence side effects. | Compatibility read modes | Compatibility branch is admitted only when no illegal response-shape controls are present. | `OVERALL_ARCHITECTURE.md`; `tests/explicit/entries/runTypedMcpQueryContract.js`; `tests/ARCHITECTURE.md` |
| BP-MCP-SEM-PAYLOAD | BP-MCP-SEM-ROOT | 1 | What may a successful ordinary semantic query return? | Payload source partition: original canonical graph objects, retrieval-process objects, or derived explanatory objects. These categories are mutually exclusive by origin and exhaust all observed success payload types. | accepted | Return only canonical `elements`, `relationships`, and `views` object collections, selected as an object-set subset or full graph. Do not return summaries, provenance, semantic index evidence, policy summaries, seeds, or derived businessObjects. | accepted by human through continued "同意" after canonical subset discussion. | "Original intent architecture" means canonical graph content, not retrieval evidence or synthesized summaries. | Current default business-summary output is derived and therefore conflicts with the new business rule. | Derived wrappers can drift from canonical graph and obscure whether consumers are reading architecture or retrieval diagnostics. | Acceptor runs ordinary semantic query that matches Element, Relationship, and View. | Successful business payload contains only original graph object collections; no `businessObjects`, `semanticSeeds`, `hitReasons`, `policySummary`, `boundarySummary`, `semanticIndex`, `descriptionSummary`, `testCoverage`, or other derived structures. | Canonical payload domain | Happens after request validation and before structural closure verification. | Chat consensus; `.argo/scripts/systemarchitecture-mcp-server.js`; `tests/explicit/entries/runBusinessSemanticSummaryProfile.js`; `design/KG/SystemArchitecture.json` |
| BP-MCP-SEM-FIELDS | BP-MCP-SEM-PAYLOAD | 2 | Does "partial" allow field-level truncation or only object-set selection? | Subset granularity partition: object-set subset, field subset, or transformed/renamed fields. These are distinct ways to interpret "partial" and exhaust practical payload-shaping choices. | accepted | "Partial" means object-set subset only. Each returned object must be byte/deep equivalent to its canonical graph object for that id. Field truncation, summary, rename, or added derived fields are forbidden. | accepted by human after Business Partner warned that field-level partial would reopen summary mode. | Consumers should not need a second canonical read to recover omitted fields from an allegedly returned architecture object. | Canonical graph object identity must be traceable by id. | Field-level truncation could masquerade as "subset" and reintroduce business-summary behavior. | Acceptor compares every returned object by id against canonical `SystemArchitecture.json`. | Every returned object deeply equals the canonical object; selection may omit whole objects but never alter returned object fields or values. | Canonical object identity | Refines BP-MCP-SEM-PAYLOAD and constrains all closure-added objects. | Chat consensus; current summary code truncates descriptions and reshapes fields; canonical JSON evidence |
| BP-MCP-SEM-CLOSURE | BP-MCP-SEM-ROOT | 1 | How must a semantic subset remain structurally coherent? | Returned object reference partition: Element has no mandatory returned-object reference, Relationship references endpoints, View references included elements and relationships. These three object types exhaust successful payload object types. | accepted | Apply minimal structural closure after semantic selection: Relationship endpoints must be included; View declared members and View-included relationship endpoints must be included. | accepted by human after third branch and final "同意". | A subset with dangling relationships or incomplete Views is not a usable architecture fragment. | Existing intent graph already contains endpoint and View closure elements; new rule strengthens them as canonical-subset response obligations. | Over-broad closure can become hidden graph expansion; under-closure returns broken architecture fragments. | Acceptor queries matched Relationship and matched View fixtures whose referenced objects were not textually matched. | Returned subset includes all required canonical endpoints and View members while excluding unrelated neighbors. | Structural coherence; endpoint closure; View closure | Runs after semantic object selection and before success response finalization. | Chat consensus; `grag-endpoint-closure`; `grag-view-closure`; `tests/ARCHITECTURE.md` |
| BP-MCP-SEM-ELEMENT | BP-MCP-SEM-CLOSURE | 2 | If only an Element is selected, what else must be included? | Element-specific reference rule; Element does not mandate relationship or View inclusion. This is separate from Relationship and View because its closure obligations are empty. | accepted | Return the Element object only unless other objects independently match or are required by another returned object's closure. | accepted by human through agreement to "Element 命中不自动拉相邻关系". | Element hits should not explode into neighborhood expansion. | Canonical graph may contain many neighboring relationships and Views. | Returning neighbors would reduce precision and violate minimal subset semantics. | Acceptor queries an Element with many adjacent relationships and Views, with no independent relationship/View match. | Only the matched Element is returned, except for independently matched objects; no automatic adjacent Relationship or View expansion. | Minimal closure boundary | Element rule precedes Relationship/View closure only in the sense that it has no closure side effects. | Chat consensus |
| BP-MCP-SEM-REL | BP-MCP-SEM-CLOSURE | 2 | If a Relationship is selected, what else must be included? | Relationship endpoint partition: source endpoint, target endpoint, and non-endpoint neighbors. The first two are mandatory and the third is excluded. | accepted | Include the selected Relationship and both canonical endpoint Elements. Do not include endpoint neighbors, endpoint-owned Views, or unrelated relationships by closure alone. | accepted by human. | Every returned Relationship must be interpretable without external lookup. | Relationship objects use `source_id` and `target_id`; endpoint closure is already a graph concern. | Missing endpoints produce unreadable or misleading relationship fragments; neighbor expansion creates over-broad results. | Acceptor triggers Relationship seed whose endpoints are not semantic matches. | Relationship and both source/target Elements are returned as original graph objects; no other neighbor objects are pulled solely by endpoint closure. | Relationship endpoint closure | After semantic relationship selection; before View closure final checks. | `grag-endpoint-closure` in `design/KG/SystemArchitecture.json`; `tests/ARCHITECTURE.md`; chat consensus |
| BP-MCP-SEM-VIEW | BP-MCP-SEM-CLOSURE | 2 | If a View is selected, what else must be included? | View membership partition: declared included elements, declared included relationships, endpoints of included relationships, and overlapping Views. First three are mandatory; overlapping Views are excluded unless independently selected. | accepted | Include selected View, all declared included Elements, all declared included Relationships, and both endpoint Elements for each included Relationship. Do not cascade into overlapping Views. | accepted by human. | A View is a coherent architecture projection only if its declared members and in-View relationship endpoints are present. | Existing `grag-view-closure` already states complete non-cascading View closure. | Returning View metadata without members is incomplete; cascading into overlapping Views causes uncontrolled expansion. | Acceptor queries a selected View with members and in-View relationships whose endpoints are not semantic matches. | The selected View, all included objects, and relationship endpoints are returned; no unrelated View is returned through shared membership alone. | Complete View closure | After semantic View selection; invokes Relationship endpoint closure for View-included relationships. | `grag-view-closure` in `design/KG/SystemArchitecture.json`; `tests/ARCHITECTURE.md`; chat consensus |
| BP-MCP-SEM-NOCASCADE | BP-MCP-SEM-VIEW | 3 | Do shared members cause overlapping View cascade? | Overlap selection partition: independently matched View, explicitly requested View, or merely shared-member View. Only the first two are admissible. | accepted | Do not include overlapping Views merely because they share elements or relationships with a selected View. | accepted by human. | Prevents structural closure from becoming all-diagram expansion. | Existing W6 View closure scope says shared members do not cause implicit cascade. | Overlapping View cascade can leak unrelated concerns and break semantic precision. | Acceptor queries a View that shares members with another View that does not independently satisfy query selection. | Shared member objects may appear, but the non-matched overlapping View does not appear. | Non-cascading View closure | Refines BP-MCP-SEM-VIEW after member closure. | `grag-view-closure` scope boundary; chat consensus |
| BP-MCP-SEM-BROKEN | BP-MCP-SEM-CLOSURE | 2 | What if canonical graph references are broken? | Structural integrity result partition: all references resolvable, Relationship endpoint broken, View member/reference broken. These states are mutually exclusive at the failing reference level and cover closure failure modes. | accepted | Fail closed on broken references. Do not return partial success, do not drop broken objects, and do not hide missing endpoints or View members. | accepted by human. | A successful canonical subset must be structurally coherent; broken references are graph integrity failures, not retrievable partial content. | Canonical graph is authoritative, but authority does not mean broken references can be presented as coherent architecture. | Silent discard would hide architecture corruption; partial success would mislead consumers. | Acceptor runs query against fixture with missing/deleted/cross-version Relationship endpoint or missing View member/relationship. | No successful subset is returned; response names structural failure; no partial Relationship or incomplete View is emitted. | Graph integrity failure handling | Final gate after closure; blocks success response when closure cannot be satisfied. | Chat consensus; existing DT-13 structural-error semantics |
| BP-MCP-SEM-ACCEPTANCE | BP-MCP-SEM-ROOT | 1 | What explicit business acceptance cases should be mounted? | Acceptance partition by risk: illegal input, canonical-only output, relationship endpoint closure, View membership closure, no View cascade, broken reference rejection. These correspond one-to-one to the accepted business risks and cover all decision branches. | accepted | Mount six explicit acceptance testcase semantics: reject response shape control; canonical object subset only; relationship endpoint closure; View membership closure; no overlapping View cascade; broken reference rejection. | accepted by human. | Each key business rule needs an acceptor control point and observation point. | Existing tests may be extended or new mounted testcases may be added by downstream agents; BusinessPartner does not physicalize entrypoints. | Missing testcase mapping lets implementation satisfy only part of the decision tree. | See each testcase-specific control point above. | See each testcase-specific observation point above. | Acceptance semantics | Depends on all accepted decision nodes and feeds ImplementationDesign physicalization later. | Chat consensus; current explicit acceptance inventory in `tests/ARCHITECTURE.md` |
